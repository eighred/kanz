# BUILDER-STAGE LOCK — the protoc toolchain that emits the Python schema SDK.
#
# Separate from requirements.txt on purpose: this is what GENERATES /app/gen in
# the builder stage, and none of it ships in the runtime image. Merging the two
# would put setuptools and a second protobuf into the deployed layer to satisfy a
# build-time need.
#
# grpcio-tools was already version-pinned (kanz-py/Dockerfile:20, `==1.68.1`) —
# but a version is not an artifact, and its THREE TRANSITIVE dependencies were
# unpinned and unnamed. The protoc that emits every message this platform's AI
# layer reads was therefore partly floating, and a stub generated by a different
# protobuf than the one recorded here is a different wire contract.
#
# TO REGENERATE (from the repository root, network required) — same procedure as
# requirements.txt, with the grpcio-tools pin as the only input:
#
#   python -m pip install --dry-run --ignore-installed --report /tmp/b.json \
#     --target /tmp/discard \
#     --python-version 3.12 --implementation cp --abi cp312 \
#     --platform manylinux2014_x86_64 --only-binary=:all: \
#     "grpcio-tools==1.68.1"
#
# Moving grpcio-tools is a deliberate act: change the specifier above, rerun,
# and REGENERATE THE GO SDK TOO (`cd kanz-schemas && buf generate`) so the two
# language SDKs are not emitted by different protoc versions.
#
# PLATFORM: linux/amd64, CPython 3.12 — see requirements.txt's PLATFORM note.
# grpcio-tools 1.68.1 publishes no manylinux aarch64 wheel, which is the reason
# an arm64 build of this image is not possible at any pinning level.
#
# GENERATED 2026-08-06 against pypi.org. Every hash was resolved by pip AND
# independently re-fetched from https://pypi.org/pypi/<name>/<version>/json and
# compared; none is hand-written.

grpcio==1.83.0 \
    --hash=sha256:aeb339838db07600481ef869507279b75326c75eac6d10f7afa62a0da1d2bcdd
grpcio-tools==1.68.1 \
    --hash=sha256:c81d0be6c46fcbcd2cd126804060a95531cdf6d779436b2fbc68c8b4a7db2dc1
protobuf==7.35.1 \
    --hash=sha256:11d6b0ec246892d85215b0a13ca6e0233cf5284b68f0ac02646427f4ff88a799 \
    --hash=sha256:230a75ddfc2de4806e56696ce9640c1cdfdb6543b7cfce98d42a4c0a0e7bdb87 \
    --hash=sha256:24f857477359a85c0c235261b8ba905fd51b2562f4a64ca1df5473f29850cbf6 \
    --hash=sha256:353652e4efd0bca5b5fc2656abf8307ef351f0cf938c9eba09f0e09c20a25c30 \
    --hash=sha256:4bc97768d8fe4ad6743c8a19403e314511ed9f6d13205b687e52421c023ac1b9 \
    --hash=sha256:74758715c53d7158fb76caf4f0cfdacc5329a4b1bb994f865d6cf302d413a1c4 \
    --hash=sha256:b73f9489a4b8b1c9cb1f8ed951c736392592edb24b9d6819f36d2e10b171d5b4 \
    --hash=sha256:ce115a26fe0c39a2c29973d914d327e516a6455464489fe3cd1e51a1b354f81a
setuptools==83.0.0 \
    --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
typing-extensions==4.16.0 \
    --hash=sha256:481caa481374e813c1b176ada14e97f1f67a4539ce9cfeb3f350d78d6370c2e8
