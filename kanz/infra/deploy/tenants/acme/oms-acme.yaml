# Code generated by internal/tenantgen (cmd/kanz-tenantgen). DO NOT EDIT BY HAND.
#
# Rendered from infra/deploy/oms-deploy.yaml for one tenant (MT-02). CI
# (test/arch/tenant_compute_test.go) re-renders this file from the live base
# on every run and fails the build on any byte difference. If the base
# changed underneath this file, regenerate it — do not hand-patch:
#   go run ./cmd/kanz-tenantgen -tenant <tenant>
---
apiVersion: v1
imagePullSecrets:
  - name: ghcr-pull
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  ports:
    - name: http
      port: 8090
      targetPort: 8090
  selector:
    app: oms
    tenant: acme
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: oms
      tenant: acme
  template:
    metadata:
      labels:
        app: oms
        app.kubernetes.io/part-of: kanz
        tenant: acme
    spec:
      containers:
        - env:
            - name: OMS_DATABASE_URL_FILE
              value: /run/secrets/db/database-url
            - name: OMS_TENANT
              value: acme
            - name: OMS_REQUIRE_MANDATE
              value: "false"
            - name: OMS_LISTEN
              value: :8090
            - name: OMS_NATS_URL
              value: nats://nats.kanz-messaging.svc:4222
            - name: OMS_SOURCE
              value: oms
            - name: OMS_VENUE_ENDPOINTS
              value: XBIN/binance-main=venue-binance.kanz-services.svc:9000,XOKX/okx-sub-1=venue-okx.kanz-services.svc:9000
            - name: OMS_REQUIRE_VERIFIED_ACCOUNT
              value: "false"
            - name: OMS_VENUE_ACCOUNTS
              value: ""
            - name: OMS_REQUIRE_VENUE_ACCOUNT
              value: "false"
            - name: OMS_CONSUMER_GROUP
              value: oms
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spiffe/spire-agent.sock
          image: ghcr.io/eighred/oms@sha256:c5cd064bb77fab3571a57d549aa571f51e28719229d5cdec0c1191489589685a
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 15
          name: oms
          ports:
            - containerPort: 8090
              name: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
            - mountPath: /run/spiffe
              name: spiffe
              readOnly: true
            - mountPath: /tmp
              name: tmp
      initContainers:
        - args:
            - --dir
            - /migrations/oms
          env:
            - name: KANZ_MIGRATE_DATABASE_URL_FILE
              value: /run/secrets/db/migrate-database-url
          image: ghcr.io/eighred/kanz-migrate@sha256:909865997d2e96fd9898c5c34a6f11ade3cd65b71a18f1424d0bcda118e0e689
          name: migrate
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: oms-acme
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: oms
              tenant: acme
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
      volumes:
        - emptyDir: {}
          name: tmp
        - csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: oms-db
          name: db
        - csi:
            driver: csi.spiffe.io
            readOnly: true
          name: spiffe
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: oms
      tenant: acme
