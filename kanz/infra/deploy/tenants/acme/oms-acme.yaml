# Code generated by internal/tenantgen (cmd/kanz-tenantgen). DO NOT EDIT BY HAND.
#
# Rendered from infra/deploy/oms-deploy.yaml for one tenant (MT-02). CI
# (test/arch/tenant_compute_test.go) re-renders this file from the live base
# on every run and fails the build on any byte difference. If the base
# changed underneath this file, regenerate it — do not hand-patch:
#   go run ./cmd/kanz-tenantgen -tenant <tenant>
---
apiVersion: v1
imagePullSecrets:
  - name: ghcr-pull
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  ports:
    - name: http
      port: 8090
      targetPort: 8090
    - name: query
      port: 9090
      targetPort: 9090
  selector:
    app: oms
    tenant: acme
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: oms
      tenant: acme
  template:
    metadata:
      annotations:
        prometheus.io/port: "8090"
        prometheus.io/scrape: "true"
      labels:
        app: oms
        app.kubernetes.io/part-of: kanz
        tenant: acme
    spec:
      containers:
        - env:
            - name: OMS_DATABASE_URL_FILE
              value: /run/secrets/db/database-url
            - name: OMS_TENANT
              value: acme
            - name: OMS_REQUIRE_MANDATE
              value: "true"
            - name: OMS_LISTEN
              value: :8090
            - name: OMS_GRPC_LISTEN
              value: :9090
            - name: OMS_NATS_URL
              value: nats://nats.kanz-messaging.svc:4222
            - name: OMS_SOURCE
              value: oms
            - name: OMS_VENUE_ENDPOINTS
              value: XBIN/binance-main=venue-binance.kanz-services.svc:9000,XOKX/okx-sub-1=venue-okx.kanz-services.svc:9000
            - name: OMS_DEFAULT_VENUE_MIC
              value: XBIN
            - name: OMS_REQUIRE_VERIFIED_ACCOUNT
              value: "false"
            - name: OMS_REQUIRE_ORDER_TYPE_SUPPORT
              value: "false"
            - name: OMS_REQUIRE_DUAL_CONTROL
              value: "false"
            - name: OMS_DUAL_CONTROL_MIN_NOTIONAL
              value: ""
            - name: OMS_VENUE_ACCOUNTS
              value: ""
            - name: OMS_REQUIRE_VENUE_ACCOUNT
              value: "false"
            - name: OMS_PRICE_MAX_AGE
              value: 30s
            - name: OMS_QUOTE_MAX_AGE
              value: 6s
            - name: OMS_CONSUMER_GROUP
              value: oms
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spiffe/spire-agent.sock
          image: ghcr.io/eighred/oms@sha256:94e500afa9492a360902da2fa249c219dac3a1e158b7904697fb77b5c08a5b74
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 15
          name: oms
          ports:
            - containerPort: 8090
              name: http
            - containerPort: 9090
              name: query
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
            - mountPath: /run/spiffe
              name: spiffe
              readOnly: true
            - mountPath: /tmp
              name: tmp
      initContainers:
        - args:
            - --dir
            - /migrations/oms
          env:
            - name: KANZ_MIGRATE_DATABASE_URL_FILE
              value: /run/secrets/db/migrate-database-url
          image: ghcr.io/eighred/kanz-migrate@sha256:43799cfa71cb4ff6d5c290c2941d2f19d76b458d0c2ec0c110ee9294fc588334
          name: migrate
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: oms-acme
      terminationGracePeriodSeconds: 60
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: oms
              tenant: acme
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
      volumes:
        - emptyDir: {}
          name: tmp
        - csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: oms-db
          name: db
        - csi:
            driver: csi.spiffe.io
            readOnly: true
          name: spiffe
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: oms-acme
  namespace: kanz-services
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: oms
      tenant: acme
