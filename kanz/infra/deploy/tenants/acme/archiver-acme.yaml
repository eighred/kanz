# Code generated by internal/tenantgen (cmd/kanz-tenantgen). DO NOT EDIT BY HAND.
#
# Rendered from infra/deploy/archiver-deploy.yaml for one tenant (MT-02). CI
# (test/arch/tenant_compute_test.go) re-renders this file from the live base
# on every run and fails the build on any byte difference. If the base
# changed underneath this file, regenerate it — do not hand-patch:
#   go run ./cmd/kanz-tenantgen -tenant <tenant>
---
apiVersion: v1
imagePullSecrets:
  - name: ghcr-pull
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: archiver-acme
  namespace: kanz-services
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: archiver-acme
  namespace: kanz-services
spec:
  ports:
    - name: http
      port: 8086
      targetPort: 8086
  selector:
    app: archiver
    tenant: acme
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    kanz.io/singleton: "true"
    tenant: acme
  name: archiver-acme
  namespace: kanz-services
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: archiver
      tenant: acme
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        prometheus.io/port: "8086"
        prometheus.io/scrape: "true"
      labels:
        app: archiver
        app.kubernetes.io/part-of: kanz
        tenant: acme
    spec:
      containers:
        - env:
            - name: ARCHIVER_LISTEN
              value: :8086
            - name: ARCHIVER_NATS_URL
              value: nats://nats.kanz-messaging.svc:4222
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spiffe/spire-agent.sock
            - name: ARCHIVER_KAFKA_BROKERS
              value: kafka.kanz-messaging.svc:9094
            - name: ARCHIVER_OTLP_ENDPOINT
              value: otel-collector.kanz-observability.svc:4317
            - name: ARCHIVER_TENANT
              value: acme
          image: ghcr.io/eighred/archiver@sha256:f201c12594c7fc2f9a29ae7ad4a3c91e9e08a68afc4e81bae055d04f6a6a2553
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8086
            initialDelaySeconds: 5
            periodSeconds: 15
          name: archiver
          ports:
            - containerPort: 8086
              name: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8086
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /tmp
              name: tmp
            - mountPath: /run/spiffe
              name: spiffe
              readOnly: true
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: archiver-acme
      terminationGracePeriodSeconds: 60
      volumes:
        - emptyDir: {}
          name: tmp
        - csi:
            driver: csi.spiffe.io
            readOnly: true
          name: spiffe
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: archiver-acme
  namespace: kanz-services
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: archiver
      tenant: acme
