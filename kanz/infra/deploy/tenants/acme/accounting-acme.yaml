# Code generated by internal/tenantgen (cmd/kanz-tenantgen). DO NOT EDIT BY HAND.
#
# Rendered from infra/deploy/accounting-deploy.yaml for one tenant (MT-02). CI
# (test/arch/tenant_compute_test.go) re-renders this file from the live base
# on every run and fails the build on any byte difference. If the base
# changed underneath this file, regenerate it — do not hand-patch:
#   go run ./cmd/kanz-tenantgen -tenant <tenant>
---
apiVersion: v1
imagePullSecrets:
  - name: ghcr-pull
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: accounting-acme
  namespace: kanz-services
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: accounting-acme
  namespace: kanz-services
spec:
  ports:
    - name: http
      port: 8101
      targetPort: 8101
    - name: metrics
      port: 8080
      targetPort: 8080
  selector:
    app: accounting
    tenant: acme
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: accounting-acme
  namespace: kanz-services
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: accounting
      tenant: acme
  template:
    metadata:
      annotations:
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"
      labels:
        app: accounting
        app.kubernetes.io/part-of: kanz
        tenant: acme
    spec:
      containers:
        - env:
            - name: ACCOUNTING_DATABASE_URL_FILE
              value: /run/secrets/db/database-url
            - name: ACCOUNTING_TENANT
              value: acme
            - name: ACCOUNTING_LISTEN
              value: :8101
            - name: ACCOUNTING_METRICS_LISTEN
              value: :8080
            - name: ACCOUNTING_NATS_URL
              value: nats://nats.kanz-messaging.svc:4222
            - name: SPIFFE_ENDPOINT_SOCKET
              value: unix:///run/spiffe/spire-agent.sock
          image: ghcr.io/eighred/accounting@sha256:cbc5bfebc53eab95c7f618564f4a2477ed4dc4d00bd0dad58be6211c4ead5c6a
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8101
            initialDelaySeconds: 5
            periodSeconds: 15
          name: accounting
          ports:
            - containerPort: 8101
              name: http
            - containerPort: 8080
              name: metrics
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8101
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
            - mountPath: /run/spiffe
              name: spiffe
              readOnly: true
            - mountPath: /tmp
              name: tmp
      initContainers:
        - args:
            - --dir
            - /migrations/accounting
          env:
            - name: KANZ_MIGRATE_DATABASE_URL_FILE
              value: /run/secrets/db/migrate-database-url
          image: ghcr.io/eighred/kanz-migrate@sha256:909865997d2e96fd9898c5c34a6f11ade3cd65b71a18f1424d0bcda118e0e689
          name: migrate
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: accounting-acme
      terminationGracePeriodSeconds: 60
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: accounting
              tenant: acme
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
      volumes:
        - emptyDir: {}
          name: tmp
        - csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: accounting-db
          name: db
        - csi:
            driver: csi.spiffe.io
            readOnly: true
          name: spiffe
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: accounting-acme
  namespace: kanz-services
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: accounting
      tenant: acme
