# Code generated by internal/tenantgen (cmd/kanz-tenantgen). DO NOT EDIT BY HAND.
#
# Rendered from infra/deploy/risk-engine-rollout.yaml for one tenant (MT-02). CI
# (test/arch/tenant_compute_test.go) re-renders this file from the live base
# on every run and fails the build on any byte difference. If the base
# changed underneath this file, regenerate it — do not hand-patch:
#   go run ./cmd/kanz-tenantgen -tenant <tenant>
---
apiVersion: v1
imagePullSecrets:
  - name: ghcr-pull
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: risk-engine-acme
  namespace: kanz-services
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: risk-engine-acme
  namespace: kanz-services
spec:
  ports:
    - name: http
      port: 8081
      targetPort: 8081
    - name: grpc
      port: 9090
      targetPort: 9090
  selector:
    app: risk-engine
    tenant: acme
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: risk-engine-acme
  namespace: kanz-services
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: risk-engine
      tenant: acme
  strategy:
    canary:
      steps:
        - setWeight: 20
        - pause:
            duration: 60s
        - analysis:
            args:
              - name: canary-hash
                valueFrom:
                  podTemplateHashValue: Latest
            templates:
              - templateName: risk-engine-canary
        - setWeight: 50
        - pause:
            duration: 120s
        - analysis:
            args:
              - name: canary-hash
                valueFrom:
                  podTemplateHashValue: Latest
            templates:
              - templateName: risk-engine-canary
        - setWeight: 100
  template:
    metadata:
      annotations:
        prometheus.io/port: "8081"
        prometheus.io/scrape: "true"
      labels:
        app: risk-engine
        app.kubernetes.io/part-of: kanz
        tenant: acme
    spec:
      containers:
        - env:
            - name: RISK_ENGINE_DATABASE_URL_FILE
              value: /run/secrets/db/database-url
            - name: RISK_ENGINE_NATS_URL
              value: nats://nats.kanz-messaging.svc:4222
            - name: RISK_ENGINE_KAFKA_BROKERS
              value: kafka.kanz-messaging.svc:9094
            - name: RISK_ENGINE_SOURCE
              value: risk-engine
            - name: RISK_ENGINE_TENANT
              value: acme
            - name: RISK_ENGINE_VENUE_ACCOUNTS
              value: ""
            - name: RISK_ENGINE_DATAMASTER_URL
              value: http://datamaster.kanz-services.svc:8080
            - name: RISK_ENGINE_GRPC_LISTEN
              value: :9090
            - name: RISK_ENGINE_SPIFFE_SOCKET
              value: unix:///run/spiffe/spire-agent.sock
            - name: RISK_REQUIRE_VALIDATED_ANALYTICS
              value: "true"
          image: ghcr.io/eighred/risk-engine@sha256:66441aea5636ca12f1dd59634afa2fc7dcc251a2c8c6a97ce08fc82fd952eca2
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 15
          name: risk-engine
          ports:
            - containerPort: 8081
              name: http
            - containerPort: 9090
              name: grpc
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
            - mountPath: /run/spiffe
              name: spiffe
              readOnly: true
            - mountPath: /tmp
              name: tmp
      initContainers:
        - args:
            - --dir
            - /migrations/risk-engine
          env:
            - name: KANZ_MIGRATE_DATABASE_URL_FILE
              value: /run/secrets/db/migrate-database-url
          image: ghcr.io/eighred/kanz-migrate@sha256:909865997d2e96fd9898c5c34a6f11ade3cd65b71a18f1424d0bcda118e0e689
          name: migrate
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          volumeMounts:
            - mountPath: /run/secrets/db
              name: db
              readOnly: true
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: risk-engine-acme
      terminationGracePeriodSeconds: 60
      topologySpreadConstraints:
        - labelSelector:
            matchLabels:
              app: risk-engine
              tenant: acme
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
      volumes:
        - emptyDir: {}
          name: tmp
        - csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: risk-engine-db
          name: db
        - csi:
            driver: csi.spiffe.io
            readOnly: true
          name: spiffe
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  labels:
    app.kubernetes.io/part-of: kanz
    tenant: acme
  name: risk-engine-acme
  namespace: kanz-services
spec:
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
        scaleUp:
          stabilizationWindowSeconds: 30
    restoreToOriginalReplicaCount: true
  cooldownPeriod: 120
  maxReplicaCount: 12
  minReplicaCount: 3
  scaleTargetRef:
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    name: risk-engine-acme
  triggers:
    - metadata:
        activationThreshold: "50"
        metricName: kanz_bus_pending_messages
        query: sum(kanz_bus_pending_messages{group="risk-engine"})
        serverAddress: http://prometheus.observability.svc:9090
        threshold: "500"
      type: prometheus
