apiVersion: v1
kind: Namespace
metadata:
  name: kanz-messaging
  labels:
    app.kubernetes.io/part-of: kanz
    # SEC-01a: opts this namespace into SPIRE workload-identity issuance — the
    # ClusterSPIFFEID namespaceSelector matches this label.
    kanz.internal/spiffe: enabled
