---
# SEC-01d — one SecretProviderClass per (namespace, secret-consumer). It is the
# only place a secret's Vault path is named; the consuming pod just mounts the
# CSI volume and reads the file. Rotating the secret in Vault changes nothing
# here.
#
# The risk-engine + schema-registry Postgres DSN is the one genuine runtime
# secret in the platform today (everything else — peer auth, broker transport —
# is mTLS, where the SVID is the credential, so there is no password to store).
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: risk-engine-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    # Auth via the workload's SPIFFE JWT-SVID against the Vault `jwt` backend
    # (runbook step 4). The role's bound_subject = the consumer's SPIFFE ID, so
    # only the risk-engine SA can read this path — deny-by-default, same as the
    # SEC-01b AuthorizeServices allow-list.
    vaultAuthMountPath: "jwt-spiffe"
    role: "risk-engine"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/risk-engine"
        secretKey: "dsn"
      # INFRA-M6a: the kanz-migrate initContainer's DDL-capable DSN, separate
      # from the app's non-superuser credential (same rationale as oms-db).
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/risk-engine"
        secretKey: "migrate_dsn"
---
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: schema-registry-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "schema-registry"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/schema-registry"
        secretKey: "dsn"
---
# The OMS order-store DSN (EXEC-M7c). This one is load-bearing for correctness,
# not just durability: order.Postgres is the admission gate that decides which
# delivery of an order reaches a live venue, and it only holds across replicas
# while the pods share a database. An OMS pod that cannot read this secret falls
# back to the in-memory store, whose gate is a process-local mutex — so a
# multi-replica OMS without this DSN double-trades. oms-deploy.yaml pins the two
# together.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: oms-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "oms"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/oms"
        secretKey: "dsn"
      # INFRA-M6a: the kanz-migrate initContainer's DSN. A SEPARATE credential
      # from the app's, deliberately: the OMS connects as a non-superuser (that
      # is what makes FORCE RLS apply to it), while migrations need rights to
      # create tables and policies. Keeping them apart means a leaked app
      # credential cannot run DDL.
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/oms"
        secretKey: "migrate_dsn"
---
# wealth: the durable store DSN (its postgres.go shipped in PARITY-02b and sat
# unused until nothing constructed it). Losing this secret is not a capital bug
# like the OMS's — the service degrades to the in-memory store — but it silently
# discards every write on restart, so it is wired the same way.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: wealth-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "wealth"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/wealth"
        secretKey: "dsn"
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/wealth"
        secretKey: "migrate_dsn"
---
# alternatives: the durable store DSN (its postgres.go shipped in PARITY-02b and sat
# unused until nothing constructed it). Losing this secret is not a capital bug
# like the OMS's — the service degrades to the in-memory store — but it silently
# discards every write on restart, so it is wired the same way.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: alternatives-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "alternatives"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/alternatives"
        secretKey: "dsn"
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/alternatives"
        secretKey: "migrate_dsn"
---
# venue-binance's own order view DB (INFRA-M7a-2). Losing it does not lose money
# directly, but it blinds the healing watchdog across a restart: the adapter can
# no longer enrich a fill for an order it worked, nor tell its reconciler what
# should be open at the exchange.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: venue-binance-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "venue-binance"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/venue-binance"
        secretKey: "dsn"
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/venue-binance"
        secretKey: "migrate_dsn"
---
# The Binance API key + secret. These are the most dangerous credentials in the
# platform: they move the fund's money at a real exchange. They exist in exactly
# one process now (venue-binance) instead of inside the OMS, which is the security
# half of why the connector was extracted at all.
#
# The role's bound_subject is the venue-binance SPIFFE ID, so ONLY that workload's
# ServiceAccount can read this path — deny-by-default (SEC-01b).
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: venue-binance-keys
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "venue-binance"
    objects: |
      - objectName: "api-key"
        secretPath: "kv/data/kanz/venue-binance"
        secretKey: "api_key"
      - objectName: "api-secret"
        secretPath: "kv/data/kanz/venue-binance"
        secretKey: "api_secret"
---
# venue-okx's own order view DB (INFRA-M7a-3). Same role as venue-binance's: lose
# it and the healing watchdog is blind across a restart.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: venue-okx-db
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "venue-okx"
    objects: |
      - objectName: "database-url"
        secretPath: "kv/data/kanz/venue-okx"
        secretKey: "dsn"
      - objectName: "migrate-database-url"
        secretPath: "kv/data/kanz/venue-okx"
        secretKey: "migrate_dsn"
---
# The OKX API credentials. THREE of them: OKX signs with key + secret + a
# passphrase (OK-ACCESS-PASSPHRASE) chosen when the key was created. All three are
# required; a missing passphrase rejects every signed request.
#
# Like Binance's, these move the fund's money and now live in exactly one process.
# The role's bound_subject is the venue-okx SPIFFE ID, so ONLY that workload can
# read them — the OMS cannot, which is the security half of the extraction.
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: venue-okx-keys
  namespace: kanz-services
  labels:
    app.kubernetes.io/part-of: kanz
spec:
  provider: vault
  parameters:
    vaultAddress: "https://vault.vault.svc:8200"
    vaultAuthMountPath: "jwt-spiffe"
    role: "venue-okx"
    objects: |
      - objectName: "api-key"
        secretPath: "kv/data/kanz/venue-okx"
        secretKey: "api_key"
      - objectName: "api-secret"
        secretPath: "kv/data/kanz/venue-okx"
        secretKey: "api_secret"
      - objectName: "api-passphrase"
        secretPath: "kv/data/kanz/venue-okx"
        secretKey: "api_passphrase"
---
# Reference consumer patch — apply to the risk-engine Deployment (lands with
# CICD-01c). The DSN arrives as a tmpfs file and the service reads it via
# RISK_ENGINE_DATABASE_URL_FILE; there is NO plaintext RISK_ENGINE_DATABASE_URL.
#
# apiVersion: apps/v1
# kind: Deployment
# metadata: { name: risk-engine, namespace: kanz-services }
# spec:
#   template:
#     spec:
#       containers:
#         - name: risk-engine
#           env:
#             - name: RISK_ENGINE_DATABASE_URL_FILE
#               value: /run/secrets/db/database-url
#           volumeMounts:
#             - { name: db, mountPath: /run/secrets/db, readOnly: true }
#       volumes:
#         - name: db
#           csi:
#             driver: secrets-store.csi.k8s.io
#             readOnly: true
#             volumeAttributes:
#               secretProviderClass: risk-engine-db
