apiVersion: batch/v1
kind: Job
metadata:
  name: nats-bootstrap
  namespace: kanz-messaging
spec:
  template:
    spec:
      containers:
        - name: bootstrap
          env:
            - name: NATS_REPLICAS
              value: "1"
