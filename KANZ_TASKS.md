# KANZ TASK BOARD

Task IDs are module-prefixed (`EVT`, `RISK`, `PRED`, `DATA`). Each item is scoped to 1–3 days. Epics are split into lettered subtasks.

---

## BACKLOG

### Event System

### Risk Engine

### Go ↔ Python Prediction Layer


### Data Integrity Layer

_(none)_

---

## PLATFORM HARDENING ROADMAP — Phase 1 "Make It Real"

> Converts the library/contract foundation (EVT/RISK/PRED/DATA epics) into a running, persistent, secured, observable system. New module prefixes: `ORCH` (runtime), `PERS` (persistence), `SEC` (security), `CICD` (delivery), `OBS` (telemetry). Sequenced by ROI ranks 1–6. Subtasks scoped 1–3 days; each epic carries its own `Tests:` subtask(s) per board convention.

### Platform Runtime — risk-engine service + durable state

_Objective: deploy a concrete `risk/api/v1.Engine` that ingests state, recomputes, publishes, and survives restarts. Unblocks the entire risk capability (ROI #1–2)._

_(epic complete — ORCH-01a..f + PERS-01a..e in DONE)_

### Security & Identity

_Objective: zero-trust transport + workload identity + secrets management. Gate to any institutional client or auditor (ROI #3)._

_(epic complete — SEC-01a..e in DONE)_

### Delivery, Build & Observability

_Objective: a CI/CD safety net for the `kanz` module, published schema SDKs, and the telemetry the DATA-08/09 dashboards already assume (ROI #4–6)._

- [ ] **DEBT-01a** Run `schema-release.yml` → publish + tag the `kanz-schemas-go` companion module repo · _(kanz-schemas CI)_
- [ ] **DEBT-01b** Publish Python (PyPI/private index) + TS (npm `@kanz-eng/kanz-schemas`) SDK artifacts · _(kanz-schemas CI)_
- [ ] **DEBT-01c** Pin published SDK versions; remove the local `replace github.com/kanz-eng/kanz-schemas-go => ../kanz-schemas/gen/go` in `kanz/go.mod` (+ kanz-py/ts equivalents) · `kanz/go.mod`, `kanz-py/pyproject.toml`
- [ ] **DEBT-01d** CI guard: fail the build if any `replace` directive or stale gen/ path reappears · `.github/workflows/kanz-ci.yml`

> **DEBT-01a..d are blocked on external publishing** (PyPI/npm/companion-repo tags) not doable on the dev box, and the `replace` directive DEBT-01d guards against is still load-bearing until DEBT-01a..c land (EVT-17b). Deferred until a release pipeline run.

_(OBS-01a..e epic complete — see DONE)_

---

## PLATFORM HARDENING ROADMAP — Phase 2 "Safe For Clients"

> Makes the running system multi-client: a governed external surface, identity-based access control, and tenant isolation. New prefixes: `API` (edge/BFF), `AUTH` (authn/authz), `MT` (multi-tenancy). ROI ranks 7–9. Depends on Phase 1 (notably ORCH-01 runtime, SEC-01 mTLS, PERS-01 durable state).

### External API Gateway & BFF

_Objective: an authenticated, rate-limited, versioned external surface over the risk `Engine` read/scenario API. The only governed entry point for clients and UIs (ROI #7)._

- [x] **API-01a** Define the Engine read surface as a gRPC service proto (`Exposure`/`Measures`/`EvaluateScenario`/`Health`) reusing `domain.v1` + risk payloads; CODEOWNERS + buf discovery · `kanz-schemas/proto/query/v1/risk_query.proto` — _(see DONE)_
- [x] **API-01b** Implement the gRPC query server inside `risk-engine`, backed by the concrete `EngineImpl` (ORCH-01b); reads served from durable state (PERS-01) · `kanz/services/risk-engine/internal/grpcsrv/` — _(see DONE)_
- [x] **API-01c** Build the `api-gateway` BFF: gRPC + REST transcoding (grpc-gateway/Connect), OpenAPI emit, API version negotiation, forwards to `risk-engine` over mTLS · `kanz/services/api-gateway/cmd/api-gateway/`, `internal/` — _(see DONE)_
- [x] **API-01d** Edge controls: OIDC/JWT auth middleware (consumes AUTH-01), per-tenant rate limits + quotas, idempotency-key dedup, request signing · `kanz/services/api-gateway/internal/middleware/` — _(see DONE)_
- [x] **API-01e** Tests: gateway contract tests — authz required (401/403), rate-limit (429), gRPC↔REST transcoding parity, query p99 budget, version-negotiation matrix · `kanz/services/api-gateway/internal/` — _(see DONE)_

### AuthN / AuthZ — RBAC + ABAC

_Objective: identity on every request and fine-grained, deny-by-default authorization on every query/command. Compliance + client requirement (ROI #8)._

- [x] **AUTH-01a** OIDC integration + JWT validation middleware → typed `Principal` (subject, tenant, roles, claims) on ctx · `kanz/pkg/auth/principal.go`, `kanz/pkg/auth/oidc.go` — _(see DONE)_
- [x] **AUTH-01b** Authorization layer over a policy engine (OPA/Cedar): portfolio- and tenant-scoped, deny-by-default; policy bundle in infra · `kanz/pkg/auth/authz.go`, `kanz/infra/security/policies/` — _(see DONE)_
- [x] **AUTH-01c** Command issuer verification: bind the authenticated `Principal` to `command.v1.CommandMetadata.issuer` at publish/validate; reject mismatch (forged-issuer guard) · `kanz/pkg/bus/command.go`, `kanz/pkg/auth/issuer.go` — _(see DONE)_
- [x] **AUTH-01d** Authorization decision logging into the observation stream (`DecisionLog` or a dedicated authz FACT) so every allow/deny is auditable (feeds AUDIT-01) · `kanz/pkg/auth/audit.go` — _(see DONE)_
- [x] **AUTH-01e** Tests: role×resource policy matrix (allow/deny), deny-by-default on unknown action, issuer-mismatch rejection, tenant-scope enforcement · `kanz/pkg/auth/authz_test.go` — _(see DONE; **closes the AUTH-01 epic**)_

### Multi-Tenancy Foundation

_Objective: tenant as a first-class isolation boundary across events, state, brokers, metrics, and access. Costly to retrofit — land before onboarding real clients (ROI #9)._

- [x] **MT-01a** Add `tenant_id` to the Envelope as an additive field (envelope-policy additive-only discipline; new field number, forward-compatible per EVT-21c). **Replay nuance**: `Validate` requires `tenant_id` on live publish, but pre-tenancy events read from old Kafka logs lack it — map those to a reserved `__system__` tenant on the replay path rather than rejecting (preserves EVT-20 replay determinism) · `kanz-schemas/proto/envelope/v1/envelope.proto`, `kanz/pkg/bus/validate.go` — _(see DONE)_
- [x] **MT-01b** Tenant propagation through the bus: Producer stamps `tenant_id` from ctx/config; Consumer stashes it onto ctx (same mechanism as lineage, EVT-17c) so derived events inherit it · `kanz/pkg/bus/producer.go`, `kanz/pkg/bus/context.go`, `kanz/pkg/bus/consumer.go` — _(see DONE)_
- [x] **MT-01c** Broker-level isolation: NATS accounts per tenant + Kafka topic-prefix + per-tenant ACLs · `kanz/infra/{nats,kafka}/tenancy.yaml` — _(see DONE)_
- [x] **MT-01d** State isolation: Postgres RLS keyed on `tenant_id`, tenant set from the session GUC · `kanz/services/risk-engine/migrations/0002_tenant_rls.sql`, `kanz/internal/risk/state/persist/` — _(see DONE)_
- [x] **MT-01e** Per-tenant quotas + admission control at the gateway + `tenant` metric-label convention · `kanz/services/api-gateway/internal/middleware/{quota,metrics}.go`, `kanz/pkg/observability/tenant.go` — _(see DONE)_
- [x] **MT-01f** Tenant lifecycle automation: onboard/offboard in one workflow · `kanz/infra/tenancy/` — _(see DONE)_
- [x] **MT-01g** Tests: cross-tenant isolation contract tree · `kanz/test/contract/tenancy/` — _(see DONE; **closes the MT-01 epic**)_

---

## PLATFORM HARDENING ROADMAP — Phase 3 "Value & Maturity"

> Turns the placeholder risk math into real analytics, governs the models, and earns regulatory + operational trust. New prefixes: `MODEL` (quant analytics + data plane), `MLOPS` (model governance), `SRE` (reliability), `AUDIT` (auditability), `DR` (disaster recovery). Depends on Phase 1–2 (runtime, persistence, observability, tenancy).

### Real Risk Analytics & Market Data Plane

_Objective: replace the RISK-07 placeholder measures (VaR=1%×gross, Delta=net) with genuine VaR / factor / sensitivity analytics backed by a market & historical data plane. This is the actual product value (ROI #12). The RISK-07 `Registry` is the designed seam — quants register real `MeasureFunc`s without touching `api/v1` or callers._

- [x] **MODEL-01a** Define reference + market-history schemas: instrument reference (identifiers, asset_class, sector, currency), price/return history (builds on EVT-10 `market.v1`) · `kanz-schemas/proto/reference/v1/instrument.proto`, `price_history.proto` — _(see DONE)_
- [x] **MODEL-01b** Market-data ingestion service: consume `market.*` events → time-series store (ClickHouse/Timescale) with point-in-time-correct reads · `kanz/services/market-data/`, `kanz/internal/marketdata/store/` — _(see DONE)_
- [x] **MODEL-01c** Historical-returns provider injected into VaR `MeasureFunc`s via closure — keeps the pure `func(*Portfolio) v1.Measure` call-site signature while the func closes over the data provider held in the Registry · `kanz/internal/risk/compute/marketdata.go` — _(see DONE)_
- [x] **MODEL-01d** Historical-simulation VaR measure registered over the RISK-07 `Registry` (replaces the 1%×gross placeholder; the RISK-12 property test that pins `VaR99/Gross=0.01` intentionally breaks here as failure-as-documentation) · `kanz/internal/risk/compute/var/historical.go` — _(see DONE)_
- [x] **MODEL-01e** Monte-Carlo VaR with a correlated factor-shock model · `kanz/internal/risk/compute/var/montecarlo.go` — _(see DONE)_
- [x] **MODEL-01f** Factor model + instrument→sector lookup → completes the RISK-06 `ExposureBySector` deferral (the deferred dimension needed exactly this reference table) · `kanz/internal/risk/compute/factor/` — _(see DONE)_
- [x] **MODEL-01g** Wire real volatility/covariance into the RISK-08 uncertainty path (populate `domain.Position.MarketValueUncertainty` from the vol model — RISK-08's propagation is already built and currently fed nil) · `kanz/internal/risk/compute/volatility.go`, `kanz/internal/risk/engine/` — _(see DONE)_
- [x] **MODEL-01h** Stress/scenario library extending RISK-09: named historical scenarios (2008, COVID) + hypothetical curve shifts as new shock types in `api/v1/scenario.go` + dispatch · `kanz/internal/risk/scenario/library/` — _(see DONE)_
- [x] **MODEL-01i** Tests: VaR backtest (exception rate within Kupiec band over historical window), independent-reference reconciliation within tolerance, point-in-time correctness (no future leakage) · `kanz/internal/risk/compute/var/` — _(see DONE; **closes the MODEL-01 epic**)_

### Model Validation, Explainability & Governance (MLOps)

_Objective: no model serves production without recorded validation; drift auto-triggers revalidation; every prediction is explainable. SR 11-7 / model-risk compliance (ROI #13). Scaffolding already exists — PRED-09 registry, PRED-10 shadow/canary, PRED-02 confidence/degraded, DATA-04 drift._

- [x] **MLOPS-01a** Add a validation-status gate to the PRED-09 `Registry`: a model cannot be registered `primary` without a recorded, non-expired validation record · `kanz-py/kanz_inference/registry/` — _(see DONE)_
- [x] **MLOPS-01b** Validation suite: holdout/backtest metrics, calibration check (feeds PRED-02 §2.3 per-model `confidence_threshold` — uncalibrated ⇒ always DEGRADED), stability/bias · `kanz-py/kanz_inference/validation/` — _(see DONE)_
- [x] **MLOPS-01c** Drift→revalidation loop: a DATA-07 `drift_detected` event for a model's feature triggers an automated revalidation/retrain signal · `kanz-py/kanz_inference/governance/drift_trigger.py` — _(see DONE)_
- [x] **MLOPS-01d** Explainability: populate `PredictionEnvelope.explanation` (SHAP-style per-feature contributions — the field already exists in PRED-01) on the inference path · `kanz-py/kanz_inference/explain/` — _(see DONE)_
- [x] **MLOPS-01e** Champion/challenger promotion driven by the PRED-10 `ShadowObserver` writing comparison metrics to a store; promotion gated on challenger out-performance + validation · `kanz-py/kanz_inference/governance/promotion.py` — _(see DONE)_
- [ ] **MLOPS-01f** Real point-in-time feature store behind the existing PRED-11 `FeatureStore` Protocol (Feast or equivalent) replacing the trivial in-memory impl — the boundary was designed for exactly this swap · `kanz-py/kanz_inference/featurestore/`
- [ ] **MLOPS-01g** Model cards + approval workflow + model→feature→training-data lineage record · `kanz-py/kanz_inference/governance/`
- [ ] **MLOPS-01h** Tests: promotion-blocked-without-validation, drift-triggers-revalidation, explanation-present-on-every-prediction, champion/challenger selection correctness · `kanz-py/tests/`

### Reliability — SLOs, Error Budgets, Chaos

_Objective: prove the resilience primitives (RISK-11 degraded mode, PRED-07 circuit breaker, EVT-17e DLQ/retry, PRED-08 admission control) actually hold under real faults (ROI #17)._

- [ ] **SRE-01a** Define per-service SLIs/SLOs (availability, query latency, data freshness from DATA-02) + error-budget policy · `kanz/infra/observability/slo/`
- [ ] **SRE-01b** SLO recording rules + multi-window burn-rate alerts (extends DATA-09, over the OBS-01 metrics) · `kanz/infra/observability/slo/`
- [ ] **SRE-01c** Chaos experiments: broker kill, network partition, latency injection, pod eviction — assert degraded-mode/circuit-breaker/DLQ behave as designed · `kanz/infra/chaos/`
- [ ] **SRE-01d** Scheduled GameDay suite + runbooks-as-code · `kanz/infra/chaos/gamedays/`, `kanz/docs/runbooks/`
- [ ] **SRE-01e** Load/soak harness establishing baseline capacity + the LATENCY-01 p99 budget · `kanz/test/load/`

### Auditability & Regulatory Reporting

_Objective: tamper-evident reconstruction of every decision, command, and data-quality event. Institutional + regulatory non-negotiable (ROI #18). The lineage substrate (correlation/causation, DecisionLog, CommandOutcome, FACT-grade quality events) already exists — consolidate and make it tamper-evident._

- [ ] **AUDIT-01a** Append-only audit projection: a consumer materializes decisions, commands, outcomes, quality events, and AUTH-01d authz decisions into a queryable audit store · `kanz/services/audit/`
- [ ] **AUDIT-01b** Tamper-evidence: hash-chaining over the audit log + WORM/object-lock storage · `kanz/services/audit/internal/chain/`
- [ ] **AUDIT-01c** Causal-chain reconstruction API: given any `event_id`, walk `correlation_id`/`causation_id` to the full upstream lineage (the chain EVT-17c builds) · `kanz/services/audit/internal/lineage/`
- [ ] **AUDIT-01d** Regulatory report generation (configurable templates) + retention/legal-hold policy · `kanz/services/audit/internal/report/`
- [ ] **AUDIT-01e** Tests: reconstruct a decision to its inputs in <1min, tamper attempt detected by chain verification, sample regulatory report generated end-to-end · `kanz/services/audit/`

### Disaster Recovery & Backup

_Objective: provable cross-region RPO/RTO for the durable log, state stores, and registry. Continuity + regulatory (ROI #11)._

- [ ] **DR-01a** Kafka cross-region replication (MirrorMaker2 / cluster-linking) of the log of record to a DR region · `kanz/infra/dr/kafka/`
- [ ] **DR-01b** Postgres PITR + cross-region replicas (PERS-01 risk state + schema registry) · `kanz/infra/dr/postgres/`
- [ ] **DR-01c** NATS DR: automate spine reconstruction from the Kafka log (the architecture already makes NATS rebuildable) with a documented RTO · `kanz/infra/dr/nats/`
- [ ] **DR-01d** Failover automation + runbook: traffic shift, promote replicas, re-bootstrap services (PERS-01d) · `kanz/infra/dr/`, `kanz/docs/runbooks/dr.md`
- [ ] **DR-01e** Quarterly DR drill validating RPO ≤ 1min / RTO ≤ 15min via live failover · `kanz/docs/runbooks/dr-drill.md`

---

## PLATFORM HARDENING ROADMAP — Phase 4 "Scale & Strategic Bets"

> Closes the remaining cross-cutting scale/hardening gaps and the long-horizon bets that compound into Aladdin-class leverage. New prefixes: `INFRA` (cloud-native), `LATENCY` (hot-path), `SEC-02` (supply-chain/runtime), `DEBT-02/03` (coordination, Python CI), `LAKE` (lakehouse/backtest), `LIN` (lineage/governance), `AUTO` (autonomous ops), `DEVX` (developer platform). Scale/hardening epics come first since the bets depend on them.

### Cloud-Native Platform

_Objective: production K8s topology with IaC, GitOps, autoscaling, and multi-AZ resilience. Current manifests are single-cluster bootstrap (ROI #10)._

- [ ] **INFRA-01a** Cloud foundation as IaC (Terraform/Pulumi): VPC, K8s cluster, managed/self-hosted Kafka + NATS + Postgres · `kanz/infra/terraform/`
- [ ] **INFRA-01b** GitOps delivery (Argo CD/Flux): app-of-apps + per-environment overlays · `kanz/infra/gitops/`
- [ ] **INFRA-01c** KEDA autoscaling on Kafka consumer lag / NATS pending — the OBS-01c signal and the exact PRED-14 backpressure premise (add workers to work down lag) · `kanz/infra/deploy/`
- [ ] **INFRA-01d** Multi-AZ topology spread + PodDisruptionBudgets + resource quotas/limits · `kanz/infra/deploy/`
- [ ] **INFRA-01e** Tests/validation: AZ-kill leaves the system serving; autoscaler demonstrably reacts to injected lag (ties SRE-01c chaos) · `kanz/infra/chaos/`

### Low-Latency Hot Path

_Objective: bound and shrink ingest→risk→publish latency under load — validate the latency premise the NATS spine was chosen for (ROI #16)._

- [ ] **LATENCY-01a** Latency SLOs + continuous load test gating regressions in CI · `kanz/test/load/`
- [ ] **LATENCY-01b** Profile the compute hot paths (Decimal arithmetic, map iteration in RISK-06/07 exposure/measures) · `kanz/internal/risk/compute/`
- [ ] **LATENCY-01c** Optimizations: object pooling, batched framing, per-partition single-writer goroutines (lock-free state, an alternative the RISK-05 design noted) · `kanz/internal/risk/`, `kanz/pkg/bus/`
- [ ] **LATENCY-01d** Tests: p99 budget held under 10× burst; `testing.B` benchmark regression guard in CI · `kanz/internal/risk/compute/`

### Supply-Chain & Runtime Hardening

_Objective: extend the CICD-01d build-side gate to runtime admission + dependency hygiene (ROI #19)._

- [ ] **SEC-02a** Runtime hardening: distroless, non-root, read-only rootfs, seccomp/gVisor, K8s NetworkPolicies (default-deny) · `kanz/infra/security/runtime/`
- [ ] **SEC-02b** Admission control verifies cosign signatures (from CICD-01d) + blocks critical-CVE images at deploy · `kanz/infra/security/admission/`
- [ ] **SEC-02c** Dependency hygiene: Renovate/Dependabot automation + scheduled scan + a vuln-remediation SLA · `.github/`
- [ ] **SEC-02d** Secret scanning (gitleaks) in CI + pre-commit hook · `.github/workflows/`, `kanz/.githooks/`
- [ ] **SEC-02e** Tests: an unsigned or critical-CVE image is rejected by admission; a planted secret blocks CI · `kanz/infra/security/`

### Distributed Coordination (Tech Debt)

_Objective: replace per-process in-memory state with shared/distributed state where horizontal scale-out needs strong guarantees (ROI #14)._

- [ ] **DEBT-02a** Distributed dedup-window option (Redis/Dragonfly) behind the existing EVT-17d `DedupWindow` API · `kanz/pkg/bus/dedup.go`
- [ ] **DEBT-02b** Coordinate the PRED-09 model registry (back on `platform.model` topic + cache) and offer a shared-state DATA-05 reconciler mode · `kanz-py/kanz_inference/registry/`, `kanz/internal/integrity/reconcile.go`
- [ ] **DEBT-02c** Document strong-vs-best-effort guarantees per component (which rely on idempotent handlers vs shared state) · `kanz/docs/coordination.md`
- [ ] **DEBT-02d** Tests: N-replica consumer group under chaos redelivery → no duplicate side-effects · `kanz/pkg/bus/`

### Python CI Hardening (Tech Debt)

_Objective: stop the `kanz-py` path rotting — documented stale tests + missing gRPC stubs (ROI #15)._

- [ ] **DEBT-03a** Generate Python gRPC stubs into the SDK so the PRED-08 interactive-servicer tests run · `kanz-schemas/buf.gen.yaml`, `kanz-py/`
- [ ] **DEBT-03b** Fix the 16 stale `test_consumer.py` tests (`Message(subject=...)` required positional arg, EVT-18d) · `kanz-py/tests/test_consumer.py`
- [ ] **DEBT-03c** Wire `kanz-py` into the CICD-01b pytest job (suite green, gated) · `.github/workflows/kanz-ci.yml`

### Lakehouse & Backtesting Plane

_Objective: stream the event log into an analytical store for backtesting, research, and training. The system is backtest-ready by design (EVT-21d replay determinism + PRED-11 point-in-time store) but has no analytical store to exploit it (ROI #20)._

- [ ] **LAKE-01a** CDC/sink from the Kafka log → lakehouse (Iceberg/Delta), schema-evolution-aware (consumes registry EVT-16 descriptors) · `kanz/services/lake-sink/`
- [ ] **LAKE-01b** Point-in-time-correct dataset/feature materialization (joins MODEL-01b market history + MLOPS-01f feature store) · `kanz/services/lake-sink/internal/`
- [ ] **LAKE-01c** Backtest harness reusing the EVT-20 replay pipeline + point-in-time store · `kanz/tools/backtest/`
- [ ] **LAKE-01d** Research access: query engine (Trino) + notebook environment over the lakehouse · `kanz/infra/lakehouse/`
- [ ] **LAKE-01e** Tests: a strategy backtested over historical events reproduces live decisions bit-for-bit (leverages EVT-21d determinism) · `kanz/tools/backtest/`

### Data Lineage & Governance Catalog

_Objective: an automated, queryable lineage graph + data catalog + PII governance. Per-event lineage exists on the wire; nothing aggregates it (ROI #21)._

- [ ] **LIN-01a** Harvest envelope lineage (correlation/causation/source/schema_ref) into an OpenLineage emitter · `kanz/services/lineage/`
- [ ] **LIN-01b** Data catalog (DataHub/OpenMetadata): datasets, schemas (from the EVT-16 registry), ownership (CODEOWNERS), classification · `kanz/infra/catalog/`
- [ ] **LIN-01c** PII tagging + governance policy + access logging (ties AUTH-01b authz + MT-01d tenant scope) · `kanz/services/lineage/internal/governance/`
- [ ] **LIN-01d** "Where did this number come from" lineage query/UI over the graph (complements the AUDIT-01c causal chain) · `kanz/services/lineage/internal/query/`
- [ ] **LIN-01e** Tests: any output's full upstream lineage is queryable; PII access is governed + logged · `kanz/services/lineage/`

### Autonomous Operations

_Objective: closed-loop ops — the signals already exist (consumer lag, DATA-07 quality events, drift, circuit-breaker state, degraded mode); add the actuation layer (ROI #22)._

- [ ] **AUTO-01a** Event-driven ops controller reacting to DATA-07 quality events / SLO burn-rate / drift / circuit-breaker state · `kanz/services/autopilot/`
- [ ] **AUTO-01b** Auto-remediation: data quarantine on DATA-05 reconciliation divergence; model auto-rollback on drift (MLOPS-01e promotion in reverse) · `kanz/services/autopilot/internal/remediate/`
- [ ] **AUTO-01c** Scale/failover actuation closing the loop the INFRA-01c KEDA + DR-01d failover opened · `kanz/services/autopilot/internal/actuate/`
- [ ] **AUTO-01d** Runbook-as-code execution + human-in-the-loop escalation on novel/unrecognized conditions · `kanz/docs/runbooks/`, `kanz/services/autopilot/`
- [ ] **AUTO-01e** Tests: simulated broker stall / model drift / data gap → auto-remediated, with escalation only on unrecognized conditions · `kanz/services/autopilot/`

### Developer Productivity Platform

_Objective: one-command local stack, golden-path scaffolding, docs portal — kill the hand-run buf-generate onboarding pain (ROI #23)._

- [ ] **DEVX-01a** One-command local stack (Tilt/devcontainers): NATS + Kafka + Postgres + services + seeded data · `kanz/dev/`, `Tiltfile`
- [ ] **DEVX-01b** Service scaffolding generator following the EVT-16a layout conventions (`services/<name>/cmd|internal`) · `kanz/tools/scaffold/`
- [ ] **DEVX-01c** Internal docs/catalog portal (Backstage) surfacing services, schemas, and runbooks · `kanz/infra/backstage/`
- [ ] **DEVX-01d** Ephemeral preview environments per PR (per-namespace) · `.github/workflows/`, `kanz/infra/gitops/`
- [ ] **DEVX-01e** `make`/task targets + onboarding doc replacing the hand-run buf-generate flow (the memory-documented friction) · `kanz/Makefile`, `kanz/docs/onboarding.md`

---

## TODO

_(none)_

---

## IN PROGRESS

**Phase-3 MODEL-01 epic COMPLETE** (real risk analytics, 01a–i). **MODEL-01a–i DONE**: (01a) `reference.v1` schemas; (01b) market-data ingestion + bitemporal point-in-time `store.Store`; (01c) `StoreReturnsProvider` + `BindReturns` seam; (01d) historical-sim VaR; (01e) Monte-Carlo VaR (correlated factor-shock); (01f) the `factor.Classifier` instrument→sector/asset-class lookup + `factor.SectorExposure`/`ComputeExposure` completing the RISK-06 ExposureBySector deferral; (01g) `compute.VolModel`/`ReturnsVolModel` + `PopulateUncertainty` enrichment wiring real per-position 1σ MV uncertainty (=`|MV|×σ`, σ = sample-stdev of the same MODEL-01c return series) into the RISK-08 propagation at the four engine snapshot boundaries (`EngineImpl` query path + `Recomputer`), filling the previously-always-nil `Measure.UncertaintyAbs` (incl. the VaR band); (01h) stress/scenario `library` + new `v1.SectorShock` (sector-differentiated, resolved against the 01f factor model) + `scenario.WithClassifier`/`engine.WithClassifier` dispatch — `GFC_2008`/`COVID_2020` named full-GICS-curve scenarios via `Named`/`Names`, `SectorCurve` the hypothetical-curve primitive; (01i) the VaR verification suite — Kupiec-band backtest (deterministic, 17/1050≈1.62% exceptions, LR=3.42<3.841), independent delta-normal reconciliation (MC ≤2.5%, historical ≤8%), and end-to-end point-in-time/no-future-leakage through the bitemporal store. **Next: the MODEL-01 epic is closed** — the Phase-3 successor is **MLOPS-01** (model validation/explainability/governance, kanz-py) or **SRE-01**/**AUDIT-01**; the open production-wiring integration below is the prerequisite to make 01's models actually serve. **Engine-wiring seam (still open, an integration task)** — the one piece that turns the real models on in production: the risk-engine must construct a `compute.StoreReturnsProvider` over a `marketdata/store` (and a `factor.Classifier` from reference data) and call `varmodel.Register`/`RegisterMonteCarlo` + use `factor.ComputeExposure` + pass `engine.WithVolModel`/`WithRecomputeVolModel(compute.NewReturnsVolModel(provider, 0))`; the engine builds no market-data/reference store today, so `DefaultRegistry` still serves the `compute.VaR99` placeholder + 2-dimension exposure + nil vol model (no uncertainty band). **Carried-forward seams still open**: (a) `Observation.CurrencyCode` left empty by ingestion — reference-data join fills it; (b) `MarketDataBatch` carriage un-wired; (c) `reference.v1` still not in `gen/go` — 01f used a Go-native `factor.Classification` (like `store.Observation`) so it didn't force a regen, but a reference-data STORE/service (the reference mirror of 01b, feeding both the `Classifier` and the `Observation.CurrencyCode` join) is the missing piece, likely its own task; (d) the price store is platform-global (not tenant-scoped); (e) VaR tail-aligns returns by recency, not date; (f) the 01g vol model is delta-normal (`|MV|×σ`, per-position independent under RISK-08's default propagation) — it does NOT yet use MODEL-01e's full covariance for cross-position correlation in the band, and is wired-but-off in production until the engine-wiring seam above lands.

---

_(prior context — **AUTH-01 + MT-01 epics COMPLETE**. MT-01a–g all in DONE: envelope `tenant_id` (v2) + bus propagation + broker isolation (NATS accounts / Kafka topic-prefix ACLs) + Postgres RLS + per-tenant gateway quotas/admission + `tenant` metric-label convention + tenant lifecycle automation + the cross-tenant isolation contract tree. **Carried-forward follow-ups** (not epic blockers): (a) Kafka producer must prepend `{tenant}.` to the topic from envelope `tenant_id` (MT-01c bus-wiring); (b) inference admission in kanz-py (MT-01e); (c) SPIFFE-aware Kafka `principal.builder.class` to ship (MT-01c); (d) tenant metric label is gateway-only, not retrofitted onto bus/data/risk series. **Next Phase-2/3 candidate**: API-01 + AUTH-01 + MT-01 done → Phase-3 MODEL-01 (real risk analytics) or remaining hardening. **Follow-ups surfaced**: (a) MT-01c bus-wiring — Kafka producer must prepend `{tenant}.` from envelope `tenant_id`; (b) MT-01e inference admission lives in kanz-py (Python), out of the Go gateway scope; (c) the `tenant` label is delivered at the gateway + as an observability convention — retrofitting the per-subject `kanz_bus_*`/`kanz_data_*`/`kanz_risk_*` series is deliberately deferred (cardinality + OBS-01e/CICD-01e contract churn). **Bus-wiring follow-up surfaced by 01c**: the Kafka producer must prepend `{tenant}.` to the topic from envelope `tenant_id` (empty/`__system__` → no prefix); NATS needs no subject change (account-scoped). **SEC-01 companion surfaced by 01c**: a SPIFFE-aware Kafka `principal.builder.class` (`io.kanz.kafka.SpiffePrincipalBuilder`, configured but not yet shipped) to surface `User:spiffe://…` as the ACL principal, since SVID subject DNs are empty. **Key MT follow-up surfaced by 01b**: the risk-engine recomputer publishes derived events from a background ctx (debounced/async), so per-event tenant can't propagate via ctx there — it stamps a configured fallback (`RISK_ENGINE_TENANT`, default `__system__`); true per-portfolio tenant stamping (engine carries portfolio→tenant in state) is an MT-01d concern. AUTH-01 integration follow-ups still pending: AuditedAuthorizer into the request path, VerifyCommandIssuer into command publishers, bus-backed DecisionRecorder with AUDIT-01.)_

---

## DONE

### Model Validation, Explainability & Governance (MLOps)

- [x] **MLOPS-01e** Champion/challenger promotion — `kanz-py/kanz_inference/governance/promotion.py` (+`__init__` exports). Turns PRED-10 shadow evaluation into an automated, gated promotion. **`ShadowComparisonStore` IS a PRED-10 `ShadowObserver`** (structural; `isinstance` passes the runtime_checkable protocol): on `observe` it accumulates per-`(primary_id, challenger_id)` behavioral metrics (observation count, mean abs divergence, opposite-sign disagreement rate) — everything computable without labels — and stashes the pair's predictions per subject. **`record_outcome(subject_id, actual)`** is the outcome-join: when a realized value arrives (a later consumer feeds it), it credits squared error to both models, so **live RMSE — real out-performance on production traffic — falls out** (the prediction pair alone can't say which model is better; that needs ground truth). `stats(primary_id, challenger_id) → ComparisonStats`. **`Promoter.evaluate(feature_set_ref)`** promotes the best challenger only when **all three gates** hold: (1) **soak** — `observations ≥ min_observations` AND `scored ≥ min_scored` (don't promote on a handful / on unjoined predictions); (2) **validation** — a current passing MLOPS-01a `ValidationRecord` (and `register(primary=True)` re-checks the gate on promotion); (3) **out-performance** — `challenger_rmse ≤ champion_rmse·(1−improvement_margin)` (a marginal win doesn't justify switch risk). On promotion it re-registers the winner primary and **demotes the old champion to shadow** (keeps running for comparison); returns a `PromotionDecision{promoted, previous_primary, reasons}` with a per-candidate explanation either way. Pure decision + one registry mutation, safe to call on a schedule. Tests (13): store-is-observer, behavioral + RMSE accumulation, disagreement rate, unknown-outcome no-op, happy-path promote + demote, best-of-several, each gate blocking independently (soak/scored/validation/out-performance/below-margin), no-primary, constructor guards. Full kanz-py suite 218 passed / 3 skipped · `kanz-py/kanz_inference/governance/`
- [x] **MLOPS-01d** Prediction explainability — `kanz-py/kanz_inference/explain/{explainer.py,__init__.py}` (new `explain` package). Populates the `PredictionEnvelope.explanation` map (SHAP-like per-feature contributions, the field present since PRED-01) on the inference path. **`PermutationExplainer`**: model-agnostic Shapley-value estimation by **permutation sampling** (Štrumbelj–Kononenko) — for each random feature ordering, add features one at a time to a baseline coalition and credit each with the model-output change as it joins; average over orderings. **No `shap` dependency** (the dep-bloat rule) — only needs `model.predict`. Two properties hold *exactly* regardless of sample count (per-ordering credits telescope): **efficiency** — contributions sum to `pred(full) − pred(baseline)` (the proto's "sum need not equal `value`" = value minus the baseline prediction), and **determinism** — fixed `seed` + stable sorted-feature ordering ⇒ reproducible attribution for replay/audit (EVT-21d). Coalition predictions memoized per `frozenset` within one `explain` to bound the model-call count. **Baseline** = "feature absent": default drops the feature from the vector (the proto's omit-⇒-absent semantics); an optional `baseline` map supplies per-feature reference values for models that refuse partial input. **`ExplainingModel`** decorates any `Model` (satisfies the protocol, drops into the StreamingWorker/interactive path in place of the bare model): returns the inner prediction with `explanation` filled. Only **NORMAL** predictions are explained (attributing a degraded/zero fallback would mislead), and explanation is **best-effort** — if attribution raises (e.g. a model that rejects perturbed vectors) the prediction returns unexplained rather than failing inference. Tests (10): linear model attributes `w·x`, efficiency sum on a non-additive interaction model, seed determinism, empty-vector→empty, baseline shifts attribution, samples≥1 guard, decorator populates/skips-degraded/best-effort-on-failure/nil-guards. Full kanz-py suite 205 passed / 3 skipped · `kanz-py/kanz_inference/explain/`
- [x] **MLOPS-01c** Drift→revalidation loop — `kanz-py/kanz_inference/governance/{drift_trigger.py,__init__.py}` (new `governance` package). **Closes the loop DATA-04 (drift) + MLOPS-01a/b (validation) left open**: when a feature a model depends on drifts, its recorded validation is no longer trustworthy, so the model must be revalidated. `DriftTrigger.handle(env, payload)` consumes the DATA-07 `data.feature.drift_detected` `DataQualityEvent` (same `EventHandler(env, payload)` shape as `StreamingWorker.handle`), acts only on the `drift` oneof variant (gap/staleness + sub-`min_severity` events are no-op acks), and fans out a `RevalidationRequest` per affected model. **Two pluggable boundaries** (the drift event names a single `feature`, but the registry indexes by `feature_set_ref` — there is no feature→model index in it): `ModelResolver`/`StaticModelResolver` (feature→model_ids, the deployment's feature catalog; the in-memory stand-in mirrors MODEL-01f's `StaticClassifier`) and `RevalidationSink`/`LoggingRevalidationSink` (where the signal goes — bus command, retrain job, MLOps ticket; mirrors PRED-05's `PredictionPublisher` — this package owns the trigger, not the delivery). `RevalidationRequest` carries the full drift evidence (model_id, feature, metric/score/threshold, severity, subject/partition_key, baseline_ref, reason, requested_at). **Per-(model, feature) cooldown** (default 1h, injected clock) collapses a drift detector's per-window re-emits into one signal — the revalidation-storm guard (same intent as the Go recompute debounce). `run(subscriber, group, subject=SUBJECT_DRIFT_DETECTED)` wires the subscription; `handle` is public so tests drive it without a subscriber. Tests (12): evidence-carrying request, fan-out to all affected, unused-feature no-op, gap-variant ignored, below-severity ignored, empty-feature skip, cooldown suppress-then-fire + per-(model,feature) keying, run subscribes to the drift subject, malformed-payload raises (DLQ path), nil-collaborator guards, logging-sink smoke. Full kanz-py suite 195 passed / 3 skipped · `kanz-py/kanz_inference/governance/`
- [x] **MLOPS-01b** Model validation suite — `kanz-py/kanz_inference/validation/{validator.py,__init__.py}`. **The evidence producer behind the MLOPS-01a gate**: scores a model over a labeled holdout, computes the model-risk metrics, decides pass/fail, and emits a `registry.ValidationRecord` via `ValidationResult.to_record(now, validity)`. `Validator(thresholds).validate(model, samples)` is async (awaits `model.predict` per `ValidationSample{feature_vector, actual}`, in time order). Three metric families per the board: **(1) holdout accuracy** — RMSE/MAE over (predicted `value`, realized `actual`); **(2) calibration** — Expected Calibration Error of the self-reported `confidence` vs realized correctness (`|value−actual| ≤ correct_tolerance`), binned (10 bins). This drives **PRED-02 §2.3**: `_recommend_threshold` returns the lowest confidence at which accuracy reaches `target_accuracy` for a calibrated model, and **1.0 (every prediction DEGRADED) when uncalibrated** — where uncalibrated = all-zero confidence (the proto's "no calibrated confidence" sentinel) **or** ECE > `max_calibration_error`; **(3) stability + bias** — bias = mean signed error (systematic over/under-prediction); stability = 2nd-half/1st-half RMSE ratio over the time-ordered holdout (catches error degrading across the window even when the aggregate looks fine). **Pass/fail**: `min_samples` (auto-fail below — no sign-off on a handful of points), optional `max_rmse`/`max_abs_bias`, `max_stability_ratio` gate primary-ness; **uncalibrated does NOT fail** the model (it may serve, just always-DEGRADED via threshold 1.0) — matching the degraded-contract intent. Pure metric core (`_evaluate`) split from the async scoring loop so it's testable without a model. Ties to 01a end-to-end: a passing result's record unlocks `register(primary=True)`, a failing one is recorded (audit) but the gate rejects. Does NOT own the validity-period policy (caller passes `validity`, default 90d) or persistence/scheduling (MLOPS-01c). Tests (12): hand-computable RMSE/MAE/bias, too-few-samples auto-fail, rmse/bias bound breaches, all-zero-confidence + overconfident-wrong both uncalibrated→1.0, well-calibrated→usable threshold, stable vs degrading stability, to_record validity window, and the 01a round-trip (pass unlocks / fail blocks primary). Full kanz-py suite 183 passed / 3 skipped · `kanz-py/kanz_inference/validation/`
- [x] **MLOPS-01a** Validation-status gate on the PRED-09 `Registry` — `kanz-py/kanz_inference/registry/registry.py` (+`__init__.py` exports). **SR 11-7 / model-risk gate: no model serves production without recorded, current validation.** New `ValidationRecord(model_id, validated_at, expires_at, passed=True, report_uri="")` (frozen) with `is_valid(now) = passed and expires_at > now` — a validation is *current*, not an open-ended sign-off (the revalidation-cadence the policy mandates). `Registry.record_validation(record)` stores latest-per-model (a revalidation supersedes; failing records kept as audit evidence but never satisfy the gate); `validation_for(model_id)` reads it back for health/audit + MLOPS-01e promotion. **`register(..., primary=True)` is gated**: requires a recorded, non-expired, passing record for `metadata.model_id` else raises **`ValidationError(ValueError)`** (subclasses ValueError so existing reject-as-ValueError callers keep working, while promotion code can catch the gate specifically). **The gate runs after input validation, before any mutation** (a blocked register leaves the registry untouched) and **re-runs on every primary register** incl. hot-reload (an expired validation blocks re-registration). **Shadows are exempt** (`primary=False`): a shadow is under evaluation, not serving — and shadow eval is what *produces* the validation evidence (PRED-10), so the gate bites exactly at promotion-to-primary. Clock injected via `Registry(clock=...)` so the expiry boundary is deterministically testable; defaults to UTC now. Existing PRED-09/PRED-10 tests updated to record a passing validation before primary registration (new `_register_primary` helpers); +12 gate tests: blocked-without/expired/failed validation, valid succeeds, shadow exempt, wrong-model's-validation doesn't count, latest-wins, ValidationError isa ValueError, injected-clock boundary, hot-reload re-check after expiry. Full kanz-py suite 171 passed / 3 skipped · `kanz-py/kanz_inference/registry/`

### Real Risk Analytics & Market Data Plane

- [x] **MODEL-01i** VaR verification suite, **closes the MODEL-01 epic** — `kanz/internal/risk/compute/var/backtest_test.go`. Three properties a real VaR plane must hold, each deterministic via a fixed seed (no flaky statistics): **(1) Kupiec backtest** — rolls a 250-day historical-VaR window across a 1300-day simulated N(0,σ) return path, counts one-day-ahead exceptions (realized loss > the VaR predicted from the *prior, out-of-sample* window), and asserts the Kupiec proportion-of-failures LR statistic < χ²₀.₉₅(1)=3.841 (calibration not rejected at 95%). Observed: 17/1050 exceptions (1.62%), LR=3.42 — a correctly-calibrated 99% VaR breaches ~1% of days. **(2) Independent-reference reconciliation** — both models vs the closed-form delta-normal `VaR=(z₉₉·σ−μ)·value` (z₉₉=2.32635) computed from the *same sample's* moments: Monte-Carlo (200k draws) within 2.5% (it simulates exactly that normal — estimation error isolated by reconciling to the sample moments, not the true ones), historical within 8% (resampling/empirical-quantile sampling error). **(3) Point-in-time / no-future-leakage** — end-to-end through `StoreReturnsProvider`→`Historical`: a late price correction (a past `observation_time` restated with a `knowledge_time` 15 days *after* the backtest horizon) is invisible to the as-of-day-5 read (which matches a store that never had the correction, to 1e-6 — the leakage assertion) and only moves the VaR once the horizon advances past its `knowledge_time` (`late > early`). Reuses the `historical_test.go` helpers (`asOf`/`money`/`portfolio`/`dval`); adds `sliceProvider` (settable window for the rolling backtest), `kupiecLR`, `meanStd`, `portfolioAsOf`. `go test ./internal/risk/...`/`vet` green · `kanz/internal/risk/compute/var/`
- [x] **MODEL-01h** Stress/scenario library extending RISK-09 — `kanz/internal/risk/scenario/library/library.go`, `api/v1/scenario.go` (`SectorShock`), `scenario/scenario.go` (dispatch + `WithClassifier`), `engine/engine.go` (`WithClassifier`). **New shock type `v1.SectorShock{Taxonomy,Code,Pct}`**: shocks every position whose instrument classifies into a sector, resolved point-in-time against the **MODEL-01f factor model** at apply time — carried as plain strings (not `factor.Sector`) because `api/v1` is the leaf package outsiders import and can't depend on `internal/.../factor` (would cycle `factor→compute→api/v1`). **Why sector-differentiated, not ParallelShift**: a crisis isn't uniform (2008 financials −55% vs staples −15%; COVID energy −55% vs staples −25%) — only a per-sector curve preserves the dispersion, the whole point of a named scenario. **Dispatch** adds a `case v1.SectorShock` to `applyShock`; `scenario.Evaluate` gained a variadic `...Option` (`WithClassifier`) so all 9 existing 3-arg call sites + the `api/v1` contract are untouched; **no classifier wired ⇒ SectorShock is a silent no-op** (the established unknown-shock/nil-model degradation), as are unclassified instruments. `EngineImpl.EvaluateScenario` passes its `engine.WithClassifier` classifier through. **Library** (`package library`): `GlobalFinancialCrisis2008()`/`Covid2020Crash()` return full 11-sector GICS curves (illustrative drawdown magnitudes — stress inputs, not the MODEL-01b backtest plane); `SectorCurve(taxonomy, map[code]pct)` is the hypothetical-curve-shift primitive the named scenarios are built from + the seam for custom curves (drops nil/zero points, stable sort by code for replay determinism); `Named(name)`/`Names()` is the name→shocks dispatch an API/CLI drives `EvaluateScenario` from (`GFC_2008`, `COVID_2020`). Builders (not cached slices) so each lookup hands back a fresh caller-owned slice. ctx for the classifier is `Background` in the dispatch (point-in-time rides on `asOf`; the static classifier ignores ctx) — same wired-but-off-in-production posture as 01f/01g (the engine builds no reference-data classifier yet). Tests: sector-targeted hit/miss, no-classifier no-op, unclassified skip, full-GICS-curve shape + ascending-code order, nil/zero drop, end-to-end GFC differentiation. `go build ./...`/`vet`/`test ./internal/risk/...` green · `kanz/internal/risk/scenario/library/`
- [x] **MODEL-01g** Real volatility into the RISK-08 uncertainty path — `kanz/internal/risk/compute/volatility.go`, `engine/{engine.go,recompute.go}`. **Closes the "fed nil" gap**: RISK-08 built + tested the propagation through measures (`uncertainty.go`) but every real position carried `MarketValueUncertainty=nil` (no vol model existed), so `Measure.UncertaintyAbs` was always nil in production. **`VolModel` is the reference mirror of the MODEL-01c returns provider**: `Volatility(ctx, instrumentID, asOf) (sigma, ok, err)` — one-sigma *fractional* return vol, point-in-time (asOf bounds the bitemporal knowledge horizon, no future leakage). **`ReturnsVolModel`** estimates it as the unbiased (n-1) sample stdev of the **same return series that feeds historical/MC VaR** (`compute.ReturnsProvider`), so the band and the VaR figure come off one consistent source; `DefaultVolMinObs=20` ⇒ `ok=false` below it (no band off a handful of points, vs. fabricating a noisy/zero one). **`PopulateUncertainty(ctx, p, vm)` is the engine-side enrichment seam** (mirror of `factor.Classifier` feeding exposure): given a Store.Snapshot *clone* it fills each position's band = `|MarketValue| × σ` (delta-normal 1σ), mutating the snapshot before compute — **the pure `MeasureFunc`/`ComputeMeasures` contract is untouched**, the band enters via the snapshot not the call site (same discipline as MODEL-01c's `BindReturns`). nil-as-absent preserved end-to-end: nil MV / insufficient history / model error ⇒ band left nil ⇒ RISK-08 reports "no uncertainty propagated", and a nil `vm` is a no-op (engine without a market-data plane behaves exactly as pre-01g). **Wired at all four snapshot boundaries**: `EngineImpl.{exposureSet,measureSet,EvaluateScenario}` (query path, via `engine.WithVolModel`) + `Recomputer.recompute` (async publish path, via `engine.WithRecomputeVolModel`) — two construction seams, one model, distinct names since both live in `package engine`. This also fills the VaR `UncertaintyAbs` band the 01d/01e measures left nil (Gross/Net/VaR/Delta all propagate from the per-position bands). Goes through float64 for the band (σ is a float estimate — acceptable per `decimalToFloat`'s note; money values stay Decimal). **Production-wiring still open** (the same integration task carried since 01c): the risk-engine builds no market-data store, so `main.go` constructs neither a `StoreReturnsProvider` nor the vol model — the option is wired-and-tested but not turned on until that store lands. Tests: sigma==independent sample-stdev, insufficient-history→nil, delta-normal band (incl. short |MV|), point-in-time asOf, end-to-end propagation through `GrossExposure`, nil-model no-op. `go build ./...`/`vet`/`test ./internal/risk/...` green · `kanz/internal/risk/compute/volatility.go`
- [x] **MODEL-01f** Factor model + instrument→sector lookup, completing the RISK-06 ExposureBySector deferral — `kanz/internal/risk/compute/factor/{factor.go,exposure.go}`, `compute/decimal.go` (3 exported money wrappers). **`Classifier` is the reference-data mirror of the MODEL-01c returns provider**: instrument→`Classification{Sector, AssetClass}`, point-in-time (`Classify(ctx, instrumentID, asOf)` — sector reclassifications are slowly-changing per the reference.v1 `as_of` SCD, so a backtest reads the mapping in effect then). `StaticClassifier` is the in-memory stand-in the engine loads from reference state; the production source is a reference-data store (the reference mirror of MODEL-01b, not yet built). `Sector{Taxonomy,Code}.Key()` builds the `"GICS:45"` bucket the domain uses, mirroring `reference.v1.SectorClassification`. **`SectorExposure(ctx, p, c)` completes RISK-06**: buckets base-currency positions by their instrument's sector (the RISK-07 same-currency convention — cross-currency needs FX), gross=Σ|MV| / net=ΣMV per bucket, **`UNCLASSIFIED` catch-all** for unknown/sectorless instruments so the SECTOR decomposition reconciles with the per-instrument total (pinned by `TestSectorExposure_Reconciles`). **`compute.ComputeExposure` signature left untouched** (the deferral note's promise): `factor.ComputeExposure` layers the SECTOR dimension onto compute's instrument+currency set when a `Classifier` is wired; absent one the engine calls the 2-dimension compute version. **Money arithmetic reused, not forked**: exported `compute.AddMoney/AbsMoney/ZeroMoney` thin wrappers keep the single point of truth for exponent-alignment/sign (decimal.go's own warning) rather than duplicating it in factor. **Factor-model scope**: `Classification` carries both factor dimensions (sector consumed now; `AssetClass` carried for the future reduced-rank factor covariance that could replace MODEL-01e's full empirical covariance — no covariance machinery built yet, not speculative beyond the reference field). Tests: GICS bucketing (gross/net incl. shorts), UNCLASSIFIED catch-all, reconciliation, non-base-currency skip, combined-set has all three dimensions. `go build ./...`/`vet`/`test` green · `kanz/internal/risk/compute/factor/`

- [x] **MODEL-01e** Monte-Carlo VaR + correlated factor-shock model — `kanz/internal/risk/compute/var/montecarlo.go` (+`Config.Draws/Seed` in `historical.go`). Second VaR model in `package varmodel`, registered identically (`MonteCarlo(cfg) compute.ReturnsMeasure` + `RegisterMonteCarlo` over `MeasureVaR99` — interchangeable with historical-sim). **The covariance IS the factor structure**: fits a multivariate-normal model to the provider's historical returns — per-instrument mean vector + full **Bessel-corrected sample covariance** estimated over the tail-aligned window — and draws `Draws` correlated synthetic scenarios via the **Cholesky factor** (`sim = μ + L·z`, z standard-normal), revalues per-scenario P&L = Σᵢ valueᵢ×simᵢ, and takes `−quantile(P&L, 1−conf)` (reusing the package `quantile`/decimal helpers from 01d). **Diversification falls out of the correlation**: a hedged (anti-correlated) book gets a lower VaR than a concentrated one — the property historical resampling can't model directly, pinned by `TestMonteCarlo_DiversificationReducesVaR`. **Deterministic given inputs + `Config.Seed`** (RNG re-seeded per call, default fixed) so a replayed recompute reproduces the same number (EVT-21d determinism — a random VaR would violate it). **Cholesky `covJitter` (1e-12)** floors a non-positive pivot so a singular/degenerate covariance (perfectly correlated/constant series) factorizes gracefully instead of NaN-ing. Inline linear algebra (mean/cov/Cholesky) — no gonum dep (avoid bloat). Same base-currency filter + insufficient-data→zero (need ≥2 scenarios for a covariance) as historical. Tests: determinism, sane magnitude (~value×σ×z₉₉), diversification, base-currency/insufficient guards, `RegisterMonteCarlo` override. Full factor model (reduced-rank sector/style factors) is MODEL-01f; this uses the full empirical covariance. `go build ./...`/`vet`/`test` green · `kanz/internal/risk/compute/var/`

- [x] **MODEL-01d** Historical-simulation VaR — `kanz/internal/risk/compute/var/historical.go` (new `package varmodel`; dir is `var` but the identifier can't be the keyword), `internal/risk/compute/correctness_test.go` (RISK-12 comment). **`varmodel.Historical(cfg) compute.ReturnsMeasure`** revalues the portfolio under each past return scenario: per-position P&L = Σᵢ valueᵢ × returnᵢ (base-currency positions only — the RISK-07 same-currency convention, cross-currency needs an FX layer), scenarios **tail-aligned to the shortest series** (providers return most-recent-N so the recent tail lines up; exact date-alignment is a MODEL-01b refinement once returns are date-tagged), then **VaR₉₉ = −quantile(P&L, 0.01)** floored at 0 via the empirical linear-interpolated quantile (R-7/NumPy). Insufficient data ⇒ zero-value measure (the MeasureFunc never-error contract; response layer marks degraded). **`varmodel.Register(ctx, r, provider, cfg)`** overrides `MeasureVaR99` with `compute.BindReturns(ctx, provider, Historical(cfg))` — the engine calls it once it has a price-store provider. **The placeholder `compute.VaR99` is NOT deleted**: `varmodel` imports `compute`, so `compute.DefaultRegistry` can't hold the provider-backed model (import cycle) — the placeholder stays as the honest **no-market-data fallback**, and Register overrides it only where a provider exists. **RISK-12 failure-as-documentation**: `TestProperty_VaR99IsOnePercentOfGross` stays green (it pins the surviving placeholder/fallback) but its comment now records that the property holds for the fallback ONLY; the real model's decoupling is pinned by the NEW `varmodel.TestHistorical_DecouplesFromGrossPlaceholder` (hist VaR 98 ≠ 1%×gross 10 on a hand-computable distribution). Tests: empirical-quantile arithmetic (hand-checked), decoupling, base-currency filter, insufficient-data→zero, and an end-to-end Memory-store→`StoreReturnsProvider`→`Register`→`ComputeMeasures` proving the served VaR99 is data-driven, not the placeholder. Local `decimal⇄float` bridge (compute's is package-private). `go build ./...`, `go vet`, `go test ./internal/risk/... ./internal/marketdata/...` green · `kanz/internal/risk/compute/var/`

- [x] **MODEL-01c** Historical-returns provider + VaR closure-injection seam — `kanz/internal/risk/compute/marketdata.go`. **The deliverable is the SEAM, not the VaR math** (that's 01d): `BindReturns(ctx, provider, ReturnsMeasure) MeasureFunc` closes a provider-aware computation over a `ReturnsProvider` + a captured ctx, yielding a plain `func(*domain.Portfolio) v1.Measure` the RISK-07 `Registry` stores and `ComputeMeasures` runs **unchanged** — the data dependency enters by closure, the api/v1 contract + MeasureSet shape + every caller stay exactly as RISK-07 designed. ctx is captured at bind time (the recomputer is debounced/async, so the pure signature carries no ctx of its own — same reasoning as the MT-01b `RISK_ENGINE_TENANT` fallback). **`ReturnsProvider`** (consumer-defined interface, idiomatic Go) `Returns(ctx, instrumentID, asOf, window) ([]float64, error)` — returns `double` (a return is a derived statistic, the EVT-14/MODEL-01a rationale), empty (not error) on insufficient history so the VaR caller degrades per the MeasureFunc "never error" contract. **`StoreReturnsProvider`** is the concrete historical-returns provider over the MODEL-01b `store.Store`: reads the close series **point-in-time-correct** (`store.Query{End: asOf, AsOf: asOf}` bounds BOTH observation_time and the bitemporal knowledge horizon at p.AsOf()), keeps the last window+1 prices, differences them into simple/log returns (`ReturnMethod`, default log, default 250-day lookback). **Deliberately registers NOTHING into `DefaultRegistry`** — VaR99 stays the 1%×gross placeholder, so the RISK-12 property test (`VaR99/Gross=0.01`) still passes; 01d writes the `ReturnsMeasure` (historical-sim) and `Register`s `BindReturns(...)` over it, which is where RISK-12 intentionally breaks (failure-as-documentation). Reuses the package-private `decimalToFloat`. Tests: simple/log returns + zero-guard, point-in-time no-future-leakage (a day-9 restatement invisible to an as-of-day-4 read), window-tail truncation, insufficient-history→empty, and the `BindReturns` seam threading provider + captured ctx into the bound `MeasureFunc`. `go build ./...`, `go vet`, `go test ./internal/risk/... ./internal/marketdata/...` green · `kanz/internal/risk/compute/marketdata.go`

- [x] **MODEL-01b** Market-data ingestion service + point-in-time store — `kanz/internal/marketdata/{store/,ingest.go}`, `kanz/services/market-data/`, `kanz-schemas/proto/reference/v1/price_history.proto` (+`PRICE_KIND_LAST`). **The `store.Store` is bitemporal — the load-bearing MODEL-01i decision**: every `Observation` carries `ObservationTime` (the time a price applies to) AND `KnowledgeTime` (when Kanz learned it); a read scoped to `Query.AsOf` sees only `KnowledgeTime <= AsOf` and, per `ObservationTime`, the latest such version — so a backtest as-of T reproduces exactly what was knowable at T and a late correction never leaks backward (proven by `TestMemory_NoFutureLeakage`). **`Store` is a Go-native record layer over the MODEL-01a proto** (`Observation` ≈ `reference.v1.PriceObservation`), the same two-layer split as `persist.PortfolioRecord`/`domain.Portfolio` — so the service does NOT depend on the not-yet-generated `reference.v1` Go SDK; price stays exact `commonpb.Decimal`. **Memory + Postgres parity** (the EVT-16a schema-registry pattern): `Memory` is the executable reference implementing the full point-in-time contract (restatement collapse, window, kind filter, `LatestAsOf` bounding both axes); `Postgres` mirrors it in SQL via `DISTINCT ON (observation_time, kind) ORDER BY knowledge_time DESC` and an idempotent `INSERT … ON CONFLICT DO NOTHING` keyed on the full bitemporal identity. **Timescale over ClickHouse** (the board offered both): Timescale is a Postgres extension so it rides the existing pgx stack with zero new drivers (the `0001_price_history.sql` migration makes a hypertable when the extension is present, plain indexed table otherwise — identical code); **not tenant-scoped** (price history is universal market fact, the schema-registry call). **Ingestion mirrors the RISK-04/05 split**: `marketdata.Ingestor` is a thin `bus.EventHandler` translating the market oneof → mark (**Bar→CLOSE, Trade→LAST, Quote→MID** with an exact `(bid+ask)/2`), stamping `knowledge_time` from envelope `ingestion_time`; an untranslatable event errors → DLQ (market-data loss must be loud). **Service** (`services/market-data/`) is the risk-engine shape: env config, `internal/server` probes, OTel/metrics, NATS-spine subscribe fan-out over `MARKET_DATA_SUBJECTS` (default the `market.>` wildcard), Postgres store when `MARKET_DATA_DATABASE_URL` set else in-memory. `PRICE_KIND_LAST` added to the 01a proto (additive, pre-release) so a trade has an honest mark. `go build ./...`, `go vet`, `go test ./internal/marketdata/...` green; `buf lint`/`build` clean. **Batch carriage** (`MarketDataBatch`) left as a documented seam (`TranslateBatch` exists; no batching producer / event_type discriminator yet) · `kanz/internal/marketdata/`, `kanz/services/market-data/`

- [x] **MODEL-01a** Reference + market-history schemas — `kanz-schemas/proto/reference/v1/{instrument.proto,price_history.proto}`, `kanz-schemas/CODEOWNERS`, **begins the MODEL-01 epic**. New **payload package `reference.v1`, deliberately NOT a streaming domain** (carries no new `{domain}.{entity}.{event_type}` name — like `common.v1`/`domain.v1`, the proto package ≠ a subject-taxonomy domain; how it's ingested/stored is MODEL-01b's concern, so no architecture-review domain addition here). **Split into two focused files** (the multi-file convention of `common.v1`) even though the board named only `instrument.proto`. **`InstrumentReference`** keys on the same canonical `instrument_id` as `market.v1` + the envelope `partition_key` (a price tick joins to its reference record with no translation); carries `InstrumentIdentifiers` (ISIN/CUSIP/SEDOL/FIGI/RIC/Bloomberg — a closed, meaningful struct, NOT an opaque map), `AssetClass`/`SectorClassification`/`currency_code`/`issuer_id` which **map field-for-field onto the existing `domain.v1.ExposureDimension`** (ASSET_CLASS/SECTOR/CURRENCY/ISSUER) so MODEL-01f's instrument→sector lookup and the RISK-06 `ExposureBySector` deferral are fed from here; `SectorClassification` is structured `{taxonomy,code}` so the `"GICS:45"` exposure bucket is built from the two parts without parsing. **Slowly-changing-dimension `as_of`** versions a reference snapshot (point-in-time reads pick latest `as_of <= query_time` — past backtests see the classification in effect then). **Price/return history is bitemporal** (`observation_time` = the time the value applies to, `knowledge_time` = when it became known) — the load-bearing decision for MODEL-01i point-in-time correctness / no-future-leakage: a backtest as-of T filters `knowledge_time <= T` so a late correction never leaks. **Prices are exact `common.v1.Decimal`** (the money/price/size rule); **returns are `double`** — a return is a dimensionless derived statistic feeding the VaR models, the same EVT-14 rationale that allows `double` for metrics/drift scores. `PriceHistory`/`ReturnSeries` are per-instrument batch carriers (the `market.v1.MarketDataBatch` analogue — shared `instrument_id` == `partition_key`, ordered). CODEOWNERS `/proto/reference/` → `@kanz-eng/market-data @kanz-eng/quant-ml @kanz-eng/architecture`. `buf lint` clean (only pre-existing inference RPC-naming warnings remain) + `buf build` OK; additive new package can't trip the `breaking: FILE` baseline · `kanz-schemas/proto/reference/v1/`

### Multi-Tenancy Foundation

- [x] **MT-01g** Cross-tenant isolation contract tree — `kanz/test/contract/tenancy/isolation_test.go`, **closes the MT-01 epic**. The capstone proof the tenant boundary holds end-to-end: asserts the COMPOSITION of the per-layer mechanisms as one suite, so a regression in any layer that opens a cross-tenant path surfaces here. **Four layers**: (1) **BUS** (pure) — a live FACT is always stamped with its producing tenant; live `Validate` rejects an untenanted event (can't sit on a real tenant's stream) while replay tolerates it; a handler-derived publish inherits the inbound tenant from ctx (proven by giving the producer a DIFFERENT config tenant — ctx must win, not the fallback); (2) **REPLAY** (pure) — legacy untenanted replay maps to `SystemTenant` (__system__), never a real tenant; (3) **API** (pure) — `auth.PolicyAuthorizer` denies a cross-tenant query even for admin `"*"` (tenant isolation beats RBAC), same-tenant allowed; (4) **STORE/RLS** (integration, gated on `TEST_POSTGRES_URL`, skips under a superuser role) — A's rows invisible to B via `Load`/`LoadAll`, and a raw cross-tenant `INSERT` (the RLS-escape) is rejected by `WITH CHECK`. External `_test` package on the public bus/auth/persist surface (frames bodies via `envelopepb.EventFrame`, fakes `bus.Client`/`Subscriber`); pure layers every `go test`, store gated. `go test ./test/contract/tenancy/...` + vet green · `kanz/test/contract/tenancy/`
- [x] **MT-01f** Tenant lifecycle automation — `kanz/infra/tenancy/{tenantctl.sh,tenant.example.env,onboard-job.yaml,README.md}`. **ONE idempotent, dependency-ordered workflow** fans a single tenant definition across every isolation boundary: (1) identity — `tenant-<name>` SPIFFE-enabled namespace + ServiceAccounts ⇒ SEC-01a SVIDs; (2) broker — NATS account (MT-01c) + per-SA Kafka `{tenant}.` prefixed topics & PREFIXED ACLs (renders the MT-01c kafka-tenant Job); (3) state — a Postgres non-superuser login role granted on the RLS state tables (MT-01d needs no per-tenant DDL, only the role so FORCE RLS applies under it); (4) quota — merges the tenant's budget into the gateway overrides ConfigMap (MT-01e). **Order = the dependency chain** (the SVID is the credential the broker mapping + ACL principal + DB role all key on); **offboard reverses it** (quota→state→broker→identity, ACL-revoke before topic-delete so no orphan grant). **Schema registry deliberately excluded** (platform-global, MT-01d). Idempotent throughout (`apply`/`--dry-run|apply`/`IF NOT EXISTS`/`--if-not-exists`); **tenant rows retained on offboard unless `PURGE_ROWS=1`** (deleting client data is a one-way door). Runs locally or in-cluster (`kanz-tenancy` ns + scoped ClusterRole + a Job mounting `tenantctl.sh` from a `--from-file` ConfigMap). Honest edges: NATS dynamic account needs the JWT resolver+operator (else prints the static-append step); DB role no-ops without `ADMIN_DATABASE_URL`. bash `-n` clean; YAML parses · `kanz/infra/tenancy/`
- [x] **MT-01e** Per-tenant quotas + admission control + `tenant` metric label — `kanz/services/api-gateway/internal/middleware/{quota,metrics}.go`, `kanz/services/api-gateway/internal/config/config.go`, `kanz/services/api-gateway/cmd/api-gateway/main.go`, `kanz/pkg/observability/tenant.go`. **Two complementary noisy-neighbor guards**: the API-01d token bucket smooths request RATE (429), new **admission control caps CONCURRENCY** via a per-tenant in-flight semaphore (`MaxInFlight` → 503, a transient-capacity signal distinct from 429). Both key on the authenticated tenant so isolation holds. **Per-tenant quotas as policy-as-data**: `TenantLimits{Default, Overrides}` — default from env, per-tenant overrides from a JSON file (`API_GATEWAY_QUOTAS_FILE`, `DisallowUnknownFields`), same ConfigMap-mounted shape as AUTH-01b's `risk-authz.json`. Old `RateLimit` kept for back-compat; new `Quota(limits, obs)` in the chain (Version→Signing→Auth→**Measure→Quota**→Idempotency, Measure outside Quota so `requests_total` counts the 429/503 with their codes); token-bucket refill factored to `bucket.take`. **Tenant metric label (extends OBS-01)**: `observability.TenantLabel`+`NormalizeTenant` set ONE platform label convention; gateway is the first adopter — every `kanz_gateway_*` series carries `tenant` (`requests_total{tenant,code}`, `request_duration_seconds`, `rate_limited_total`, `admission_rejected_total`, `inflight_requests`). `GatewayMetrics` implements `QuotaObserver`, nil-safe like `BusMetrics`. **Deliberate scope**: NOT retrofitted onto `kanz_bus_*`/`kanz_data_*`/`kanz_risk_*` (cardinality + OBS-01e/CICD-01e contract churn; per-tenant attribution belongs at the admission edge); `"anonymous"` (≠`__system__`) labels an unauthenticated caller. **Inference admission** (other half named in MT-01e) is a kanz-py follow-up. Tests: per-tenant override isolation, admission concurrency (503 + other-tenant-unaffected), observer balanced-inflight, metrics series/label/codes, nil-safety; `go test ./services/api-gateway/...` + observability green · `kanz/services/api-gateway/internal/middleware/`, `kanz/pkg/observability/tenant.go`
- [x] **MT-01d** State isolation via Postgres RLS — `kanz/services/risk-engine/migrations/0002_tenant_rls.sql`, `kanz/internal/risk/state/persist/{store,postgres,postgres_test}.go`, `kanz/services/risk-engine/cmd/risk-engine/main.go`, `kanz/services/schema-registry/migrations/0001_init.sql` (note). **The tenant boundary becomes a property of the database, not a query convention** — a read missing a `WHERE tenant_id =` clause still can't leak across tenants. All stateful tables gain `tenant_id` (PKs recomposed to `(tenant_id, …)` so the same `portfolio_id` coexists per-tenant) + **`ENABLE` + `FORCE ROW LEVEL SECURITY`** + a `tenant_isolation` policy `USING/WITH CHECK (tenant_id = current_setting('app.tenant_id', true))`. **`FORCE` is load-bearing** (subjects the table owner — the engine's role — to the policy, so the engine connects as a **non-superuser**; superusers bypass RLS). **Deny-by-default mirrors AUTH-01b**: GUC unset ⇒ `current_setting(…,true)` NULL ⇒ predicate NULL ⇒ nothing visible/writable (fail-closed). **The GUC = the authenticated session's tenant** = the engine's configured `cfg.Tenant` (MT-01b), set on every pooled connection via `pgxpool.AfterConnect → set_config('app.tenant_id', tenant, false)`; the persist INSERTs also stamp `tenant_id` explicitly via `current_setting(...)`. Single-tenant-per-deployment now (so `LoadAll`/`Load`-by-id are correctly tenant-scoped by RLS); multi-tenant-in-one-process would switch to per-tx `SET LOCAL` (documented future). `PortfolioRecord` gains `TenantID` (read back; DB is authority). **Schema registry deliberately NOT tenanted** (note in its 0001 migration): schemas are universal platform contracts — partitioning per-tenant would fork the contract and break cross-tenant `payload_schema_ref` resolution. Test `TestPostgres_RLSTenantIsolation` proves cross-tenant invisibility + per-tenant id coexistence + tenant stamping (gated on `TEST_POSTGRES_URL`, skips under a superuser role); build + vet + `go test ./internal/risk/state/persist/...` green · `kanz/services/risk-engine/migrations/0002_tenant_rls.sql`, `kanz/internal/risk/state/persist/`
- [x] **MT-01c** Broker-level tenant isolation — `kanz/infra/nats/{tenancy.yaml,nats.yaml}`, `kanz/infra/kafka/{tenancy.yaml,kafka.yaml}`, `kanz-schemas/docs/subject-taxonomy.md` §6. **Tenant is an isolation boundary, NOT part of the logical name** — the 3-segment `{domain}.{entity}.{event_type}` contract is untouched; tenant manifests per-broker because their native isolation primitives differ (the per-broker-native stance EVT-08/09 took). **NATS = account-per-tenant (the load-bearing choice)**: an account's subjects are physically invisible to other accounts, so `tls.verify_and_map` binds the client SVID's URI SAN → a per-tenant user → account and **isolation is enforced by which account a connection lands in, not the subject string** — the bus keeps publishing the unchanged 3-segment subject (subject prefixing inside a shared account gives no real isolation). `tenancy.yaml` ships `SYS` + `__system__` (platform/legacy) + an `acme` tenant template, each tenant account JetStream-quota'd (noisy-neighbor bound; MT-01e tightens); `nats.yaml` gains `verify_and_map` + an `include "tenants.conf"`. **Kafka = topic-prefix `{tenant}.{domain}.{entity}` + PREFIXED ACL**: Kafka has no account concept so the topic name is the only ACL handle — the KRaft `StandardAuthorizer` runs **deny-by-default** (`allow.everyone.if.no.acl.found=false`, mirroring AUTH-01b) and each tenant SVID principal gets RW only on its `{tenant}.` prefix + group prefix. **`__system__` reconciles with MT-01a**: pre-tenancy/untenanted events keep the **un-prefixed legacy topics** (no rename, no broken consumers; only new tenants get prefixed topics). **Two seam-ahead deps**: (1) the Kafka producer must prepend `{tenant}.` from envelope `tenant_id` (empty/`__system__` → no prefix) — the bus-wiring follow-up (NATS needs none, account-scoped); (2) SVID certs have empty subject DNs (identity is the URI SAN), so the broker needs a SPIFFE-aware `principal.builder.class` (`io.kanz.kafka.SpiffePrincipalBuilder`, configured not shipped) to surface `User:spiffe://…` as the ACL principal — the SEC-01 companion. PLAINTEXT:9092 `ANONYMOUS` (inter-broker + bootstrap) stays super-user until inter-broker SSL lands. Static accounts now (append+reload); dynamic JWT account-resolver onboarding is the MT-01f path · `kanz/infra/{nats,kafka}/tenancy.yaml`
- [x] **MT-01a/b** Envelope `tenant_id` + tenant propagation through the bus — `kanz-schemas/proto/envelope/v1/envelope.proto` (field 20), `kanz/pkg/bus/{validate,producer,consumer,context}.go`, `kanz/services/risk-engine/{internal/config,cmd}`, **begins the MT epic**. **`tenant_id` added as Envelope field 20, envelope_version bumped 1→2 per envelope-policy §5/§6** (additive-only; it passes both §2 tests — universality + payload-blind tooling, since broker/RLS/metrics isolation all key on it without deserializing). **Asymmetric validation is the load-bearing rule (MT-01a)**: `Validate` (live path) REQUIRES `tenant_id`, but `ValidateReplay` does NOT — pre-tenancy events read from old durable logs carry the empty string, and the consumer maps those to the reserved `SystemTenant` (`__system__`) rather than rejecting, preserving EVT-20 replay determinism; the requirement lives in `Validate` (not the shared `validateEnvelopeFields`) precisely so the replay path stays tolerant. **Propagation (MT-01b) mirrors lineage (EVT-17c)**: Producer stamps with precedence **Event.TenantID > ctx (`TenantIDFromContext`) > `ProducerConfig.Tenant`**; Consumer stashes the inbound tenant onto ctx (empty→`SystemTenant`) so a derived publish inside a handler inherits it. **The risk-engine recomputer is the wrinkle**: it publishes derived FACTs from a background, debounced ctx (not the triggering handler's), so ctx-propagation can't carry the tenant there — it stamps a configured fallback `RISK_ENGINE_TENANT` (default `__system__`); **true per-portfolio tenant stamping (engine carrying portfolio→tenant in state) is a documented MT-01d follow-up** — the codebase's "single-X-now, multi-later bootstrap" pattern. **gen/go is gitignored (EVT-15a), so only the .proto is committed**; the Go SDK was regenerated locally to build/test (buf.exe is Application-Control-blocked on this box, so a throwaway go-built driver drove the local `protoc-gen-go` to regen just envelope.pb.go). Tests: bus-layer `tenant_test.go` (precedence + consumer propagation + replay→`__system__`), `Validate` requires-tenant / `ValidateReplay`-tolerant cases, envelope_version=2 assertion, and ~14 producer/contract test sites updated to supply a tenant. Full suite green · `kanz/pkg/bus/`, `kanz-schemas/proto/envelope/v1/envelope.proto`

### AuthN / AuthZ — RBAC + ABAC

- [x] **AUTH-01e** Authorization contract test suite — rewrote `kanz/pkg/auth/authz_test.go` into the AUTH-01 contract suite, **closes the epic**. **One exhaustive table (`authMatrix`) drives TWO tests** (the load-bearing structure): `TestPolicyAuthorize_Matrix` pins the deny-by-default decision model over role×action×scope — RBAC (reader→read, analyst→read+scenario, admin→`*`, no-role/unknown-role deny, multi-role uses the granting one), **tenant isolation beating RBAC** (admin cross-tenant DENIED), and **portfolio scope beating RBAC** (scoped admin out-of-scope DENIED) — and `TestAuthLayerComposes` replays the SAME matrix through the `AuditedAuthorizer` to prove (a) audit wrapping is **verdict-transparent** (the audited decision always equals the raw one) and (b) each decision records exactly one `DecisionLog` whose recorded verdict matches — the 01b↔01d composition contract. **`TestRealPolicyBundle` is the drift guard**: it loads the ACTUAL shipped `infra/security/policies/risk-authz.json` (not an inline fixture) through the real authorizer and asserts the documented grants — so editing the bundle to grant more/less breaks the build (the PERS-01e "read the real artifact" discipline, here the policy file instead of the migration SQL). `TestForgedIssuerRejection` pins the 01c property (self / forged / ungranted-vs-granted delegation) prominently in the epic suite; deny-by-default edge cases (nil principal, empty action even for admin, principal-without-tenant) and policy-load errors (empty roles, unknown field via `DisallowUnknownFields`, malformed JSON) get dedicated tests. All pure, run on every `go test`; 22 top-level tests across the `auth` package now green (authn OIDC + authz + issuer + audit) · `kanz/pkg/auth/authz_test.go`
- [x] **AUTH-01d** Authz decision logging — new `kanz/pkg/auth/audit.go` (+ tests). **`AuditedAuthorizer` decorates any `Authorizer` so every allow AND deny is recorded as an `observation.v1.DecisionLog`** — the feed AUDIT-01's append-only projection materializes. **Reuses `DecisionLog` (OBSERVATION) over a bespoke authz FACT (the deliberate schema choice)**: it already models "what made a decision / summary / structured factors", which is exactly an authz verdict — no new proto, no kanz-schemas churn. `BuildDecisionLog` is the single Request+Decision→DecisionLog mapping (decider, a human summary, and `attributes`: decision/action/principal.subject+tenant/resource.type+id+tenant/reason) — exported so the bus-backed recorder and tests share one mapping (the RISK-10 "one mapping, not two" rule). **auth stays decoupled from the bus (AUTH-01c stance)**: `audit.go` defines the `DecisionRecorder` seam + ships `SlogRecorder` (a *real* sink — kanz logs are stdout JSON shipped by the platform, OBS-01a — not a stub); the bus-backed recorder that publishes the DecisionLog as an OBSERVATION event lives in the wiring layer (auth never imports transport). **Auditing is best-effort by design**: a recorder failure is logged loudly but does NOT fail the request (an audit-sink outage must not become a service outage; fail-closed-on-allow noted as a high-compliance option), and `Record` SHOULD be non-blocking since `Authorize` is on the request hot path. **Durability note**: general observations are lossy-tolerant (event-class-rules §4), so the recommendation mirrors the FACT-grade `DataQualityEvent` exception — publish on a durable observation subject and treat AUDIT-01's projection as the system of record. Exported envelope constants (`AuthzDecisionDomain`/`AuthzDecisionEventType`) so AUDIT-01 can select authz decisions off the stream. Tests (mapping for allow/deny/anonymous, records-both, recorder-error-doesn't-fail-decision, nil-recorder, SlogRecorder) run every `go test` · `kanz/pkg/auth/audit.go`
- [x] **AUTH-01c** Forged-issuer guard — new `kanz/pkg/bus/command.go` + `kanz/pkg/auth/issuer.go` (+ tests). **Binds a COMMAND's `command.v1.CommandMetadata.issuer` to the authenticated `Principal` at publish; a mismatch is rejected before the command reaches the broker.** **The bus stays decoupled from the auth layer (the load-bearing decision, the SEC-01c precedent where bus took a ready `*tls.Config` not a SPIFFE source)**: `bus` exposes an injectable `CommandIssuerFunc func(ctx, issuer) error` on `ProducerConfig.VerifyCommandIssuer` and owns only the *extraction* + enforcement plumbing; `auth.VerifyCommandIssuer` owns the identity *rule* and satisfies that signature exactly, so wiring is one assignment (`cfg.VerifyCommandIssuer = auth.VerifyCommandIssuer`). **Issuer extraction is generic + payload-blind (EVT-17b preserved)**: `CommandMetadata` is field 1 of every concrete command (command.v1), so the producer reads field 1's bytes via `protowire` and unmarshals just the `CommandMetadata` — no link to any concrete command schema, works across all command types. **The binding rule (`auth.VerifyIssuer`)**: the issuer wire form is `{type}:{id}`; a principal may issue (a) as ITSELF — any issuer whose id-part == `Principal.Subject`, any type prefix (the id is the authenticated identity, what can't be forged), or (b) as a DELEGATED identity listed verbatim in its `issuers` claim (e.g. a strategy it operates). Anything else is `ErrForgedIssuer`; deny-by-default on nil-principal/empty-issuer. **Runs only when a verifier is configured** (backward compatible; non-COMMAND events never invoke it) and a COMMAND with no issuer is `ErrMissingCommandIssuer` (issuer is Required per event-class-rules §2). **Shipped seam-ahead-of-producer**: there is no Go command publisher yet, so the guard is wired into the Producer but its consumer (a real command-issuing path) lands later — the gateway carrying the full `auth.Principal` on the publish ctx is the same integration dep AUTH-01b's wiring needs. Tests: generic extraction (synthetic `dynamicpb` command embedding CommandMetadata at field 1, no-metadata, malformed), producer guard (allowed publishes / forged rejected-and-not-sent / missing-issuer / non-COMMAND-skips), and the `auth` rule matrix (self any-type, forged-id, delegated allow/deny, empty, nil, ctx-principal) — all on every `go test` · `kanz/pkg/bus/command.go`, `kanz/pkg/auth/issuer.go`
- [x] **AUTH-01b** Deny-by-default authorization layer — new `kanz/pkg/auth/authz.go` (+ tests) + `kanz/infra/security/policies/` (`risk-authz.json`, `configmap.yaml`, README). **`Authorizer` interface → `Decision{Allow, Reason}` over a `Request{Principal, Action, Resource}`**, deny-by-default: every gate must pass to allow. **`PolicyAuthorizer` evaluates RBAC role→action grants from a declarative bundle PLUS the ABAC tenant + portfolio scope, checked ISOLATION-FIRST** — tenant and portfolio scope run before the RBAC grant so a role (even the `"*"` admin wildcard) can never widen access beyond the caller's tenant or portfolio allow-list. **Tenant isolation is a CODE invariant, NOT policy data (the load-bearing decision)**: cross-tenant access is an isolation boundary (MT-01), so it must not be expressible as a grant an operator could loosen by editing the bundle — the bundle holds only role→action RBAC; tenant/portfolio scope live in `Authorize`. **Portfolio scope (ABAC)** reads a `portfolios` claim allow-list off the Principal (absent ⇒ any portfolio within the tenant). **Hand-rolled over embedding OPA/Cedar (deliberate, the task named them as the *kind* of engine)**: the decision is structurally narrow (tenant isolation + role→action + portfolio scope), so the Rego/Cedar runtime would be dependency bloat — same call as the gateway's hand-rolled rate limiter + REST transcoding; the `Authorizer` **interface is the seam** a Rego/Cedar engine slots behind if policy ever outgrows this. **Policy bundle is JSON, not Rego/YAML** — stdlib `encoding/json` (`DisallowUnknownFields` so a typo'd field fails loudly), no OPA runtime and no YAML dep; loaded via `LoadPolicyFile` (the ConfigMap-mounted deployment shape, config-as-data like the SEC-01d secret files). `Decision.Reason` is audit-facing — what AUTH-01d will log for every allow/deny. 5 test groups (role×action matrix, deny-by-default on nil-principal/empty-action/unknown-action, tenant isolation incl. admin-can't-cross-tenant + no-tenant-denied, portfolio scope in/out/unrestricted, policy-load errors) run every `go test`. **Shipped seam-ahead-of-consumer**: full wiring into the gateway request path is the integration follow-up (the gateway middleware carries a lossy local principal that drops Claims, so ABAC needs it to carry the canonical `auth.Principal` first) · `kanz/pkg/auth/authz.go`, `kanz/infra/security/policies/`
- [x] **AUTH-01a** OIDC/JWKS authenticator + canonical `Principal` — new `kanz/pkg/auth/` (`principal.go`, `oidc.go` + tests), **begins the AuthN/AuthZ epic** and replaces the API-01d bundled HS256 stand-in with real OIDC behind the SAME `Authenticator` seam. **`auth.Principal` is now the platform-wide identity** (subject/tenant/roles + full custom `Claims` map) carried on ctx via `WithPrincipal`/`PrincipalFromContext`; the gateway's local minimal principal is mapped from it by a thin adapter so the swap touched only the constructor (the promise the API-01d comment made). **`OIDCAuthenticator` verifies asymmetric JWS against JWKS the provider publishes** — `kid`-keyed key lookup, **lazy JWKS (re)fetch on a cache miss so a rotated-in key needs no restart, rate-limited by `MinRefreshInterval` (1m) so a stream of bogus kids can't hammer the provider**; OIDC discovery (`/.well-known/openid-configuration`) resolves `jwks_uri` when not configured, and the discovered `issuer` must equal the configured one. **The allow-list is asymmetric-only (RS/ES/PS) ON PURPOSE — HS\* is refused to close the alg-confusion attack** (a token HMAC-signed with the public key as the shared secret would otherwise verify); the HS256 validator survives only as the gateway's dev-mode fallback. **Network is deferred to first use, not the constructor** (lazy-connect, mirroring the gateway's upstream mTLS dial) so a momentarily-down IdP doesn't fail startup; construction only validates issuer/audience. **Bad token collapses to `ErrUnauthenticated`** (the boundary leaks no reason) while a provider/transport failure is returned distinctly so a future caller can tell 401 from 503 — at the gateway both currently map to 401 (a 503-on-IdP-outage is a noted follow-up, bounded by the ctx-less middleware seam). Roles claim normalizes both a JSON array and a space-delimited `scope` string; tenant/roles claim names are configurable. **`go-jose/v4` promoted from indirect (already pulled in by go-spiffe) to a direct dep — zero new module, satisfies CLAUDE.md dep-hygiene.** Wired into the gateway: `API_GATEWAY_OIDC_ISSUER` set ⇒ OIDC (precedence), else `API_GATEWAY_JWT_SECRET` ⇒ dev HS256, else auth DISABLED. 9 white-box tests (valid→principal + cache-hit-no-refetch, roles-as-scope-string, rejection matrix incl. wrong-aud/iss/expired/no-sub/bad-sig/malformed/**HS256-rejected**, key-rotation pickup, unknown-kid rate-limit, provider-down≠unauthenticated, config validation, ctx round-trip) — pure (httptest IdP, in-memory RSA keys), run on every `go test` · `kanz/pkg/auth/`

### External API Gateway & BFF

- [x] **API-01e** Gateway contract tests — `kanz/services/api-gateway/internal/{gateway,middleware}/*_test.go`, **closes the API-01 epic**. `contract_test.go` wires the production-shaped chain (version→auth→rate-limit→idempotency) over the gateway handler against a fake upstream client and asserts the cross-cutting contract: **authz** (no token→401, token-without-role→403, token-with-role→200), **rate-limit** (1-token bucket → second request 429), **version matrix** (absent/`v1`→200, `v2`→406), and a **query p99 budget** (200 in-process queries, p99 < 50ms — a regression guard against accidental blocking work on the hot path). `gateway_test.go` proves **gRPC↔REST transcoding parity**: the REST JSON body protojson-decodes back into a message `proto.Equal` to the canned proto the fake gRPC client returned (no DTO drift), plus as_of/measure-filter forwarding, scenario body decode (path id wins over body), gRPC-code→HTTP-status mapping, and the OpenAPI doc. middleware tests cover the HS256 validator (valid/bad-sig/expired/malformed), per-tenant rate-limit isolation, and idempotency replay (POST deduped, GET not). All run on every `go test` (pure, fake upstream) · `kanz/services/api-gateway/internal/`
- [x] **API-01d** Gateway edge controls — `kanz/services/api-gateway/internal/middleware/` (`auth.go`, `middleware.go`). **Composable `http.Handler` decorators** wired by `Chain` (outermost-first): **Version** (X-API-Version negotiation, 406 on unsupported), **Signing** (optional HMAC-SHA256 `X-Signature` over method+path+body, 401 on mismatch; body buffered+restored), **Auth**, **RateLimit**, **Idempotency**. **Auth consumes an `Authenticator` interface → typed `Principal` (subject/tenant/roles) on ctx; deny-by-default when a required role is configured** (missing→403, missing token→401, nil authenticator disables auth for dev). **The bundled `JWTAuthenticator` is a deliberately minimal stdlib-only HS256 validator — the stand-in AUTH-01a replaces with real OIDC/JWKS behind the SAME interface** (so the swap touches only the constructor, not the chain). **Per-tenant token-bucket RateLimit** keyed on the authenticated tenant (fallback subject→remote addr) so one tenant can't starve another — hand-rolled lazy-refill bucket, no x/time dep (CLAUDE.md dep-hygiene), 429 + Retry-After on exhaustion. **Idempotency** replays the cached status+body for a repeated `Idempotency-Key` on non-GET (bounded+TTL store), so a client safely retries a scenario POST without re-evaluating · `kanz/services/api-gateway/internal/middleware/`
- [x] **API-01c** api-gateway BFF — new `kanz/services/api-gateway/` (`cmd/api-gateway/main.go`, `internal/{config,gateway}`). **REST↔gRPC transcoding via protojson over the generated `query.v1.RiskQueryServiceClient` interface** — each of the four read routes (`GET /v1/portfolios/{id}/exposure|measures`, `POST .../scenario`, `GET /v1/health`) builds the proto request, calls the gRPC client, and marshals the proto response with protojson, so REST and gRPC are the SAME messages (no hand-maintained DTOs). **Hand-rolled transcoding chosen over grpc-gateway codegen (the load-bearing decision)**: a four-RPC read surface doesn't justify `google.api.http` annotations + generated `.gw.go` + an extra schema-pipeline plugin; a thin protojson adapter keeps deps to the protobuf runtime already in the module, and a static **OpenAPI 3.0 doc** is emitted at `/openapi.json` so clients still get a machine-readable contract. **Depends on the client INTERFACE** so tests inject a fake and main injects an mTLS-dialed client. **main** builds the OBS-01 provider (`/metrics`, trace-correlated logger), dials risk-engine over **mTLS when `API_GATEWAY_SPIFFE_SOCKET` is set** (SEC-01b `AuthorizeMesh`; `grpc.NewClient` lazy-connects so a momentarily-down upstream doesn't fail startup) else plaintext, mounts the `/v1` routes behind the edge chain while keeping `/healthz`/`/readyz`/`/metrics`/`/openapi.json` un-gated, and shuts down gracefully. gRPC-code→HTTP-status mapping mirrors the grpc-gateway table. **Follow-ups**: tighten upstream authz to `AuthorizeServices(risk-engine)`, add the api-gateway to the CICD-01c build matrix + a distroless Dockerfile · `kanz/services/api-gateway/`
- [x] **API-01b** Risk query gRPC server — new `kanz/services/risk-engine/internal/grpcsrv/server.go` (+ tests), wired into the risk-engine main. **A thin ADAPTER, not logic**: implements the generated `query.v1.RiskQueryServiceServer` over the in-process `api/v1.Engine` (the concrete `EngineImpl`, ORCH-01b) — every method decodes the proto request → api/v1 request, calls the Engine, encodes the api/v1 response → proto. All risk decisions (live-vs-cache fallback, staleness flags, scenario eval) stay in the Engine; the server only translates + maps errors to gRPC codes (`ErrPortfolioNotFound`→NotFound, `ErrInvalidRequest`→InvalidArgument, ctx→Canceled/DeadlineExceeded, else Internal). **Reuse, not re-derive**: response payloads go through `publish.ToProtoExposureSet`/`ToProtoMeasureSet` (the RISK-10 converters, exported for this) so a value read over RPC is byte-identical to the same FACT on the bus — one domain→proto mapping, not two. The `api/v1.ExposureSet`/`MeasureSet` interfaces only expose `AsOf()`/`Lookup()` (insufficient to serialize), so the adapter type-asserts the concrete `*domain.ExposureSet`/`*domain.MeasureSet` — sound because the engine always returns those, and **permitted because grpcsrv is under the RISK-02 `riskComposerPrefix` carve-out** (the risk-engine service is the module's composition root). **Wired into main**: shares the live `store` + `cache` + `registry` with the recomputer (so queries see live state + the recomputer's last-known-good cache), constructs `engine.New(store, registry, cache, NewDetector())`, serves on `RISK_ENGINE_GRPC_LISTEN` with **optional mTLS** — `RISK_ENGINE_SPIFFE_SOCKET` set ⇒ `transport.ServerOption(src, AuthorizeMesh())` (SEC-01b, the API-01c gateway dials over this), empty ⇒ plaintext (local/dev) — and `GracefulStop` on shutdown. Scenario shocks decode from the proto `oneof` into the api/v1 `PriceShock`/`ParallelShift`; an empty oneof member is InvalidArgument. Zero response `as_of` elides to a nil timestamp (absent, not epoch). 7 adapter tests (set/flag conversion, measure filter forwarding, shock decode, no-kind-shock rejection, mode mapping, error-code matrix, as_of round-trip + zero-elision); arch boundary test still green · `kanz/services/risk-engine/internal/grpcsrv/server.go`
- [x] **API-01a** Risk query gRPC proto — new `kanz-schemas/proto/query/v1/risk_query.proto` + CODEOWNERS entry, **begins the Phase-2 API epic**. `RiskQueryService` is the proto mirror of the `kanz/internal/risk/api/v1.Engine` Go interface: `Exposure`/`Measures`/`EvaluateScenario`/`Health` RPCs, **query + scenario + health ONLY — no Apply/Mutate/Command** (the "all state changes are event-driven" anti-decision holds at the wire surface too). **Reuse over redefinition**: responses embed the SAME `domain.v1.ExposureSet` + `domain.v1.RiskMeasureSet` the bus publishes (a value read over RPC is byte-identical to the FACT on the bus), so only the request envelopes, the scenario-shock taxonomy, and the query-trust signals are new. **Scenario shocks are a `oneof` (`PriceShock`/`ParallelShift`)** mirroring the api/v1 concrete shock types, additive via new members. **Query-local `Mode` + `QualityFlag` enums** (NORMAL/DEGRADED, DEGRADED/STALE) — deliberately NOT envelope.v1.QualityFlag, whose REPLAYED/LATE values are event-stream concerns, not query-result trust. **RPC/message naming follows buf STANDARD** (the EvaluateScenario RPC needed `EvaluateScenarioRequest/Response`, not `Scenario*`, to satisfy `RPC_REQUEST_STANDARD_NAME`). CODEOWNERS: `/proto/query/` → risk-engine + architecture. buf-discovered (under the existing `proto` module); lint clean; **Go SDK regenerated via a Go-only buf template** (both `protoc-gen-go` + `protoc-gen-go-grpc`, so `risk_query.pb.go` + `risk_query_grpc.pb.go` emit) — gen/go + the kanz module both build against it. The server impl (API-01b inside risk-engine over the concrete EngineImpl + durable state) and the gateway BFF (API-01c) are the next subtasks · `kanz-schemas/proto/query/v1/risk_query.proto`

### Observability & Telemetry

- [x] **OBS-01e** Tests pinning the DATA-08 metric contract — `kanz/internal/integrity/metrics_test.go` + the OBS-01c slices in `kanz/pkg/bus/telemetry_test.go`. **`TestMetricsContract` gathers the registry and asserts the EXACT series names AND label sets (sorted) for all nine `kanz_data_*` families, plus a reverse guard: any `kanz_data_*` series NOT in the expected map fails as contract drift** — so a rename or a stray label on either side (exporter or dashboard/alert) breaks the test, not a panel at 3am. Values are checked too (lag/age math, gap-missing accumulation, non-gap/unknown no-ops). Bus telemetry tests cover OBS-01c (`kanz_bus_publish_total{result=ok}` + a latency sample) and OBS-01d (traceparent stamped on the envelope, round-trips to a valid span context with matching trace_id) + nil-metrics-is-safe. Runs on every `go test` (pure, no infra) · `kanz/internal/integrity/metrics_test.go`, `kanz/pkg/bus/telemetry_test.go`
- [x] **OBS-01d** W3C `trace_context` → OTel span propagation across every bus hop — new `kanz/pkg/observability/trace.go` + `kanz/pkg/bus/trace.go`, wired into Producer/Consumer. **The envelope already carries `trace_context` (EVT-17b); OBS-01d makes it a REAL OTel trace, not just a copied string.** `observability.TraceparentFromContext` injects the active span to a W3C traceparent (the envelope's single-field carrier); `ContextWithTraceparent` extracts an inbound traceparent into ctx as the remote parent. **Producer.Publish now opens a PRODUCER span and stamps ITS traceparent** (trace precedence: explicit Event field > active span > legacy string ctx — the string path kept for span-unaware callers); **Consumer extracts the inbound trace onto ctx + opens a child CONSUMER span** so a handler's logs/spans and any derived publish continue the same trace_id. **The bus needs no injected tracer**: `otel.Tracer(...)` is late-bound to the global provider, so spans are no-ops until `observability.New` installs one (zero-config, zero-overhead when telemetry is off) · `kanz/pkg/bus/`, `kanz/pkg/observability/trace.go`
- [x] **OBS-01c** Risk + bus RED/USE metrics + lag/pending gauges — new `kanz/pkg/bus/metrics.go` (`BusMetrics`) + `kanz/internal/risk/engine/metrics.go` (`Metrics`), wired into Producer/Consumer/Recomputer + the risk-engine main. **`kanz_risk_recompute_total{status}` + `kanz_risk_recompute_duration_seconds` match the CICD-01e canary analysis verbatim** — the names/`status` label were the contract that template already queries (seam-ahead-of-producer), so the rollout's success-rate + p99 gates now resolve against real series; recompute counts a store-miss as no-op (not a failure) and `status=error` only on an emit failure. **Bus RED**: `kanz_bus_{publish,consume}_total{result}` + `_duration_seconds`; consume records the handler outcome (DLQ-routed still counts `error`), unframe/validate failures count with 0 duration. **lag/pending gauges** (`kanz_bus_consumer_lag`, `kanz_bus_pending_messages`) are the KEDA scale signal — exposed with setters fed by a broker-stats poller (kafka-go `Reader.Stats().Lag` / NATS `ConsumerInfo.NumPending`); the per-message path doesn't carry them, so the poller is the documented wiring. **Both exporters are optional + nil-safe** (a nil `*BusMetrics`/`*Metrics` is a no-op on every method) so non-instrumented callers and the existing tests are untouched; wired via additive `ProducerConfig.Metrics`, `WithBusMetrics`, and a backward-compatible variadic `engine.WithMetrics` · `kanz/pkg/bus/metrics.go`, `kanz/internal/risk/engine/metrics.go`
- [x] **OBS-01b** Integrity metrics exporter — new `kanz/internal/integrity/metrics.go` (`Metrics`). **A thin translation layer wrapping the DATA-01..05 detector RESULTS into the exact nine `kanz_data_*` series the dashboards + DATA-09 alerts query** — the detectors stay pure in-memory classifiers (the README's detect-then-export discipline), the orchestrator that owns them calls `ObserveStaleness/Gap/Reconcile/Drift/Discrepancies/QualityEvent` per result. Labels reuse the detector vocabulary verbatim (`subject`/`partition_key`/`source`, `transport`/`seen_on`/`missing_on`, `feature`/`metric`, `kind`/`severity`). **Decisions**: `StalenessUnknown` (no subject) is dropped rather than emitting an empty-label series; `last_event_age` is set from the result at record-time via an injectable clock (a scrape-time frontier-aging collector is a noted follow-up); drift exports BOTH score and threshold so the alert's `> on(feature,metric)` self-tracks each detector's threshold; `quality_events` takes kind/severity as strings to keep the exporter decoupled from the observation schema · `kanz/internal/integrity/metrics.go`
- [x] **OBS-01a** Telemetry foundation — new `kanz/pkg/observability/` (`observability.go`), **begins the Observability epic**. One `Provider` bundling a Prometheus registry, an OTel `TracerProvider`, and a trace-correlated slog logger, plus `MetricsHandler()` (promhttp) and `Shutdown`. **Metrics go through Prometheus client_golang DIRECTLY, not an OTel→Prometheus exporter** (the load-bearing choice): the DATA-08 / `kanz_risk_*` contracts pin exact series names, and the otel exporter mangles them with unit/`_total` suffixes — so OBS-01b/c register their collectors on `Provider.Registry` and get the names verbatim. **Tracing uses the OTel SDK** (W3C TraceContext+Baggage propagator installed globally, OTLP/gRPC export only when `OTLPEndpoint` set so startup never blocks on a collector) so the envelope `trace_context` propagates (OBS-01d). **The "slog→OTel bridge" is a handler decorator that stamps the active span's trace_id/span_id onto every record** — a correlation bridge, not the OTel log SDK (kanz logs are stdout JSON shipped by the platform; the need is log↔trace join, not a second export path); the decorator preserves the wrapper through `WithAttrs`/`WithGroup`. Baseline Go+process collectors registered for USE panels. Resource schema pinned to semconv v1.40.0 to match the SDK's `resource.Default()` (a mismatch fails the merge). Wired into the risk-engine main (`/metrics`, trace-correlated logger, OTLP endpoint config). Tests verify trace injection, /metrics baseline, and the global propagator — run on every `go test` · `kanz/pkg/observability/observability.go`

### Delivery, Build & Observability

- [x] **CICD-01e** Progressive delivery + automated rollback — new `kanz/infra/deploy/` (`risk-engine-rollout.yaml`, `analysis-template.yaml`, README), **closes the CICD-01 epic**. **Argo Rollouts replica-based canary, NOT a mesh traffic-split (the load-bearing choice)**: risk-engine is a NATS consumer + gRPC query service with no ingress, so weight-based traffic routing would need a service mesh it doesn't have — Argo Rollouts shifts a fraction of *pods* (20%→50%→100% with pauses) and gates each step on a Prometheus `AnalysisRun`, no mesh required. **Auto-rollback = the analysis abort**: `failureLimit: 0` means one bad measurement aborts the rollout, which scales the canary to zero and leaves the stable revision serving — no human in the unhappy path. Analysis (`AnalysisTemplate`) scopes every query to the canary pods via the `rollouts-pod-template-hash` arg (a regression in NEW code can't hide behind stable traffic) and asserts recompute success-rate ≥99% + p99 ≤500ms (the ORCH-01f budget) against the **OBS-01 `kanz_risk_*` series** — which don't exist until OBS-01c ships, so the queries are the contract the canary asserts (seam-ahead-of-producer, cf. PRED-04/11). **The Rollout pod composes the whole platform stack**: the CICD-01c/d cosign-signed image (admission-verified, CICD-01d), the SEC-01a SVID (SPIFFE CSI socket + `risk-engine` ServiceAccount), and the SEC-01d CSI secret for the DB DSN (`RISK_ENGINE_DATABASE_URL_FILE`, no plaintext env), on the ORCH-01a `/healthz`+`/readyz` probes. schema-registry's Rollout, digest-pinning over `:latest`, and a consumer-lag abort metric are documented follow-ups. YAML validated · `kanz/infra/deploy/`
- [x] **CICD-01d** Supply-chain gate — new `.github/workflows/release.yml` + `kanz/infra/security/admission/` (`cluster-image-policy.yaml` + README). **On a `v*` tag, per service: build→push→scan→SBOM→sign→verify.** trivy scans the pushed digest with `exit-code:1 severity:CRITICAL ignore-unfixed` (fails the job before signing, so a vulnerable image lands unsigned and admission then refuses it); syft (anchore/sbom-action) emits an SPDX SBOM; cosign signs AND `attest --type spdxjson` attaches the SBOM. **Signing is KEYLESS (the load-bearing decision)** — GitHub OIDC → Fulcio short-lived cert → Rekor transparency log, no private key to store/rotate; the signer's *identity* is the credential, the same "identity not a stored secret" stance as SEC-01 (SPIRE SVIDs, Vault SPIFFE auth). Everything after build pins the **immutable digest** (`steps.build.outputs.digest`), never a tag. A `cosign verify` self-check (issuer = GitHub OIDC, identity-regexp = this workflow on a `v*` tag) gates the release on the same predicate the cluster enforces. **Admission verification is the cluster half** (task path names only release.yml, but the gate isn't real without enforcement): a sigstore policy-controller `ClusterImagePolicy` over `ghcr.io/kanz-eng/**` requires (1) a cosign signature from the release-workflow keyless identity (issuer + exact `…/release.yml@refs/tags/v*` subject pinned) AND (2) the SPDX SBOM attestation (provenance-or-reject), `mode: enforce` = fail-closed; webhook scopes to `policy.sigstore.dev/include: "true"` namespaces. SDK-gen/build steps reuse CICD-01a/c. Pinning the distroless base + actions by digest, and bumping trivy to fail-on-HIGH once base images are clean, are documented follow-ups. Not runnable locally (no GHCR/OIDC/docker daemon) — standard sigstore keyless + trivy + syft wiring; both YAML validated · `.github/workflows/release.yml`, `kanz/infra/security/admission/`
- [x] **CICD-01c** Per-service distroless container builds — new `kanz/services/{risk-engine,schema-registry}/Dockerfile`, root `.dockerignore`, and `.github/workflows/build.yml`. **Multi-stage: `golang:1.26.1` builder → `gcr.io/distroless/static:nonroot`.** `CGO_ENABLED=0` static binary (every dep — pgx/nats/kafka-go/go-spiffe — is pure Go, verified) is exactly what distroless/static needs: no shell, no libc, no package manager, runs as uid 65532. `-trimpath -ldflags="-s -w"` for a lean reproducible binary. **The load-bearing build decision — context is the repo ROOT, not `kanz/`**: the module's `replace … => ../kanz-schemas/gen/go` points outside the module, so the Dockerfile COPYs BOTH `kanz/` and `kanz-schemas/gen/go/` side by side, and `GOFLAGS=-mod=mod` lets the build resolve the local-replace module. The SDK is generated-not-committed (EVT-15a), so the **workflow runs `buf generate` on the runner first** (reusing the CICD-01a Go-SDK steps incl. protoc-gen-go-grpc) rather than dragging buf + plugins into the Docker builder; `build-push-action` then builds with **`context: .`** (the local checkout incl. the generated `gen/go`, NOT the git context which would omit the uncommitted SDK). Layer-caching split (go.mod + gen/go → `go mod download` → source) + GHA buildx cache per service. `.dockerignore` trims junk but **deliberately keeps `kanz-schemas/gen/go`** (the build needs it) while dropping `gen/python`/`gen/ts`/`.gotmp`/node_modules/pycache. **Workflow**: matrix over the two services, builds on every PR (validation, `push: false`) and pushes to `ghcr.io/<owner>/<service>` on push to main via `metadata-action` tags (sha + branch + latest-on-default). Distroless digest-pinning + SBOM/scan/sign/admission are the CICD-01d supply-chain follow-up. Image build unverified locally (no docker daemon on the dev box) but the static binary compile — the substantive builder step — is verified for both services; workflow + Dockerfile YAML/syntax checked · `kanz/services/*/Dockerfile`, `.github/workflows/build.yml`
- [x] **CICD-01b** Python + TypeScript jobs + buf schema gate — extends `.github/workflows/kanz-ci.yml` with `python`, `ts`, and `buf-breaking` jobs, plus trigger on `kanz-py/**`. **`python` job is self-contained via `grpc_tools.protoc`, NOT buf** — the kanz-py tests need the generated Python SDK (uncommitted EVT-15a, unpublished DEBT-01b), and running the full `buf.gen.yaml` in a Python job would drag in the Go local plugins; `grpc_tools.protoc --python_out --pyi_out --grpc_python_out` over all protos emits the whole tree in one tool with no Go/buf dependency. **Crucially `--grpc_python_out` closes the Python gRPC-stub gap** (the analog of the Go protoc-gen-go-grpc gap PRED-13 fixed): `kanz_inference.interactive.servicer` imports `inference.v1.inference_service_pb2_grpc`, which `protocolbuffers/python` alone never emits — so the PRED-08 servicer test (and the module at runtime) was unimportable. Wiring this into the buf *release* pipeline so the **published** SDK ships the stub stays a follow-up; the CI gate generates it directly. PYTHONPATH spans the SDK root + `kanz-py` (PEP-420 namespace pb2 packages); broker integration tests self-skip. **Also repaired `tests/test_consumer.py` (EVT-18d), 16 tests that never ran** (the recurring "done but never executed" failure, like PRED-07): 10 failed on `Message(body=…)` missing the required `subject` positional (added `subject="x.y.z"`); the other 6 set a custom `idempotency_key` on a FACT without matching `event_id`, which `validate()` rejects per the FACT `idempotency_key==event_id` invariant — fixed at root with `_envelope(event_id="evt-A")` (sets both), intent-preserving since dedup still keys on that value. Full suite now **159 passed, 3 skipped** (verified locally). **`ts` job** generates the Protobuf-ES SDK via a **TS-only buf template** (mirrors the pinned `bufbuild/es:v2.10.0`, avoids the Go plugins), assembles `gen/ts` into the `@kanz-eng/kanz-schemas` package, then `npm pkg set`s kanz-bus's dep to a `file:` path so npm resolves the unpublished SDK from disk, and runs the package's real test runner (`tsx --test`, i.e. `node:test` — the task's "vitest" is loose shorthand; the project doesn't use vitest). **`buf-breaking` job** runs `buf lint` + `buf breaking --against '../.git#branch=main,subdir=kanz-schemas'` (fetch-depth 0), enforcing the additive-only envelope discipline (EVT-21c) on proto PRs; a no-op when no proto changed. TS job unverified locally (no buf/network here); Python + YAML verified · `.github/workflows/kanz-ci.yml`
- [x] **CICD-01a** GitHub Actions Go pipeline — new `.github/workflows/kanz-ci.yml` at the **monorepo root** (where GitHub reads workflows and where it can see both `kanz/` and `kanz-schemas/`; the nested `kanz-schemas/.github/` workflows are for that companion repo when split out). One `go` job: checkout → `setup-go` with `go-version-file: kanz/go.mod` (CI tracks the pinned 1.26.1 toolchain, never drifts) → `buf-setup` → **install BOTH `protoc-gen-go@v1.36.11` and `protoc-gen-go-grpc@v1.5.1`** → **`buf generate` the kanz-schemas Go SDK + `go mod init`/`tidy` in `gen/go`** (the SDK is generated-not-committed per EVT-15a, and the `kanz/go.mod` `replace … => ../kanz-schemas/gen/go` resolves to it within the single checkout) → `go build` → `go vet` → `go test -race ./...` → `golangci-lint`. **`GOFLAGS=-mod=mod` is a job-level env, the load-bearing knob**: the kanz-schemas-go dep is a local `replace`, so the build must be allowed to update go.mod/go.sum for the freshly generated module rather than failing read-only (matches the documented local-build procedure). **protoc-gen-go-grpc is mandatory, not optional** — it emits the `inference.v1` gRPC stubs (PRED-06) that protoc-gen-go alone omits, so `buf generate` without it leaves the module uncompilable. **Arch + contract suites need no special invocation** — `test/arch` + `test/contract/*` are ordinary untagged `_test.go`, so `go test ./...` runs them; the Postgres/Kafka integration tests skip without `TEST_POSTGRES_URL`/`TEST_KAFKA_BROKERS` so the gate needs no live brokers. **`-race` needs cgo** (default-on on ubuntu runners; can't run on this Windows box, no gcc — but build/vet/`go test ./...`/arch+contract all verified green locally). **golangci-lint via `install-mode: goinstall`** so the linter is built with the job's Go 1.26 toolchain and its parser always understands the module's language version (a pre-pinned binary built with an older Go could choke on 1.26 syntax); `--timeout=5m`. Triggers on PRs touching `kanz/**` or the schema proto/buf config, and on push to main. CICD-01b extends this same file with the Python/TS jobs + `buf breaking` gate · `.github/workflows/kanz-ci.yml`

### Security & Identity

- [x] **SEC-01e** Tests: mTLS handshake + peer-identity assertion + plaintext/unauthorized rejection — new `kanz/pkg/transport/mtls_test.go`, **closes the SEC-01 zero-trust epic**. The integration half SEC-01b/c deferred ("need a real SVID pair"), with **no external SPIRE**: the test mints a real in-memory CA + leaf X509-SVIDs (real ECDSA keys, real URI-SAN SPIFFE IDs via `id.URL()`, digitalSignature + client/server ExtKeyUsage) and drives a real TLS handshake, so go-spiffe's `tlsconfig` verification + the AuthorizeMesh/AuthorizeServices policy run end-to-end on **every `go test`** — not behind a `TEST_*` gate (PERS-01e anti-rot rule; pure crypto, no infra). 5 cases: (1) **handshake asserts peer identity** — two valid in-domain SVIDs complete mTLS and EACH side reads back the peer's SPIFFE ID via `x509svid.IDFromCert` (the property the mesh relies on, not just "handshake ok"); (2) **unauthenticated** — a no-client-cert TLS client is rejected by `RequireAnyClientCert`; (3) **plaintext** — raw cleartext bytes at the TLS server fail (no cleartext downgrade); (4) **unauthorized** — a valid in-domain SVID NOT on the server's `AuthorizeServices` allow-list is rejected (auth necessary ≠ sufficient, deny-by-default); (5) **untrusted CA** — a leaf chaining to a foreign CA fails verification. **Load-bearing test-harness decision**: `handshake()` uses a loopback **TCP** listener, NOT `net.Pipe` — on a rejected handshake the server writes a TLS alert, and unbuffered `net.Pipe` blocks that write until the 5s ctx deadline (peer stopped reading), making every rejection test take 5s; TCP's socket buffer flushes the alert so the rejecting side returns its error immediately (full suite 21s → 1.3s). `-race` unavailable on this machine (no cgo/gcc) but the `sErr` write/read is channel-synchronized so there's no race; `go vet` clean. Updated the `tls_test.go` header that called these tests deferred · `kanz/pkg/transport/mtls_test.go`
- [x] **SEC-01d** Secrets via Vault/cloud KMS through CSI mounts — new `kanz/infra/security/secrets/` (`vault.yaml`, `csi.yaml`, `secretproviderclass.yaml`, `spire-upstream.yaml`, README runbook) + a `config.secret()` `<VAR>_FILE` seam in both Go services. **Secrets reach pods as tmpfs files via the Secrets Store CSI Driver + `vault-csi-provider`, NEVER as k8s `Secret`s (base64-in-etcd, broad `get secret` RBAC) or env vars (`/proc/<pid>/environ`, crash dumps, child procs).** **Vault auth is the SPIFFE JWT-SVID** (SEC-01a's `default_jwt_svid_ttl=5m`) against Vault's `jwt` backend via the SPIRE OIDC discovery endpoint — secret access gated on the SAME attested identity as mTLS transport (SEC-01b), no second static credential; the `jwt` role's `bound_subject` = the consumer's SPIFFE ID, so it's deny-by-default per-service (mirrors SEC-01b `AuthorizeServices`). **One cloud KMS key is the platform root**: it both auto-unseals Vault (`seal "awskms"`, no Shamir shares) AND backs the SPIRE `UpstreamAuthority` — neither root ever exists as recoverable plaintext. **Plaintext removed**: the Postgres DSN is the only genuine runtime secret (everything else is mTLS where the SVID *is* the credential, so there's no password). `config.secret()` reads it from `<VAR>_FILE` in preference to the plaintext `<VAR>` env (risk-engine + schema-registry); `spire-upstream.yaml` replaces SPIRE's plaintext `keys.json` disk root + sqlite `connection_string` with the KMS `UpstreamAuthority` + a CSI-mounted postgres DSN — closing the SEC-01a HA/KMS follow-up. Vault itself is a mesh workload (SVID-fronted TLS listener via the SEC-01c spiffe-helper bridge, since Vault isn't SPIFFE-aware). Bootstrap posture (1 Raft replica) matches NATS/SPIRE; HA ≥3 peers + Vault `database` dynamic-cred engine are documented follow-ups. Unit test on the `secret()` seam (file-beats-env, trim, unreadable-falls-back) runs every `go test` — the plaintext-preference regression is security-relevant; YAML-validated · `kanz/infra/security/secrets/`
- [x] **SEC-01c** TLS/SASL on NATS + Kafka clients + broker manifests — `kanz/pkg/bus/{nats,kafka}.go` gain optional transport security, and `infra/nats` + `infra/kafka` get mTLS listeners fed by the SEC-01a SVIDs. **`NATSConfig.TLSConfig`** (+ NATS-native `Username`/`Password`/`Token`) → `nats.Secure`; **`KafkaConfig.TLSConfig` + `SASL sasl.Mechanism`** wired to BOTH the writer `Transport` and a shared reader `Dialer` (nil/nil ⇒ kafka-go DefaultDialer, exact prior plaintext behaviour). **bus stays decoupled from go-spiffe** — it takes a ready `*tls.Config` (built by the caller from `transport.ClientTLSConfig`, SEC-01b) rather than a SPIFFE source, same injection style as the rest of bus; mTLS is transport encryption AND auth (the client SVID is the credential). **Infra**: each broker gets a dedicated ServiceAccount (distinct SVID identity `…/sa/{nats,kafka}`), a `spiffe-helper` init-container (fetch first SVID before boot) + sidecar (renew + reload), a memory-backed cert emptyDir, and the SEC-01a SPIFFE CSI socket volume. NATS: `tls { verify: true }` on the client listener (reload via `--signal reload` in a shared PID namespace). Kafka: new `SSL://:9094` listener, `ssl.client.auth=required`, PEM keystore (helper concatenates cert+key into the combined `keystore.pem` Kafka's PEM store needs) + bundle truststore, endpoint-identification disabled (verify the SPIFFE URI SAN, not DNS); PLAINTEXT:9092 kept for inter-broker + the bootstrap job during migration. Broker-free unit tests assert the Kafka TLS/SASL config reaches the writer Transport + reader Dialer (and nil-dialer on plaintext); handshake/rejection integration is SEC-01e. **Also fixed a pre-existing latent failure**: `test/arch/risk_boundary_test.go` (RISK-02) flagged the risk-engine SERVICE as a boundary violator for importing impl packages — but ORCH-01/PERS-01 made it the module's designated composition root (wiring the concrete engine needs engine/state/compute/publish/ingest; api/v* is interfaces only). The test had been red since ORCH-01b and only surfaced now that the full suite runs (the `%TEMP%` Application-Control exec-block had been silently failing test binaries; running with a project-local `GOTMPDIR` fixed it). Added a `riskComposerPrefix` carve-out for the service — the standard "main/wiring layer may import internals" exemption · `kanz/pkg/bus/`, `kanz/infra/nats/`, `kanz/infra/kafka/`
- [x] **SEC-01b** Reusable mTLS transport helpers — new `kanz/pkg/transport/tls.go`, the single seam turning SEC-01a SVIDs into mutual-TLS for every gRPC client/server (covers the PRED-06/07 inference path + the future API gateway). Wraps **go-spiffe/v2** (added as a direct dep — the canonical SPIFFE library matching the deployed SPIRE control plane; hand-rolling SVID verification + bundle rotation + the client `InsecureSkipVerify`+`VerifyPeerCertificate` dance would be more code and a bigger attack surface, so it's a focused security dep, not bloat). **`NewSource` keeps a live, auto-rotating Workload API handle** (`*workloadapi.X509Source`, socket from the SEC-01a CSI mount or `SPIFFE_ENDPOINT_SOCKET`) supplying BOTH the local SVID and the trust bundle — go-spiffe re-fetches at ~half the 1h TTL so long-lived servers swap fresh certs with zero reload code here. **`ServerTLSConfig`/`ClientTLSConfig` return `*tls.Config`** (the literal deliverable) via `tlsconfig.MTLS{Server,Client}Config`, plus `Server/ClientCredentials` (gRPC `TransportCredentials`) and `ServerOption`/`ClientDialOption` (grpc.ServerOption / DialOption) convenience wrappers. **Authorization is deny-by-default and explicit**: `AuthorizeMesh()` accepts any valid SVID in the `kanz.internal` trust domain (intra-mesh default; finer access control is AUTH-01's app-layer job), `AuthorizeServices(ids…)` narrows to an allow-list (e.g. gateway accepts only risk-engine) — there is deliberately NO "authorize any trust domain" helper. `ServiceID(ns, sa)` builds `spiffe://kanz.internal/ns/<ns>/sa/<sa>` matching the SEC-01a ClusterSPIFFEID template. **Enabling hook into PRED-07**: `SyncClientOptions` gained an optional `Credentials credentials.TransportCredentials` field (nil ⇒ plaintext as before, set ⇒ mTLS) so the inference sync client actually uses these — "covers PRED-07" is real, not aspirational, without a separate constructor. Unit tests cover the pure helpers (ID format + empty rejection, authorizer construction, server-config RequireAnyClientCert + VerifyPeerCertificate + TLS1.2 floor, client-config InsecureSkipVerify+verify, gRPC wrappers non-nil) and run on every `go test`; the mTLS handshake + peer-assertion + plaintext-rejection integration tests are SEC-01e (need a real SVID pair) · `kanz/pkg/transport/tls.go`
- [x] **SEC-01a** Workload identity issuance + rotation (SPIRE) — new `kanz/infra/security/spire/` (`namespaces.yaml`, `rbac.yaml`, `spire-server.yaml`, `spire-agent.yaml`, `registration.yaml`, README), **begins the Security epic**. SPIRE server (StatefulSet + controller-manager sidecar) + per-node agent DaemonSet + SPIFFE CSI driver issue auto-rotated **X.509-SVIDs** to every pod in a `kanz.internal/spiffe: enabled` namespace (kanz-messaging — opted in via a one-line label on `infra/nats/namespace.yaml` — plus a new `kanz-services`). **SPIRE chosen over cert-manager** because the requirement is workload *identity* (attest *what* a workload is — `k8s_psat` node + `k8s` pod attestation — before issuing), not certificate provisioning to whoever holds a secret. **Trust domain `kanz.internal`, SPIFFE ID `spiffe://kanz.internal/ns/<ns>/sa/<sa>` keyed on the ServiceAccount, not the pod** — a rolling deploy keeps a stable identity while leaf certs rotate underneath; this ID is what SEC-01b asserts on peers and AUTH-01c binds to the command issuer. **Rotation is the actual deliverable**: `ca_ttl=24h`, `default_x509_svid_ttl=1h` (go-spiffe re-fetches at ~30m half-life), `default_jwt_svid_ttl=5m` — a leaked leaf is valid ≤1h and no human ever touches a cert. **Declarative issuance via a `ClusterSPIFFEID` CR** (controller-manager reconciles it into registration entries + publishes the trust bundle to the `spire-bundle` ConfigMap) — GitOps-friendly vs imperative `entry create` drift. **Node attestation = projected SA token** (TokenReview, the only cluster-write RBAC the server needs); **SVID reaches workloads via the SPIFFE CSI driver** (read-only ephemeral volume mounting the agent socket) so SEC-01b's go-spiffe client dials `unix:///run/spiffe/spire-agent.sock`. **Bootstrap posture mirroring the NATS single-StatefulSet stance**: 1 server replica on a sqlite PVC + disk keys; HA (postgres DataStore reusing the PERS-01/registry cluster, ≥2 replicas, KMS-backed `UpstreamAuthority` root) is a documented SEC-01d follow-up. CRDs ship with spire-controller-manager, applied once per cluster before `registration.yaml`. All manifests YAML-validated · `kanz/infra/security/spire/`

### Platform Runtime

- [x] **PERS-01e** Tests: crash-recovery + idempotent-replay — new `kanz/internal/risk/state/persist/postgres_test.go` (gated integration) + a DB-free `kanz/services/risk-engine/internal/app/bootstrap_test.go`, **closes the PERS-01 durable-state epic**. **Two test files is the load-bearing decision, against the codebase's recurring "tests that never ran" failure** (PRED-07/13, test_consumer.py): `postgres_test.go` is gated on `TEST_POSTGRES_URL` (skips in CI without a DB, same convention as the Kafka `TEST_KAFKA_BROKERS` suites) — so the epic's headline property, idempotent-replay, is ALSO proven by a DB-free `bootstrap_test.go` that runs on every `go test`. Without the second file the "re-applied events don't double-count" guarantee would skip silently in CI. **The idempotent-replay test is designed around set-semantics**: risk applies are last-wins (SetPosition replaces, not accumulates), so re-applying the *same* event proves nothing — the meaningful test replays a SUPERSEDED OLDER event (value 1000, as_of t1) against restored newer state (value 2000, as_of t2) whose AppliedKeys already holds the old event's key, and asserts state does NOT regress to 1000 (positions have no as_of-monotonicity guard, so that regression is the real corruption the seeded dedup prevents). Contrast test: a genuinely-new key IS applied (gap recovery). **`bootstrap_test.go` is `package app` (internal)** because the `newSource`/`now` seams are unexported — injecting an in-memory replay log + deterministic clock requires being inside the package; 8 tests: restore-rehydrates, idempotent-no-regress, new-events-applied, malformed-frame-skip-and-continue, source-error-aborts, no-resume-position-skips-replay (factory never called), LoadAll-error-aborts, restore-doesn't-clobber-live-state. **`postgres_test.go` reads the real `0001_state.sql`** (relative path, `DROP … CASCADE` then apply) so the migration stays the single source of truth; crash-recovery parity proves committed state reloads byte-for-byte across a fresh pool (RPO=0). The Postgres suite still needs a one-time run against a live DB to confirm the SQL — flagged like the EVT-20a Kafka integration test · `kanz/internal/risk/state/persist/postgres_test.go`
- [x] **PERS-01d** Restart-recovery bootstrap — new `kanz/services/risk-engine/internal/app/bootstrap.go` (`Bootstrap`), plus `state.Store.Restore` and the main wiring that closes the ORCH-01e/01c seams. **`Bootstrap.Run` = restore (`StateStore.LoadAll` → `state.Store.Restore` each) → replay (Kafka forward from each snapshot's LogPosition via the EVT-20a `replay.Reader`) → return so main switches to the live NATS spine.** **Replay applies through the BARE `state.Store`, NOT the `TriggeringApplier`** — feeding replay through the recompute decorator would emit a storm of intermediate, immediately-superseded risk FACTs; the query path computes from state on demand and live ingestion re-arms recompute, so replay stays side-effect-free. **Idempotency is the seeded dedup window, not offset precision**: `state.Store.Restore` pre-seeds each portfolio's dedup window from the persisted AppliedKeys (no per-aggregate lock, no-op-if-present — single-threaded pre-ingest, and a snapshot is never newer than the running engine), so replay can start at-or-before the true resume point and already-applied events are recognized as Seen and skipped (RPO=0, no double-count). **Resume coordinate = per-(topic,partition) MIN(offset+1) across snapshots** (`log_position` resume-at-offset+1 semantics); one `replay.Reader` per resume partition, `Range{StartOffset, EndTime: bootstrapStart}` so the read terminates and the post-`now` gap is covered by the live spine + dedup absorbing overlap. **A (topic,partition) with NO recorded position is left to the live spine, never replayed from genesis** (logged warning) — and since the 01c `LogPositionSource` is still nil, records carry nil positions today, so replay is effectively skipped until that seam is wired; the restore phase is the live half of the epic. **`eventSource` interface + `sourceFactory` field** is the test seam (default wraps `replay.NewReader`). Error discipline mirrors the EVT-20b Pipeline (`errors.As` `*MalformedFrameError` ⇒ skip; ctx-cancel/source error ⇒ abort; per-event Handler error ⇒ skip). **Main wiring**: `RISK_ENGINE_DATABASE_URL` set ⇒ pgxpool → `persist.NewPostgres` → `Bootstrap.Run` before live ingest, `go snapshotter.Run(ctx)` + `App.Checkpoint = snapshotter.Checkpoint`; unset ⇒ in-memory only. New config `RISK_ENGINE_DATABASE_URL` + `RISK_ENGINE_KAFKA_BROKERS` (comma list; empty ⇒ skip replay) · `kanz/services/risk-engine/internal/app/bootstrap.go`
- [x] **PERS-01c** Periodic durable snapshotter — new `kanz/internal/risk/engine/snapshot.go` (`Snapshotter`), plus `state.Store.SnapshotWithKeys` + `dedupWindow.Keys()`. **`Snapshotter` materializes every known portfolio's committed state into the PERS-01b StateStore on a cadence** (`DefaultSnapshotInterval=30s`) + a final pass on shutdown — each saved `PortfolioRecord` is the durable analog of an EVT-11 PortfolioSnapshot (aggregate + positions + LogPosition + applied-key tail). **"Emission" means persisting to the durable store, NOT publishing a `domain.v1.PortfolioSnapshot` to the bus** — the Postgres StateStore IS the snapshot store the 01d bootstrap `LoadAll`s; RISK-10's `publish.Publisher` only emits exposure/measures FACTs. **Periodic, not per-apply, is the load-bearing trade**: per-apply would put the SQL write on the hot ingest path; the authoritative copy is in-memory, so the durable copy only needs to bound restart replay. **`LogPositionSource` is a deliberate seam, nil for now** — ORCH-01 ingests from NATS where the durable Kafka offset isn't on the envelope, so the engine has no coordinate to stamp; 01d/a future task wires it over the Kafka consumer's committed offsets. nil ⇒ fall back to the portfolio's own LogPosition (usually nil during live NATS ingest ⇒ record carries no resume hint ⇒ bootstrap replays from log start, still correct). **`SnapshotWithKeys` captures the clone AND the applied idempotency keys under ONE per-aggregate lock** (new `dedupWindow.Keys()` reads the bounded tail), closing the 01a/01b-flagged gap where an apply could interleave to record a key whose state effect is missing from the clone — that pairing is what makes the persisted AppliedKeys a sound pre-seed for the bootstrap dedup window. Checkpoint is fail-soft per-portfolio (`errors.Join`); `Run` logs per-tick errors but never dies on a transient store blip · `kanz/internal/risk/engine/snapshot.go`
- [x] **PERS-01b** Postgres `StateStore` impl — new `kanz/internal/risk/state/persist/postgres.go` over the EVT-16a pgxpool pattern (caller owns the pool). **Per-aggregate row locking falls out of the upsert, not an explicit `SELECT … FOR UPDATE`**: `INSERT INTO portfolios … ON CONFLICT (portfolio_id) DO UPDATE` takes a row-level lock on that portfolio's row for the tx, so two concurrent Saves for the same portfolio serialize (the second blocks at the conflicting upsert) — the durable mirror of RISK-05's in-memory per-portfolio mutex; different portfolios never contend. **Read-committed (pool default) is deliberate**, NOT the registry's Serializable: the row lock already gives per-aggregate ordering and there's no cross-aggregate invariant to protect. **Save is full-replace within one tx**: upsert the portfolio row, then `DELETE … WHERE portfolio_id` + re-insert positions and applied_keys — matches snapshot hard-reset (a position dropped from the record is removed) and keeps the durable applied-key set the same bounded-tail shape as the in-memory dedup window. **Money/Decimal ↔ BYTEA via `proto.Marshal`** with a `reflect.IsNil` guard in `encode` (the typed-nil-in-interface trap: a nil `*commonpb.Money` passed as `proto.Message` is a non-nil interface over a nil pointer); nil ⇒ SQL NULL. **LogPosition decomposed into `log_{topic,partition,offset}` columns** (topic presence = set/unset discriminator). **Zero `time.Time` ↔ SQL NULL** (`nullTime`/`derefTime`) so "no state applied yet" round-trips as NULL, not the proto Unix epoch. **LoadAll is 3 table scans grouped in memory** rather than N+1 — it's the bootstrap read (PERS-01d). No Memory backend shipped (unlike the registry) — 01e's tests need a real Postgres anyway · `kanz/internal/risk/state/persist/postgres.go`
- [x] **PERS-01a** Define `StateStore` persistence interface + Postgres migration — new `kanz/internal/risk/state/persist/store.go` + `kanz/services/risk-engine/migrations/0001_state.sql`, **begins the durable-state epic**. **`StateStore` is a snapshot store, not an event store**: Save/Load/LoadAll/Ping over a flat `PortfolioRecord` (the third shape after proto-wire and domain-working, same two-layer reasoning as the domain package); `FromPortfolio`/`(PortfolioRecord).ToPortfolio` are the single canonical mapping so 01b/c/d never re-derive it divergently. PortfolioRecord reuses `domain.Position` directly (already a flat value struct) rather than a parallel position type. **RPO=0 hinges on atomic Save**: aggregate fields + full position set + LogPosition + AppliedKeys persist in one per-aggregate transaction, so the snapshot's LogPosition can never claim events it didn't include — that's what makes the offset+1 resume gap-free. **Idempotency is persisted as `AppliedKeys` data on the record, not checked per-event against the DB**: bootstrap (01d) pre-seeds the in-memory dedup window from them so a replayed boundary event is skipped by the *existing* in-memory machinery — the hot apply path never touches the DB. Needed because a portfolio is fed from multiple topic-partitions, so one scalar LogPosition is not a perfect watermark; the key tail covers the boundary. **Storage**: Money/Decimal columns are marshaled `common.v1` proto BYTEA (schema-registry opaque-descriptor pattern; `double` banned for money/sizes ⇒ no lossless native SQL column); LogPosition decomposed into text/bigint columns. Migration: `portfolios` + `positions` (FK CASCADE) + `applied_keys` (FK CASCADE, `(portfolio_id, applied_at)` index for the writer's age-based prune) · `kanz/internal/risk/state/persist/store.go`, `kanz/services/risk-engine/migrations/0001_state.sql`

- [x] **ORCH-01f** End-to-end harness over an in-memory `bus.Client` — new `kanz/services/risk-engine/internal/app/app_test.go`, **closes the ORCH-01 runtime epic**. Introduces `memBus`: a looping in-memory `bus.Client` that routes every `Publish` to the handlers `Subscribe`d on that subject, with async per-subscription buffered delivery so the engine's own re-entrant publish (apply→recompute→publish) can't deadlock the publisher — the first shared in-memory broker, the seam a future `pkg/bustest` could promote. Proves the **whole ORCH-01b/c/d/e composition** against one broker (a regression in any seam surfaces here, not just in the isolated unit tests). 3 tests: (1) **publish→output FACTs** — a published PortfolioState+PositionState pair flows through ingest→TriggeringApplier→Store+recompute→publish and emits both `exposure_recomputed` and `measures_computed`, with the decoded `RiskMeasureSet` carrying `GrossExposure==1000` (one USD position of 1000) — end-to-end measure correctness, not just event presence; (2) **recompute p99 latency budget** — 100 portfolios published, per-portfolio publish→emit latency matched via the output envelope's `partition_key`, p99 asserted < 500ms (generous in-memory bound that still catches an O(n) recompute regression); (3) **degraded-on-stale query** — 10m-stale state ingested through the bus, then `EngineImpl.Exposure` (ORCH-01b) over the same store flags `DEGRADED`, tying the publish-path ingestion to the query-path degraded composition. **`memBus` retention gotcha handled**: it only delivers to subscribers present at publish time, so a `waitSubscribed` gate blocks until the engine's async `Subscribe` goroutines have registered before any publish (otherwise the startup race drops the events) · `kanz/services/risk-engine/internal/app/app_test.go`

- [x] **ORCH-01e** Graceful shutdown lifecycle — new `kanz/services/risk-engine/internal/app/lifecycle.go` (`App`), plus a `Recomputer.Drain()` and a `main.go` rewrite that wires the full ORCH-01b/c/d pipeline behind the ORCH-01a readiness gate + this lifecycle. **`App.Run(ctx)` runs the ordered drain** on a shutdown signal (or ingestion failure): (1) **readiness off first** so k8s/the LB pulls the pod from rotation before teardown — in-flight work finishes, no NEW traffic arrives; (2) **stop consumers** (the signal cancels the ingest ctx; `bus.Consumer` dispatches one delivery at a time synchronously, so a returning `Subscribe` means the in-flight apply already completed — that IS the apply-drain); (3) **drain recomputes** via the new `Recomputer.Drain()` so the LAST settled state publishes instead of being dropped; (4) **checkpoint** (PERS-01 seam, `func(ctx) error`, nil ⇒ skipped); (5) **flush + close transports last** so the publisher stays usable through step 3 and Kafka batches flush on close. The whole sequence runs under a watchdog (`DefaultShutdownTimeout=20s`) so a wedged broker can't hold the pod past its terminationGracePeriod. **`Recomputer` refactored from AfterFunc-per-portfolio to a single-worker debounce loop** (dirty map of id→deadline; a worker scans deadlines, dispatches due recomputes on their own goroutines for cross-portfolio parallelism, sleeps until the nearest deadline or a nudge) — the AfterFunc model could not drain race-free because Go can't distinguish a stopped timer from one whose func already started; the worker model makes `Drain` (flush pending, exact) and `Close` (drop pending, hard-stop) both clean, each blocking on a `done` channel + an in-flight `WaitGroup`. **The recomputer's `baseCtx` is app-scoped (Background), NOT the signal ctx** — load-bearing so `Drain` publishes after the signal cancels ingestion. **`main.go` gates ingestion on `RISK_ENGINE_NATS_URL`**: set ⇒ `DialNATS` → producer/publisher + store + recomputer + `TriggeringApplier` + consumer + `App.Run`; unset ⇒ HTTP-only scaffold (probes up, no broker) so the binary still boots locally. Config gained `NATSURL` + `Source`. Tests: a focused `Recompute_DrainFlushesPending` (10s debounce that would never fire on its own; Drain flushes + returns promptly + is idempotent + Trigger-after-Drain no-ops) and an `App_FullChainThenGracefulShutdown` integration test driving bus→Ingestor→TriggeringApplier→Store+recompute→publish then a signal-driven shutdown asserting readiness-off + checkpoint-called + transport-closed-once + an exposure FACT emitted by the drain · `kanz/services/risk-engine/internal/app/lifecycle.go`

- [x] **ORCH-01d** Recompute trigger policy — new `kanz/internal/risk/engine/recompute.go` (`Recomputer` + `TriggeringApplier`), plus the locked-read primitive the ORCH-01b note deferred to "the recompute trigger": `state.Store.Snapshot(id)` (clones the portfolio under the per-aggregate lock — a race-free point-in-time view) backed by a new `domain.Portfolio.Clone()`. **`Recomputer.Trigger(id)` debounces per portfolio** (trailing-edge, `DefaultDebounceInterval=250ms`, tunable): repeated triggers within the window collapse to one recompute fired after the burst settles — a portfolio's revalue + N position changes recompute once against the settled state instead of publishing N immediately-superseded FACTs. On fire it `Snapshot`s → `compute.ComputeExposure`/`ComputeMeasures` → `Cache.Store{Exposure,Measures}` → RISK-10 `Publisher.EmitExposure`/`EmitMeasures`. **Concurrency**: different portfolios recompute in parallel, a portfolio's own recomputes never overlap, and reads are `Snapshot` so a recompute never races a concurrent apply. (Originally one `time.AfterFunc` timer per portfolio; ORCH-01e refactored this to a single-worker debounce loop so graceful `Drain` could flush race-free — the AfterFunc model couldn't.) **`baseCtx` scopes the publish + timer lifecycle** — `Close()` stops all pending timers and blocks further triggers (ORCH-01e drain), and an in-flight recompute past the timer still updates the cache but skips the emit when `baseCtx` is canceled (publish failures are logged, not retried — the `bus.Producer` owns delivery durability and the next apply re-triggers anyway). **`publisher` may be nil** (cache-only mode for tests / read-replicas). **`TriggeringApplier` is the seam connecting ORCH-01c ingestion to recompute**: an `ingest.Applier` decorator that delegates to the inner applier (the `state.Store`) and, only on success, fires `Trigger(portfolioID)` — so `app.NewIngest(consumer, NewTriggeringApplier(store, recomputer), …)` makes applies flow to state AND fan out a debounced recompute, with a compile-time `var _ ingest.Applier` assertion. **Also retrofitted EngineImpl (ORCH-01b) to read via `Store.Snapshot`** instead of `Lookup` (Exposure/Measures/Scenario/Health) — removes the documented data race in the query path now that the locked-read boundary exists, which `go test -race` (CICD-01a) would otherwise catch. 6 tests: burst-coalesces-to-one-emit (+ cache populated for both exposure & measures), cache-only-when-no-publisher, TriggeringApplier-fires-recompute-after-apply, Close-stops-pending-timer + Trigger-after-Close-no-ops, empty-portfolio-id-ignored · `kanz/internal/risk/engine/recompute.go`

- [x] **ORCH-01c** Wire RISK-04 `Ingestor` → `state.Store` via `bus.Consumer` — new `kanz/services/risk-engine/internal/app/` package (`ingest.go`), the service's composition root (risk *logic* stays in `internal/risk/*`; this package owns process concerns — subscription fan-out, lifecycle, later bootstrap + recompute). **`Ingest` wraps a `bus.Consumer` + the RISK-04 `ingest.Ingestor`** (built over the `state.Store` as `Applier`) and subscribes the three risk state subjects (`risk.portfolio.revalued` / `risk.position.changed` / `risk.portfolio.snapshot` — subject == event_type, so the Ingestor's `env.EventType` dispatch matches the subscription scope). **Per-aggregate ordering is upheld downstream, not here**: the three subjects are subscribed on independent goroutines (each `Consumer.Subscribe` blocks), so a portfolio's PortfolioState and PositionStates arrive concurrently with no cross-subject bus ordering — but `state.Store`'s per-portfolio mutex (RISK-05) serializes every apply for one portfolio regardless of delivery subject, so this wiring needs no coordination of its own (documented as the load-bearing reason concurrent subscribes are safe). **`Run(ctx)` is fail-fast**: one goroutine per subject under a derived cancelable ctx; the first non-`context.Canceled` subscribe error fires a `sync.Once` that records it and cancels the siblings, so a single broken subscription brings the ingester down rather than running silently degraded; clean ctx cancellation (graceful shutdown, ORCH-01e) returns nil. **Consumer is injected** (caller picks transport + retry/DLQ policy) so `Ingest` stays transport-agnostic and testable. **One durable consumer group** (`DefaultConsumerGroup="risk-engine"`) across all three subjects — each (subject, group) is its own durable consumer, so N replicas spread partitions within the group. 5 tests (external `app_test`, real `bus.Consumer` over a `fakeSub` that replays `bus.Producer`-framed wire bytes then blocks on ctx — exercises the true Unframe→Validate→dispatch path): events route into the store (portfolio base-currency + position landed, market value intact), nil-consumer + nil-applier rejection, clean-shutdown-returns-nil, and subscribe-failure-surfaces-and-cancels-siblings (asserts Run returns rather than hanging) · `kanz/services/risk-engine/internal/app/ingest.go`

- [x] **ORCH-01b** Implement concrete `EngineImpl` behind `risk/api/v1.Engine` — new `kanz/internal/risk/engine/` package (`engine.go`), the production form of RISK-13's `computeExposureWithFlags`. **Composes the four RISK-04..11 units**: `state.Store` (reader-side `Lookup`/`IDs`) + `compute` (pure `ComputeExposure`/`ComputeMeasures`) + `risk.Cache` (last-known-good fallback) + `risk.Detector` (staleness → `Mode`/`QualityFlags`), satisfying all four `v1.Engine` methods with a compile-time `var _ v1.Engine` assertion. **Canonical query pattern lifted into production**: try live (`Lookup` → compute → `Cache.Store` on success), fall back to the cached value on a store miss, tag via `Detector.Assess(result.AsOf())`; a true miss (neither store nor cache) → `ErrPortfolioNotFound`, empty `PortfolioID` → `ErrInvalidRequest`. The RISK-13 integration tests still pass unchanged and can now be rewritten against these methods (separate cleanup). **Three composition decisions worth keeping**: (1) **Measures always computes + caches the FULL set** (nil filter) then narrows to `req.Measures` via `filterMeasures` — caching a filtered set would let a later cache hit under a different filter serve a wrongly-narrowed result; unknown filter names are dropped (matches `ComputeMeasures`/`Lookup` miss semantics). (2) **`EvaluateScenario` has NO cache fallback** — the cache holds derived `ExposureSet`/`MeasureSet` values, not a `Portfolio` to shock, so a scenario needs live state; no portfolio ⇒ `ErrPortfolioNotFound`. Its flags are assessed against the *underlying* state's `AsOf` (a scenario on stale state is itself stale). (3) **The query path does not lock** — `Lookup` returns the live pointer and `compute` reads it through the copy-returning domain accessors; serializing a recompute against a concurrent apply is owned by the ORCH-01d recompute trigger (per-aggregate boundary), not the read path. `Health.AsOf` = max `AsOf` across all portfolios (a single live portfolio keeps the engine NORMAL); every method short-circuits on `ctx.Err()`. 13 tests (external `engine_test`, real `Detector` with now-relative `AsOf` since its clock is unexported): fresh/no-flags, stale/degraded, unknown→not-found, empty-id→invalid, repeat-query-stable, measures filter-narrows + empty-returns-all, scenario shock-projects-and-leaves-live-state-unmutated, scenario unknown→not-found, health latest-across-portfolios + no-state-degraded, cancelled-ctx · `kanz/internal/risk/engine/engine.go`

- [x] **ORCH-01a** Scaffold `risk-engine` service — new `kanz/services/risk-engine/` (`cmd/risk-engine/main.go` + `internal/config/` + `internal/server/`), the first tenant of the Platform Hardening roadmap and the second Go service after schema-registry. **Follows the schema-registry service skeleton exactly** (EVT-16a layout): `package main` entrypoint under `cmd/<name>/`, per-service `internal/` packages, stdlib `net/http` + `log/slog` JSON handler, `signal.NotifyContext` SIGINT/SIGTERM + 15s graceful `Shutdown`, env-sourced `config.Load` with per-field `envOr`/`parseLevel` fallback (prefix `RISK_ENGINE_`, default listen `:8081` so it doesn't collide with schema-registry's `:8080`). **Config is deliberately minimal** — only `Listen` + `LogLevel`; state/bus/recompute knobs (ORCH-01c/01d) and `DatabaseURL` (PERS-01) are added as those layers land, so `Load` returns no error today (no required fields yet). **The one design decision worth keeping — the readiness gate**: liveness (`/healthz`) and readiness (`/readyz`) are split per their k8s-probe meanings. schema-registry's `/readyz` pings storage; risk-engine has no storage yet, so readiness is an injectable `server.Readiness` (an `atomic.Bool` gate, starts NOT ready) rather than a dependency check. main flips it ready once the listener is up and clears it on shutdown. This is the seam ORCH-01c/01d move behind real signals (consumers subscribed + PERS-01 bootstrap-replay caught up) and ORCH-01e uses to stop a draining pod from receiving work — same lands-before-impl discipline as RISK-01/PRED-11. 2 tests: healthz-always-ok regardless of gate state, and readyz reflecting the gate across closed→ready→draining (503/200/503) · `kanz/services/risk-engine/`

### Data Integrity Layer

- [x] **DATA-11** Tests: late-data path + reconciliation correctness — new `kanz/internal/integrity/latereconcile_test.go`, 6 scenario tests; **closes the Data Integrity epic (DATA-01..11)**. Ties together the DATA-03 watermark/late primitive and the DATA-05 reconciler over simulated out-of-order + divergent streams (seeded rand, reproducible), the composition angle vs the per-call unit tests. **Late-data path (2)**: after 61 in-order events advance the frontier (watermark base+55s), a burst of 10 stragglers at event_time base+10s are all flagged Late, `MarkLate`'d, and observable via `IsLate` (the detect→route→payload-blind-tooling-sees-it path, DATA-06), and a subsequent base+61s event is still OnTime — proving the stragglers never rewound the frontier (the load-bearing DATA-03 invariant) end-to-end; a jittery feed (baseline +10s/step, ±3s jitter < 5s allowedLateness) raises zero late flags, proving normal intra-partition reordering within grace is never late. **Reconciliation correctness (3)**: a complete run with NATS-leads-then-Kafka-shuffled matches all 100 with zero discrepancies after a far-future sweep; a run where Kafka (log of record) silently drops ~10% surfaces exactly those keys as discrepancies after the deadline, each `SeenOn=nats/MissingOn=kafka`; the healthy case where the durable log lags NATS by 10s (< 30s deadline) raises NO false discrepancy on a mid-lag sweep and matches all 50 once Kafka catches up with `MatchLatency=10s` (the load-bearing "one-sided is not a discrepancy until aged" property under realistic skew). **Interplay (1)**: a LATE-flagged event still reconciles across transports (the reconciler keys on idempotency_key and is flag-blind — lateness and cross-transport completeness are orthogonal) and reconciliation never strips the LATE flag · `kanz/internal/integrity/latereconcile_test.go`

- [x] **DATA-10** Tests: gap/staleness/drift detection under simulated bad data — new `kanz/internal/integrity/baddata_test.go`, 7 scenario tests. **Distinct from the per-detector unit tests** (gap_test/staleness_test/drift_test, one classification call each): these drive a detector over a *realistic event sequence with faults injected deterministically* (seeded `math/rand`, reproducible) and assert the layer surfaces exactly the injected faults — the composition angle, RISK-12's seeded-loop approach applied to the integrity detectors. **Gap (3 tests)**: across 5 seeds, drop ~15% of sequences 2..200 (keep seq 1 so FirstSeen baselines at 1 and every later miss is a true gap) and assert `Σ MissingCount == len(dropped)` exactly — every dropped sequence accounted for, none double-counted; a clean 1..500 stream raises zero gaps; two sources writing a clean 1..N on the SAME partition_key produce no phantom gaps (the (source,event_type,partition_key) keying decision from DATA-01, exercised end-to-end rather than asserted per-call). **Staleness (2 tests)**: a feed whose ingestion lag grows 0→119s drives exactly 11 Fresh / 50 Stale / 59 Critical, pinning the exclusive-above boundaries (lag≤10 fresh, 10<lag≤60 stale, lag>60 critical) over a real sweep; a flood of out-of-order older events after the frontier advanced never rewinds the reported `LastEventTime`. **Drift (2 tests)**: a window sampled ~uniformly across the 4 bins (matching the uniform baseline) scores below threshold (not drifted), then after `Reset` a window collapsed entirely into the top bin drifts with a strictly-higher score — quiet-then-alarm over the realistic "feed starts returning a stuck/scaled value" failure; and a strongly-shifted but 20-sample window is withheld (not Drifted, not Sufficient) against MinSamples=100, proving the noise gate holds under bad-but-sparse data · `kanz/internal/integrity/baddata_test.go`

- [x] **DATA-09** Configure alerting on data-quality event thresholds — new `kanz/infra/observability/alerts/` (`data-quality.rules.yaml` + README), Prometheus alerting rules over the same `kanz_data_*` metric contract the DATA-08 dashboards render. **10 rules in 4 groups**: freshness (`DataStalenessWarning` lag>10s/2m, `DataStalenessCritical` >60s/1m), completeness (`DataSequenceGap` any-missing-in-5m, `DataReconcileDiscrepancy` any-in-10m, `DataReconcileBacklogGrowing` pending>1000/5m, `DataReconcileMatchLatencyHigh` p99>30s/5m), drift (`DataInputDriftWarning` score>threshold/10m, `DataInputDriftCritical` >2×threshold/5m), and quality-events (`DataQualityCriticalEvent` catch-all over the DATA-07 emission stream + `DataQualityMetricsMissing` exporter-liveness via `absent()`). **Thresholds mirror the detectors' own defaults** so a firing alert means "the detector would flag this": 10s/60s = `DefaultStalenessConfig`, 0.25 = `DefaultPSIThreshold`, 30s = `DefaultMatchDeadline` — no magic numbers invented. **Drift rules compare against the exported `kanz_data_drift_threshold` via `> on (feature, metric)`** so the alarm point tracks each detector's configured threshold automatically — the threshold is never duplicated between detector and rule (the one decision worth keeping: gaps/discrepancies have NO detector threshold to mirror because any loss/divergence is intrinsically critical, so those fire on `> 0`). `severity` (warning⇒ticket, critical⇒page) + `layer="data-integrity"` labels drive Alertmanager routing (README shows the route block + `promtool check rules` + the rule_files wiring). Same exporter dependency as DATA-08 — the rules target the metric surface a future exporter (wrapping the detectors or consuming the DataQualityEvent stream) must expose · `kanz/infra/observability/alerts/`

- [x] **DATA-08** Build data-observability dashboards (freshness, completeness, drift) — new `kanz/infra/observability/dashboards/` (first tenant of `kanz/infra/observability/`): three Grafana dashboard JSON models + a README. **Freshness** (`freshness.json`, DATA-02): max/streams-stale stats + staleness-lag and stale-frontier-age timeseries by subject/partition. **Completeness** (`completeness.json`, DATA-01 + DATA-05): missing-events + reconcile-discrepancies + pending stats, gap-rate and discrepancy (seen-vs-missing) bar timeseries, NATS↔Kafka match-latency p50/p99 (histogram_quantile), pending-by-transport. **Drift** (`drift.json`, DATA-04): features-over-threshold + max-score + drift-events stats, per-feature score timeseries, and a current-score-vs-threshold table. All three query a Prometheus datasource via a `datasource` template var + a free-text `subject`/`feature` regex filter; schemaVersion 39. **The README documents the metric contract** — `kanz_data_staleness_lag_seconds`, `kanz_data_last_event_age_seconds`, `kanz_data_gap_missing_total`, `kanz_data_reconcile_pending`, `kanz_data_reconcile_discrepancies_total`, `kanz_data_reconcile_match_latency_seconds` (histogram), `kanz_data_drift_score`, `kanz_data_drift_threshold`, `kanz_data_quality_events_total{kind,severity,subject}` — with label values reusing the detector vocabulary (subject = upstream stream; kind ∈ gap/staleness/drift; transport/seen_on/missing_on ∈ nats/kafka). **Detection-lands-before-instrumentation**: the detectors are pure in-memory classifiers and DATA-07 emits events (not Prometheus metrics) today, so the dashboards (and DATA-09 alerts) define the stable metric surface a thin exporter — wrapping the detectors or consuming the DataQualityEvent stream — must expose; wiring that exporter is a separate task, the same discipline as RISK-01's interface-before-impl. Provisioned via a Grafana file-provider (README shows the provider YAML) or hand-imported. The `data.market_stream`/`data.feature` Kafka topics carrying DATA-07's events already exist (EVT-09) · `kanz/infra/observability/dashboards/`

- [x] **DATA-07** Emit data-quality events (gap, staleness, drift) to the bus — new `kanz/internal/integrity/publish.go`. **`Publisher` is the emit half of the layer's detect-then-emit split** (the counterpart to the DATA-01/02/04 detectors and the DATA-06 flag emitter): `EmitGap`/`EmitStaleness`/`EmitDrift` translate a detector result into an `observation.v1.DataQualityEvent` (EVT-14's purpose-built schema) and publish via a shared `bus.Producer` (RISK-10 pattern — producer owned by the orchestrator so producer_sequence stays monotonic; DATA-07 owns translation only, not WHEN-to-emit). **Event-type = `data.{entity}.{kind}_detected`** (subject-taxonomy §1: `data.market_stream.gap_detected`, `data.feature.drift_detected`); the `{entity}` segment classifies the monitored stream and is **orthogonal to the kind** (staleness can hit a market_stream or a feature), so it's a per-emit caller parameter, not derived. **Crucial subject distinction**: the `DataQualityEvent.subject` FIELD names the upstream stream the problem was found on (e.g. `market.equity.trade`), NOT the `data.*` subject the report itself flows on. **OBSERVATION class, FACT-grade**: envelope `EVENT_CLASS_OBSERVATION` (so the producer auto-stamps idempotency_key = event_id); the must-not-shed/durable-retention property is a bus-topology policy, not the envelope class. **Result→detail mapping**: gap → `GapDetail{producer_source=Key.Source, expected=MissingFrom, received=Sequence, missing_count=MissingCount()}` with envelope partition_key = affected partition; staleness → `StalenessDetail{observed_lag, threshold, last_event_time}`; drift → `DriftDetail{feature, metric, score, threshold, window}` with **empty partition_key** (window-wide problem, so the auto-stamped producer_sequence is 0 and Validate's empty-pk⇒seq-0 rule holds) and subject = the feature name. **Each Emit guards its precondition** (StatusGap / Stale() / Drifted) and rejects with an error rather than re-detecting — the Publisher reports detected problems, it does not classify. **Severity is an explicit per-emit parameter** (rejected if UNSPECIFIED) because triage level is deployment/alerting policy (DATA-09), not a detector output. **Injectable clock** (`NewPublisherWithClock`) stamps event_time = detection time, deterministic in tests (DATA-05 convention). 9 tests cover nil-producer rejection, all three happy paths (envelope passes `bus.Validate` + correct event_type/domain/class + detail round-trip + clock-stamped event_time + the gap/drift partition_key contracts), the three precondition rejections (non-gap status, fresh staleness, not-drifted), and empty-entity + unspecified-severity rejection (nothing published) · `kanz/internal/integrity/publish.go`

- [x] **DATA-06** Wire `quality_flags` emission across ingestion + producers — new `kanz/internal/integrity/flags.go`. **`Mark(env, flag) bool` + `Has(env, flag) bool`** are the consolidated flag-stamping primitive: idempotent, nil-safe, order-preserving (the package convention previously hand-rolled in DATA-03's `MarkLate` and `replay.StampReplayed`). This is the **producer/ingestion-side half of the detect-then-emit split** the whole layer is built on — the DATA-01..05 detectors classify a condition, the producer/ingestor that owns the envelope stamps the matching flag here, so payload-blind tooling (DATA-07 reporting, observability, any consumer) reads the condition off `quality_flags` without re-running detection or parsing the payload. **Two flags are deliberately not stampable**: `QUALITY_FLAG_REPLAYED` (owned exclusively by replay tooling and forbidden on the live path by `bus.Validate` — stamping it from producers would let a live event masquerade as replayed and corrupt the live/replay boundary) and `QUALITY_FLAG_UNSPECIFIED` (the zero value); both are no-op-returning-false rather than errors so a misrouted stamp never aborts an otherwise-valid publish. **Five intent-revealing wrappers** for the producer/ingestion-settable conditions — `MarkSynthetic`/`MarkBackfilled`/`MarkDegraded`/`MarkRevised`/`MarkShed` — each a thin call to `Mark` so call sites read as the condition and a rename surfaces as a compile error (same alert-stability discipline as PRED-04's named degraded-reason constants). **DATA-03's `MarkLate`/`IsLate` refactored to delegate** to `Mark`/`Has` (lateness is still detected in watermark.go, but the stamp mechanism is now centralized). **Wiring choice — flags live in the integrity layer, not baked into `bus.Producer`**: the generic transport library must not depend on the integrity layer (a layering inversion), so ingestion/producer-orchestration code calls these helpers before publish; the concrete ingestion services are still stubs, so DATA-06 ships the canonical emission API they will call. 7 tests cover add-once/idempotent, order-preserving flag preservation, REPLAYED + UNSPECIFIED rejection, nil-safety, all five named wrappers, and MarkLate-still-works-after-delegation · `kanz/internal/integrity/flags.go`

- [x] **DATA-05** Implement NATS-vs-Kafka reconciliation for correctness-sensitive consumers — new `kanz/internal/integrity/reconcile.go`. **`Reconciler.Observe(transport, env) → ReconcileResult`** matches the same logical event delivered over the NATS live spine (EVT-08, hot path) against the Kafka log of record (EVT-09), so a correctness-sensitive consumer reading the fast path can be told when the durable log diverged. **Identity = `idempotency_key`, not `event_id`** — load-bearing: it's the one identifier guaranteed byte-identical across both publishes of a logical event and is already the broker dedup key (EVT-17d: rides `Nats-Msg-Id` on NATS, a user header on Kafka; == `event_id` for FACTs). **Four statuses**: `NotApplicable` (nil env / empty idempotency_key / `TransportUnspecified` — touches no state), `Pending` (first sight on this transport; awaiting the other — explicitly NOT a discrepancy yet, the two paths have independent latencies and the spine normally leads the log), `Matched` (the counterpart was already pending → confirmed on both; clears the entry, reports `FirstTransport` + `MatchLatency` = wall-clock gap between sightings, a not-lost-but-badly-lagging signal), `Duplicate` (same-side redelivery before the counterpart — keeps the original `firstSeen` so the deadline still measures from true first sight, doesn't add a second entry). **`Sweep() → []Discrepancy`** returns every pending event aged past `MatchDeadline` (default 30s, must exceed normal NATS→Kafka skew so a healthy lag never false-positives) carrying `SeenOn`/`MissingOn`/`Age`, and removes it — **report-once** (mirrors the gap detector advancing its mark); a later straggler on the missing side re-enters as Pending. **Boundary is above the deadline** (age==deadline is not yet a discrepancy — same off-by-one discipline as RISK-11 / staleness). **Match-and-forget**: on a match the key is dropped (can't remember forever), so the reconciler must be fed the POST-dedup stream on each side (each key ~once per transport); documented. **Injectable clock** via `NewReconcilerWithClock` (codebase convention, cf. PRED-07's `NewSyncClientWithStub`) so the deadline timing is testable from the external `integrity_test` package. Detection-only (DATA-06 wires flags, DATA-07 emits the DataQualityEvent), in-memory, per-process, concurrent-safe, re-baselines on restart — the fifth integrity primitive (gap/staleness/watermark/drift/reconcile). 11 tests cover NATS-first + Kafka-first match, MatchLatency-from-first-sighting, same-side duplicate (no second entry) + duplicate-keeps-original-deadline, unmatched-ages-into-discrepancy + boundary-at-deadline, report-once, matched-never-sweeps, independent-keys-isolation, three not-applicable inputs, and non-positive-deadline fallback · `kanz/internal/integrity/reconcile.go`

- [x] **DATA-04** Implement input distribution drift detection — new `kanz/internal/integrity/drift.go`. **`PSIDetector`** (one per feature) bins observed feature values over the current window and scores them against a fixed baseline distribution via **PSI (Population Stability Index)** — the canonical input-drift metric. `Observe(value)` accumulates into bins (via `sort.SearchFloat64s` over the configured edges → len(edges)+1 bins); `Assess(windowStart, windowEnd) → DriftResult` computes `PSI = Σ (actual_i − expected_i)·ln(actual_i/expected_i)` and is non-destructive; `Reset()` rolls the window. **DriftResult maps onto `observation.v1.DriftDetail`** (DATA-07 emit target): feature/metric/score/threshold/window_start/window_end. **`DriftMetric` is a string label + the result shape is metric-agnostic** so KL-divergence / KS-statistic detectors can plug in later carrying their own label without changing the result contract. **Three correctness decisions**: (1) **epsilon flooring** (`1e-6`) on both actual and expected bin proportions so an empty/collapsed bin can't produce `ln(0)`/NaN/Inf — a fully-collapsed distribution scores high-but-finite and drifts; (2) **`MinSamples` sufficiency gate** — PSI on a handful of samples is noise, so a window below MinSamples is never `Drifted` regardless of score (`Sufficient` reported separately, `SampleCount` exposed so the caller/DATA-07 can gate); (3) **`Drifted = Sufficient && Score > Threshold`** with `DefaultPSIThreshold=0.25` (the conventional "significant shift" cutoff: <0.1 negligible, 0.1–0.25 moderate, >0.25 alarm). **Baseline normalized to proportions in the constructor** (caller passes counts or proportions); validation rejects feature-empty, baseline/edges length mismatch (`len(Baseline)==len(Edges)+1`), <2 bins, non-ascending edges, negative/zero-mass baseline. Caller owns window timing (the detector tracks samples, not wall clock); concurrent-safe, in-memory per-process — the fourth detector in the package (gap/staleness/watermark/drift), detect-then-emit split preserved. 11 tests cover constructor validation matrix, default threshold, identical-to-baseline ~0 PSI, a hand-computed known PSI (60/40 vs 50/50 ≈ 0.0405), significant-shift drift, empty-bin no-NaN, insufficient-samples-never-drifts, zero-samples, Reset, window/metadata propagation, and multi-bin placement · `kanz/internal/integrity/drift.go`

- [x] **DATA-03** Implement watermarking + late-data routing (`LATE` flag) — new `kanz/internal/integrity/watermark.go`. **`WatermarkTracker.Observe(env) → WatermarkResult`** maintains a per-stream event-time watermark = `(max event_time seen) - AllowedLateness` and classifies each event. **Four statuses**: `Unknown` (nil env / missing event_time), `FirstSeen` (baseline — seeds the watermark, can't be late with no prior watermark), `OnTime` (event_time ≥ watermark — advances the frontier if newer), `Late` (event_time < watermark — arrived after the consumer provably moved past its event-time window; `QUALITY_FLAG_LATE`). **`AllowedLateness` is the bounded-out-of-orderness grace**: an event up to that far behind the frontier is still on-time (normal intra-partition reordering); older than that is late. **Late events never pull the watermark backward** (the load-bearing invariant — otherwise one straggler would re-open the late window for everything after it) and are flagged-not-dropped (the layer never black-holes). Boundary is inclusive at the watermark (event_time == watermark is OnTime, not late). **Distinct from DATA-02**: staleness = a single event's ingestion lag (`ingestion_time - event_time`, source freshness); lateness = an event's position vs the stream's *event-time progress* (ordering/window-completeness). Same `frontierKey` (event_type, partition_key) keying as DATA-02. **`MarkLate(env)` / `IsLate(env)` flag helpers** mirror EVT-20c's `StampReplayed`/`IsReplayed` — idempotent, nil-safe, preserve existing flags; this is the concrete "route late data" primitive DATA-06 wires into producers so payload-blind tooling (DATA-05 reconciliation, DATA-07 reporting) sees LATE without re-running watermark logic. Detection-only, concurrent-safe, in-memory per-process (re-baselines on restart). 11 tests cover first-seen seeding, advancing on-time, within-grace + at-watermark-boundary on-time, late + Lateness math + Watermark value, late-doesn't-move-frontier-backward, per-subject/partition isolation, non-positive-lateness default fallback, nil/missing-event_time unknown, and MarkLate idempotency + flag-preservation + nil-safety · `kanz/internal/integrity/watermark.go`

- [x] **DATA-02** Implement staleness monitoring (`event_time` vs `ingestion_time`) — new `kanz/internal/integrity/staleness.go`. **`StalenessMonitor.Observe(env) → StalenessResult`** computes per-event ingestion lag = `ingestion_time - event_time` (the source-to-edge latency signal per envelope.proto §event_time) and classifies it. **Five levels**: `Unknown` (nil env or missing event_time/ingestion_time — both Required, but a defensive monitor reports unknown rather than fabricating a lag from an epoch-zero `AsTime()` on a nil Timestamp), `Fresh` (lag ≤ WarnAfter), `Stale` (WarnAfter < lag ≤ CriticalAfter), `Critical` (lag > CriticalAfter), and `Future` (event_time ahead of ingestion_time beyond SkewTolerance — clock skew / bad source timestamp; the *opposite* of staleness, classified distinctly rather than silently read as fresh). **Boundaries exclusive-above** (lag==threshold is the lower level — RISK-11's "above the budget" discipline). **Result maps directly onto `observation.v1.StalenessDetail`** (DATA-07 emit target): `Lag`→observed_lag, `Threshold`→threshold (the *exceeded* one — WarnAfter for Stale, CriticalAfter for Critical; 0 for Fresh/Unknown/Future), `LastEventTime`→last_event_time. **Tracks the stale frontier** — the running max event_time per (subject=event_type, partition_key), updated each Observe; an out-of-order older event does NOT move it backwards. Frontier keyed by an unexported `frontierKey` (the package's exported `StreamKey` from DATA-01 has a `Source` the staleness frontier doesn't use). **`StalenessConfig` with per-field default fallback** (override one threshold without restating the rest); defaults WarnAfter=10s/CriticalAfter=60s/SkewTolerance=1s are conservative placeholders — staleness tolerance is per-source (tick feed sub-second vs EOD batch minutes), deployment-tuned. **`Stale()` helper** = Stale||Critical, the cases DATA-07 turns into events. Detection-only; concurrent-safe (mutex over frontier map); in-memory per-process, re-baselines on restart — same shape + detect-then-emit split as DATA-01. 12 tests cover fresh/stale/critical, boundary exclusivity, future-dated + small-skew-tolerated-as-fresh, nil-env + missing-timestamp unknowns, frontier advance + no-backward-move + per-subject/partition isolation, zero-field config fallback, and result subject/partition propagation · `kanz/internal/integrity/staleness.go`

- [x] **DATA-01** Implement `producer_sequence` gap detection — new `kanz/internal/integrity/` package (first tenant of the data-integrity layer), `gap.go`. **`GapDetector.Observe(env) → GapResult`** tracks the high-water `producer_sequence` per stream and classifies each observation. **Stream key = (source, event_type, partition_key)** — load-bearing: the Producer (EVT-17b) keeps a 1-based counter per (event_type, partition_key) and each Producer instance carries one `source`, so a sequence is only comparable within all three. Two sources writing the same partition_key each run their own 1,2,3…; keying on partition_key alone (as envelope.proto's stale "per-(source, partition_key)" comment suggests) would interleave them and manufacture phantom gaps. The implementation is authoritative over the proto comment. **Six statuses**: `NotApplicable` (seq==0 — producer didn't sequence, e.g. no partition_key; also nil envelope), `FirstSeen` (baseline — a first observation of seq>1 is NOT a gap because a consumer legitimately starts mid-stream via late join / partition reassignment / replay window; the caller inspects `Sequence` if it bootstraps from 1), `OK` (seq==prev+1), `Gap` (seq>prev+1, with `MissingFrom..MissingTo` inclusive + `MissingCount()`), `Duplicate` (seq==prev, redelivery — does NOT advance the mark), `Regression` (seq<prev, out-of-order/stale redelivery — flagged because per-partition ordering (event-class-rules §1) says it shouldn't happen; mark held). **Gap advances the high-water mark** so it's reported exactly once, then contiguous sequences resume OK. **In-memory, per-process, concurrent-safe** (sync.Mutex over the per-key map): reports gaps in the stream this detector sees from first event; re-baselines on restart via FirstSeen; cross-instance reconciliation is a downstream concern (DATA-07 emits per-detector findings). Detection-only — wiring `quality_flags` (DATA-06) and emitting data-quality events (DATA-07) are later tasks. 11 tests cover baseline+contiguous, gap range + single-missing + report-once-then-resume, duplicate-doesn't-advance, regression-holds-mark, stream isolation across source/event_type/partition_key, seq-0 + nil not-applicable, and concurrent multi-stream correctness · `kanz/internal/integrity/gap.go`

### Go ↔ Python Prediction Layer

- [x] **PRED-14** Tests: streaming worker backpressure (consumer-lag scaling) — new `kanz-py/tests/test_streaming_backpressure.py`, 6 tests. **The streaming worker (PRED-04) has no explicit backpressure code — it's structural** in how the worker is driven: `NATSClient.subscribe` does `fetch(batch=1)` → `await handler` → `ack`, so a slow model bounds in-flight to 1 per worker and unprocessed events pile up server-side as consumer lag (the signal to scale out by adding workers to the durable group). Tests model that delivery contract with a shared backlog `asyncio.Queue` (stand-in for the durable group — each event handed to exactly one worker via `get_nowait`) + a `ConcurrencyGate` (a shared in-flight counter + `asyncio.Event` release) so concurrency is asserted **deterministically with no sleeps** — the gated model blocks in `predict`, the test polls until the expected in-flight count, asserts, then releases. **6 tests**: (1) single worker holds the backlog while processing one event — pulls exactly one, the other 4 wait in the queue as lag, nothing published until predict completes (the core backpressure property); (2) publish-before-pull lockstep — one predict ↔ one publish, `max_in_flight==1`; (3) **scaling**: 3 workers sharing one backlog reach `in_flight==3` (adding workers raises aggregate concurrency — how lag is worked down); (4) the group delivers each of 20 events exactly once across 4 workers (no loss, no dup — `subject_id`s unique and complete); (5) single worker preserves per-subject ordering (the sequential loop is what guarantees the per-`subject_id` ordering the engine relies on end-to-end); (6) **backpressure delays but never degrades** — a slow model yields a NORMAL prediction once it returns, contrasting the streaming path's block-and-lag with the interactive servicer's (PRED-08) admission-control load-shedding (DEGRADED + `pool_saturated`). **Dev-env note**: this is the first task run with a working `kanz-py` test environment — generated the Python schema SDK via `buf generate` (remote `protocolbuffers/python`+`pyi` plugins) into the gitignored `gen/python`, importable as PEP-420 namespace packages via `PYTHONPATH`, and installed `pytest`/`pytest-asyncio`/`protobuf`/`nats-py`/`aiokafka`. Confirmed PRED-11's 18 feature-store tests (which had never run) pass. **Surfaced pre-existing breakage (out of scope, flagged)**: `tests/test_consumer.py` (EVT-18d) has 16 stale tests constructing `Message(body=...)` without the required `subject` positional arg → TypeError; and `tests/test_interactive_servicer.py` (PRED-08) needs `grpcio` + a generated python gRPC stub (not produced by the `protocolbuffers/python` plugin alone). Both predate PRED-14 and are unrelated to it · `kanz-py/tests/test_streaming_backpressure.py`

- [x] **PRED-13** Tests: degraded-mode fallback + sync timeout/circuit-breaker — new `kanz/internal/prediction/resilience_test.go` (`package prediction` internal-test, so it can inject the `CircuitBreaker.now` clock — same time-mocking approach as RISK-13's `Detector.now`) + corrections to 3 PRED-07 tests in `sync_client_test.go`. **Unblocked by fixing a real pipeline gap**: `kanz/internal/prediction` never compiled because `buf.gen.yaml` had only `protoc-gen-go` (message types), no `protoc-gen-go-grpc`, so `inference/v1` carried no `InferenceServiceClient` stub — PRED-06/07 referenced a type that was never generated. Added `protoc-gen-go-grpc` (local plugin, `paths=source_relative`) to `buf.gen.yaml`, regenerated → `inference_service_grpc.pb.go` now exists; the sync path builds end-to-end for the first time. **Latent test bugs surfaced**: with the package finally compiling, 3 PRED-07 tests failed — they asserted the live trigger reason (`inference_unavailable`/`inference_timeout`/`circuit_open`) on the cache-MISS fallback path, but the documented contract (sync_client.go `fallback` comment + BRAIN + PRED-04's Python `_fallback`) is **cache hit → trigger reason; cache miss → `no_cached_prediction`** (the trigger stays observable via the returned `err`). Corrected the 3 to assert `no_cached_prediction` on the miss path (+ the timeout one now also asserts `errors.Is(err, context.DeadlineExceeded)`); renamed two to `…NoCache…` for honesty. **7 new tests**: (1) **cache-hit × trigger-reason matrix** — timeout-with-cache-hit reports `inference_timeout` while serving the cached value, open-circuit-with-cache-hit reports `circuit_open` without hitting the wire — the reason mapping the no-cache tests structurally cannot reach; the circuit-open case also asserts the **trust-laundering guard** (a cached NORMAL is re-served as DEGRADED, never NORMAL, per PRED-02 §1); (2) **feature-translation failure** (a `FeatureValue{}` with unspecified Kind) returns DEGRADED and never calls the stub, mirroring the empty-SubjectID validation path; (3) **no-cache fallback propagates the request `as_of`** + subject_id; (4) **deterministic circuit-breaker timing** via injected `now`: failed half-open probe re-opens AND resets the cooldown window (next probe waits another full cooldown — documented behavior previously unreachable without controlling the clock), cooldown boundary is exclusive (`Sub > cooldown`: at exactly cooldown still open, 1ns past → half-open — RISK-11's "above the budget" discipline), and a success below threshold resets the consecutive-failure count so interrupted failures never accumulate to open · `kanz/internal/prediction/resilience_test.go`, `kanz/internal/prediction/sync_client_test.go`, `kanz-schemas/buf.gen.yaml`

- [x] **PRED-12** Contract tests: feature/prediction schema versioning — new `kanz/test/contract/inference/schema_versioning_test.go` (`package inference_test`, external — exercises only the public `inferencepb` + `pkg/bus` surface like the other EVT-21/PRED contract trees). Proves the runtime invariants the prediction layer's versioning + cross-path design depend on, for the two PRED-01 payloads (FeatureVector in, PredictionEnvelope out), complementing the static gates (`buf breaking` EVT-07 + CODEOWNERS review on `proto/inference/`) which can't prove live-binding behavior. **Six contract areas, 9 tests**: (1) **version-identifier conventions** — `feature_set_ref` is `"{name}:{version}"`, `model` is `"{name}@{version}"`; the distinct separators are the routing-layer's load-bearing signal (PRED-03/04 routing, PRED-09 lookup), asserted to `strings.Cut` cleanly and to NOT cross-contaminate (`:` never in model, `@` never in feature_set_ref) + round-trip byte-stable across marshal (routing happens after the payload crosses the bus/gRPC boundary) + the PRED-05 `feature_vector_event_id` join key survives; (2) **forward compat** — an unknown field (tag 1000, outside 19000–19999 reserved, EVT-21c's choice) appended to FeatureVector + to PredictionEnvelope survives unmarshal + re-marshal (replay re-marshals payloads, so a dropped field corrupts replay of newer-schema features — the determinism property PRED-06 extends across paths); (3) **forward-compat of a future FeatureValue oneof arm** — an unknown oneof tag (99, not 1–4) reads as unset `Value` but its bytes survive re-marshal, so quant-ml can add a new value type (e.g. sparse-vector) without a consumer flag-day; (4) **backward compat** — minimal v0-shaped FeatureVector (no source_event_ids/values) + NORMAL PredictionEnvelope (no class_label/explanation/degraded_reason) decode with absent fields at zero; (5) **oneof tagged-union discrimination** — scalar/ordinal/categorical/vector round-trip preserving their Go type, pinning PRED-01's "a categorical can never be misread as a scalar magnitude"; (6) **open-enum semantics** — a future PredictionMode value (3) is preserved as its raw number (not clamped) AND must not decode as NORMAL, enforcing PRED-02 §4's "non-NORMAL ⇒ DEGRADED" so a current consumer in front of a newer producer never acts on unrecognised full-trust; (7) **cross-path payload identity (PRED-06)** — the same FeatureVector marshaled as a sync gRPC body equals the payload recovered from a streaming EventFrame via `bus.Unframe`, and decodes back `proto.Equal`, proving a bus-logged feature can be replayed into the sync RPC. **Infra note**: required regenerating `kanz-schemas/gen/go` (the committed-locally Go SDK was stale — missing the `inference/v1` package entirely) via a Go-only `buf generate` template + `go mod init/tidy`; surfaced a pre-existing gap that `buf.gen.yaml` has no `protoc-gen-go-grpc` plugin, so `inference_service.pb.go` carries no `InferenceServiceClient` stub and `kanz/internal/prediction` (PRED-07 sync_client.go) does not build — out of PRED-12 scope (this test targets only the message schemas) but flagged for a follow-up to wire go-grpc into the release pipeline · `kanz/test/contract/inference/`

- [x] **PRED-11** Define feature-store boundary interface (trivial first impl) — new `kanz-py/kanz_inference/featurestore/` package, `store.py` + `__init__.py`. **`FeatureStore` Protocol + `InMemoryFeatureStore` trivial impl**. Lands-before-impl (same pattern as PRED-04's `Model`/`PredictionPublisher`, RISK-04's `Applier`): both inference paths today already receive a full `FeatureVector` (streaming decodes it from `inference.feature.computed`; interactive gets it as the gRPC body), so nothing consumes the store yet — the boundary is fixed first so a real store (Feast/Redis/point-in-time warehouse) plugs in behind it without touching callers and PRED-12's schema-versioning contract tests have a target. **Point-in-time `get` is the load-bearing decision**: `get(subject_id, feature_set_ref, as_of)` returns the vector effective AT OR BEFORE `as_of`, never a later one. This is the single property separating a feature store from a cache — a "latest wins" store would silently leak future data into replay/backtest (breaking EVT-21d-style determinism) and create training/serving skew. `as_of=None` ⇒ most recent; `as_of` before all stored marks ⇒ `None` (no future leakage), and the caller treats a miss as missing-features → DEGRADED per PRED-02, NOT zero features. **Keyed by `(subject_id, feature_set_ref)` jointly** — a subject carries features under several feature sets at once (different models read different sets), so feature_set_ref is part of identity not just routing; same routing key as the registry + worker. **Async-everywhere** to match the eventual remote-store contract + the `Model` protocol; the trivial impl never awaits, so lock-free under the single event loop (same reasoning as PRED-04's `LastKnownCache`). **Ordering via `(seconds, nanos)` tuple** (proto Timestamps aren't directly orderable) + `bisect` over a parallel sorted keys list: `put` inserts keeping history sorted (out-of-order puts still resolve correctly), `bisect_right` on the target finds the rightmost at-or-before entry. **`put` idempotent-by-as_of**: same as_of overwrites (re-materialise one point in time), new as_of extends history. **Defensive copy on both sides** (`CopyFrom`) so caller mutation can't bleed into stored state or vice versa — RISK-03 immutability discipline. **Trivial impl deliberately omits**: persistence, TTL/eviction, online/offline split, distributed state (process-local, like the PRED-09 registry) — each is a real-store concern behind the same contract. **pyproject needs no change** — `kanz_inference*` already auto-discovers the new subpackage. 18 tests cover protocol shape, put/get round-trip, miss + unknown-feature-set miss, point-in-time (between-marks returns earlier not latest, exact-match inclusive, before-all returns None, nanos ordering, out-of-order put correctness), same-as_of overwrite, subject + feature-set isolation, defensive copy both directions, and missing subject_id/feature_set_ref/as_of rejection · `kanz-py/kanz_inference/featurestore/`, `kanz-py/tests/test_feature_store.py`

- [x] **PRED-10** Implement shadow / canary inference path — new `kanz-py/kanz_inference/shadow/`. **`ShadowExecutor` satisfies Model protocol**, wrapping PRED-09's Registry: routes inbound features to primary + shadows by `fv.feature_set_ref`. Workers (PRED-04/PRED-08) construct one ShadowExecutor per process and use it as their Model — no worker-side awareness of shadows needed. **`model_id = "shadow-executor"` composite name** — single ShadowExecutor handles many feature_sets, so no single canonical id; per-response `PredictionEnvelope.model` carries the actual primary model_id for traceability. **Shadows run AFTER primary, not in parallel — load-bearing decision**: if shadows ran via asyncio.gather, response latency would be bounded by slowest task, defeating canary's "risk-free evaluation" premise. Awaiting primary first then firing shadows via `asyncio.create_task` (no await) keeps primary latency budget unaffected by shadow throughput. Trade-off: a shadow depending on something the primary's execution side-effected (cache warmup, GPU pre-alloc) sees different state — acceptable because such coupling indicates a stateful Model, itself a correctness concern PRED-04/08's per-call contract doesn't permit. **Shadow failure isolation**: each shadow's `predict` + each observer's `observe` are wrapped in try/except in `_run_shadow_and_observe`; exceptions logged but never re-raised. By the time a shadow can fail, the primary's response has been returned — no path for shadow exception to affect consumer. **Task tracking via `_shadow_tasks` set + `add_done_callback`** prevents GC from reclaiming fire-and-forget tasks before they complete; also enables `wait_for_shadows()` for graceful shutdown and deterministic test completion. **`ShadowObserver` Protocol with `@runtime_checkable`**: async `observe(fv, primary, primary_pred, shadow, shadow_pred)`. Async to allow remote sinks (metrics push, database write) without blocking the shadow path. Default `LoggingShadowObserver` logs each pair at INFO with delta — fine for low-volume canary; high-volume production swaps for a metrics-store observer. **No primary registered for feature_set_ref → DEGRADED response** with `inference_unavailable` reason (PRED-02 §2.1 reused identifier from PRED-04/08). 12 tests cover constructor nil rejection, primary-only happy path, no-primary degraded, shadow fan-out to same fv, observer records pairs, shadow exception isolated, observer exception isolated, no-shadows no-observer-calls, shadows-after-primary ordering (slow shadow doesn't block primary), routing by feature_set_ref, LoggingShadowObserver smoke + protocol compliance · `kanz-py/kanz_inference/shadow/`

- [x] **PRED-09** Implement model versioning + artifact registry — new `kanz-py/kanz_inference/registry/` package. **`ModelMetadata` frozen dataclass** carries the four contract fields: `model_id` (`"{name}@{version}"`, same shape as PredictionEnvelope.model + DecisionLog.decider), `feature_set_ref` (`"{name}:{version}"`, the routing key matching FeatureVector.feature_set_ref), `confidence_threshold` (per PRED-02 §2.3 — per-MODEL, not global; uncalibrated models pick 1.0 so every prediction marks DEGRADED), `artifact_uri` (traceability only; registry doesn't fetch). Frozen so it's safely hashable + can be a dict key in higher-level structures. **`Registry` with explicit primary/shadow distinction at registration time** — load-bearing for PRED-10's shadow/canary flow. Alternative ("single map, caller picks the right one") would spread routing policy across every caller; explicit primary/shadow at `register(metadata, model, primary=True/False)` concentrates policy in one place and gives workers two intent-revealing lookup methods (`primary_for_feature_set`, `shadows_for_feature_set`). **Three indexes maintained**: `_by_id` (canonical lookup), `_primary_for_fs` (1:1 per feature_set), `_shadows_for_fs` (1:N per feature_set). Redundancy keeps both lookups O(1). **Re-registering an existing model_id replaces the entry** (hot-reload pattern); `_remove_from_role_indexes` scrubs old role so a "primary → shadow" demotion is consistent. **`ModelLoader` Protocol** for pluggable artifact loading — registry stores constructed Model instances; how artifact_uri becomes a Model is a deployment concern (pickle/ONNX/torchscript/remote-server differ wildly). Protocol exists so loaders plug in without changing the registry contract. **Validation at register time**: model_id must contain `@`, feature_set_ref must contain `:`, both non-empty, model non-nil. Strict parsing deferred but format hint enforced. **What the registry deliberately does NOT do**: artifact loading (deferred to ModelLoader plugin); lifecycle (warmup, LRU eviction — long-running worker keeps all registered, PRED-09b can add if memory pressure becomes real); distributed state (process-local — each worker has its own; cross-process coordination is config management + rolling restarts, not runtime registry). 14 tests cover register+lookup round-trip, miss, primary lookup, shadow collection, latest-wins primary displacement, promote-shadow-to-primary rollout pattern, list_models sorted, unregister removes from all indexes + returns true/false, all validation rejections (empty/no-@/no-:/nil model), ModelMetadata hashability · `kanz-py/kanz_inference/registry/`

- [x] **PRED-08** Implement isolated interactive inference worker pool + admission control — new `kanz-py/kanz_inference/interactive/servicer.py` (gRPC servicer + admission control + `serve` helper). **`InferenceServicer` subclasses `InferenceServiceServicer`** (generated from PRED-06 proto) and implements `Predict(FeatureVector) → PredictionEnvelope` on the grpc-aio async path. Reuses PRED-04's `Model` protocol — same model artifact serves both streaming and interactive paths. **Counter-based admission control, not Semaphore**: asyncio Semaphore's `acquire()` blocks indefinitely, defeating "fail fast on overload"; counter-with-immediate-reject gives the exact "in_flight ≥ max ⇒ reject now" semantic admission control needs. Race-free under asyncio (single event loop, no await between read and increment). **`pool_saturated` added to PRED-02 §2.1** as a fourth identifier under "inference unavailable" trigger (additive doc edit). Distinct from generic `inference_unavailable` so observability can distinguish "overload" from "downstream failure" — same alert-stability discipline as the other PRED-02 identifiers. **Process isolation from streaming**: per KANZ_BRAIN anti-decision "market ingestion, AI inference, batch compute must be process-isolated". Interactive servicer runs in its own process; admission rejections show up as DEGRADED responses (not bus consumer-lag), preserving the isolation contract that streaming bursts don't starve interactive. **Cancellation propagates** (asyncio.CancelledError re-raised, NOT caught and synthesised) — when client deadline expires, gRPC framework cancels the server coroutine and the client (PRED-07) sees DEADLINE_EXCEEDED → translates to `inference_timeout`. Adding a server-side timeout would race the client deadline. **`finally` decrements in_flight** so cancellation + exceptions both release the slot. **Server-side timeout deliberately NOT enforced**: PRED-07's client deadline is the canonical timeout; adding a server-side one creates a race. If client doesn't set a deadline (a bug), server runs to completion — surfaced in observability, not silently capped. **`serve(model, address, max_in_flight) → server`** is a small helper for the common case: construct grpc.aio.Server, register servicer, bind, start. Caller awaits `server.wait_for_termination()`. **`grpcio>=1.62`** added to pyproject (first grpc dep). **DEFAULT_MAX_IN_FLIGHT = 32** — conservative for a single Python worker on typical container size; tune per deployment based on observed p99 × target QPS. 11 tests cover constructor validation, happy-path predict, in_flight increments/decrements correctly on success + exception + cancellation, model exception → DEGRADED inference_unavailable with would-have-been model_id, admission rejection at saturation (model.predict NOT called for rejected request), admission recovers after slot frees, cancellation propagates, AsOf propagated through degraded response · `kanz-py/kanz_inference/interactive/`, `kanz-schemas/docs/inference-degraded-contract.md`

- [x] **PRED-07** Implement Go-side sync inference client — new `kanz/internal/prediction/sync_client.go`; adds `google.golang.org/grpc v1.69.0` to go.mod. **Three resilience primitives in one file**: SyncClient (gRPC stub wrapper), CircuitBreaker (consecutive-failure breaker, half-open probe), PredictionCache (per-subject_id last-known NORMAL prediction — same shape as PRED-04's LastKnownCache, separate instance because Go orchestrator and Python worker are different processes). **Predict ALWAYS returns non-nil envelope** — transport failure / timeout / open circuit → DEGRADED envelope per PRED-02 §1 "never black-hole". Returns `(*PredictionEnvelope, error)` where envelope is always populated; error is non-nil for OBSERVABLE FAULTS (transport, validation) so callers can log/metric them. Application code branches on `env.Mode` regardless of err. **Four degraded-reason constants matching PRED-02 §2**: `ReasonInferenceUnavailable` / `ReasonInferenceTimeout` / `ReasonNoCachedPrediction` / `ReasonCircuitOpen`. Named bindings (not free-form strings at call sites) so renames surface as Go compile errors — same alert-stability discipline as PRED-04's Python constants. **Fallback logic mirrors PRED-04 verbatim**: cache hit → `proto.Clone(cached)` with `Mode=DEGRADED + DegradedReason=<reason>`; cache miss → zero-value DEGRADED with `no_cached_prediction`. **Cache rejects DEGRADED responses** — same trust-laundering protection from PRED-02 §1. **Circuit breaker is custom, ~50 lines**: consecutive-failure count + cooldown, half-open probe. Trade-off vs sony/gobreaker or hashicorp/go-resiliency: zero deps, no rolling windows or success-ratio thresholds yet — promote to a real library if those become needed. **State() method** returns "closed"/"open"/"half_open" for tests and future observability. **`NewSyncClientWithStub`** lets tests inject a fake `inferencepb.InferenceServiceClient` so the test suite runs without a gRPC server. **Defaults** (`DefaultSyncClientOptions`): timeout=200ms (more generous than BRAIN's <50ms server target — allows occasional spikes without firing breaker), breaker threshold=5, cooldown=10s. 11 tests cover normal-path, cache-only-NORMAL discipline, transport-error fallback, cache-hit-reuses-cached, timeout fallback, circuit opens after N failures, circuit closes after successful probe, empty-SubjectID rejection (stub not called), cache overwrite + nil-store + empty-id-noop, breaker state transitions · `kanz/internal/prediction/sync_client.go`, `kanz/go.mod`

- [x] **PRED-06** Define gRPC interface for interactive inference — new `kanz-schemas/proto/inference/v1/inference_service.proto`. Single RPC: `Predict(FeatureVector) returns (PredictionEnvelope)`. **No wrapper request/response messages** — uses PRED-01's payloads directly. This is the "same proto on both paths" property: a FeatureVector logged from the streaming path can be replayed into this RPC without field migration; a PredictionEnvelope captured from either path is consumable by the same downstream code. Replay-determinism (EVT-21d-style) extends across transports. **Deadline semantics**: callers MUST set a gRPC deadline. Per PRED-02 §2.2, exceeding it returns a DEGRADED PredictionEnvelope with `degraded_reason = "inference_timeout"` — NOT a gRPC DEADLINE_EXCEEDED. Returning an error would force callers to handle two failure paths (gRPC status + degraded envelope); unified-response keeps PRED-02 §1's "never black-hole" rule on the sync path. **OK/non-OK split is intentional, reserved for transport-vs-application failures**: `OK + DEGRADED envelope` = inference layer reached + decided it couldn't serve full-trust (caller branches on PredictionEnvelope.mode); `non-OK gRPC status` = transport failure (UNAVAILABLE/INTERNAL/RESOURCE_EXHAUSTED — inference layer unreachable, caller never saw the model's opinion). PRED-07's client library translates non-OK to a synthesized DEGRADED envelope with `degraded_reason = "inference_unavailable"` so application code sees one shape. Split lets observability distinguish transport-vs-application failures while keeping the application contract uniform. **Target latency p99 < 50ms** (KANZ_BRAIN); PRED-07's circuit breaker fails over to the streaming-path cache when the inference layer can't hold that budget. **Service location**: implementation lives in Python (PRED-08 worker pool); client lives in Go (PRED-07) · `kanz-schemas/proto/inference/v1/inference_service.proto`

- [x] **PRED-05** Implement prediction-event publishing from Python workers — new `kanz-py/kanz_inference/publish.py`. **Class named `Publisher`, not `PredictionPublisher`** (which is the Protocol name in `streaming.worker`) — module-scoped name `kanz_inference.publish.Publisher` avoids the collision while structural typing makes the class duck-compatible with the protocol without an explicit `class Publisher(PredictionPublisher)` declaration. **`publish_prediction(source_envelope, prediction)` flow**: validate inputs → derive QualityFlag list from `prediction.mode` → enter `propagation_context` with lineage from source envelope → `producer.publish(Event(...))`. **Lineage propagation via contextvars** (EVT-18c): the Producer's `_stamp` reads correlation/causation/trace from contextvars; `propagation_context` sets them. Causation_id = source_envelope.event_id (NOT source_envelope.causation_id) — same chain-linking rule as EVT-17c, the next event's causation points at the CURRENT event. **PRED-02 §3 producer obligation honored**: `prediction.mode == DEGRADED` → envelope `quality_flags = [QUALITY_FLAG_DEGRADED]`. The flag exists at the envelope layer so payload-blind observability (data-integrity layer DATA-*) sees the signal without parsing the payload. **Subject + schema constants**: `inference.prediction.scored` + `inference.v1.PredictionEnvelope:1` (subject taxonomy §1 example, PRED-01 payload). **partition_key = subject_id** structural — per-subject ordering features→prediction must hold end-to-end. **Validation at publish boundary**: nil envelope/prediction + missing subject_id + missing as_of all rejected with ValueError. Other invariants (FACT idempotency_key == event_id, all envelope-policy §7 required fields) caught by `kanz_bus.validate` inside the Producer. **`@runtime_checkable` added to the protocol** in worker.py so the cross-module compliance test can `isinstance(Publisher(), PredictionPublisher)` to verify the duck-typing holds. 11 tests cover constructor + happy-path normal + DEGRADED-sets-flag + lineage propagation (correlation/causation/trace) + partition_key=subject_id + payload round-trip + nil-envelope/nil-prediction/missing-subject_id/missing-as_of rejection + FACT idempotency invariant + protocol compliance · `kanz-py/kanz_inference/publish.py`, `kanz-py/tests/test_prediction_publish.py`

- [x] **PRED-04** Implement Python streaming inference worker — new `kanz-py/kanz_inference/` package (Python counterpart to Go `kanz/internal/prediction/`), first subpackage `streaming/`. pyproject.toml updated to include both `kanz_bus*` + `kanz_inference*` under the existing distribution (future task pairs with PRED-09 model-registry packaging to split into separate distributions). **`StreamingWorker.handle(env, payload)`** is the canonical EventHandler shape — decodes FeatureVector payload, calls `Model.predict`, hands result to `PredictionPublisher`. Public method name (not underscore-prefixed) so tests drive it directly without spinning up a fake Subscriber loop; `run(group)` wraps subscription wiring for production. **Three boundary protocols** (lands-before-impl pattern from RISK-04): `Model` (async predict + model_id attr; implementations live in quant model artifacts), `PredictionPublisher` (PRED-05 will satisfy — boundary kept here in worker.py since the worker is the only caller), `Subscriber` (already exists in kanz_bus). **`LastKnownCache`** implements PRED-02 §2.1 fallback: per-subject_id dict of most-recent NORMAL prediction. **Two structural rules from PRED-02**: only NORMAL predictions seed the cache (caching a DEGRADED prediction would let it serve later requests as if fresh — trust laundering forbidden by §1); model exception → cache lookup → on hit re-emit cached value with `mode=DEGRADED + degraded_reason=inference_unavailable`; on miss synthesize `value=0/confidence=0/degraded_reason=no_cached_prediction`. **Degraded-reason identifiers** are constants from PRED-02 (`REASON_INFERENCE_UNAVAILABLE`, `REASON_NO_CACHED_PREDICTION`) — string-typed but with named bindings so a future rename surfaces as a Python import error rather than silent alert-rule breakage. **Lineage**: `feature_vector_event_id` on the prediction = envelope.event_id of the originating feature event — propagated through the fallback path explicitly (the normal path lets the Model set it). **No asyncio.Lock on cache**: single event loop ⇒ dict read/write is GIL-atomic; mirrors RISK-11 Cache's simplicity. 11 tests cover constructor nil-arg rejection, normal-path publish, cache-only-NORMAL discipline, cache-rejects-DEGRADED-storage, fallback-cache-hit + fallback-cache-miss + lineage-through-fallback paths, malformed-payload-raises (model + publisher not called), and cache overwrite + miss + empty-subject-noop · `kanz-py/kanz_inference/streaming/`, `kanz-py/tests/test_streaming_worker.py`

- [x] **PRED-03** Implement Go-side feature event publishing — new `kanz/internal/prediction/` package (first PRED-go file). **Go-native types mirror inference.v1 proto**: `SubjectID`, `FeatureSetRef`, `FeatureName` typed-string aliases; `FeatureVector` struct (SubjectID, FeatureSetRef, Values map, AsOf, SourceEventIDs); `FeatureValue` tagged-union (Kind enum + per-kind field). Same two-layer split RISK-03/RISK-10 chose — proto is wire shape, Go-native is in-memory engine shape; translation at publish boundary. Future Go consumers (PRED-11 feature store) see Go-native types only. **Convenience constructors `Scalar`/`Ordinal`/`Categorical`/`Vector`** — `prediction.Scalar(1.5)` reads better than struct-literal-with-Kind. `Vector` defensive-copies the input slice so caller mutations don't bleed into already-constructed FeatureValues. **`Publisher.PublishFeatures(ctx, fv)`** wraps a shared `bus.Producer` (same RISK-10 pattern — shared Producer keeps producer_sequence monotonic across event types from the same source). Validates Go-native input (SubjectID + FeatureSetRef + Values non-empty + AsOf non-zero + every FeatureValue.Kind != UNSPECIFIED), translates to proto, hands to Producer (which auto-stamps envelope identity + lineage from ctx). **partition_key = SubjectID** so per-subject ordering holds end-to-end: features for one subject arrive at the inference worker in order, predictions back to Go preserve the same ordering — same per-aggregate ordering discipline RISK-04→RISK-10 establishes for portfolios. **Event-type + schema-ref**: `inference.feature.computed` + `inference.v1.FeatureVector:1` (subject taxonomy §1 example, PRED-01 payload). **Module-private** under `kanz/internal/`; lifts to `kanz/internal/prediction/api/v1/` if a cross-module consumer (replay tooling, scenario harness) appears, matching the RISK-01 promotion pattern. 9 tests cover NilProducer rejection, happy-path (envelope validate + payload round-trip + all 4 FeatureValue kinds), each missing-required-field rejection (SubjectID + FeatureSetRef + Values + AsOf + UNSPECIFIED-kind), defensive-copy of Vector input, and partition_key = SubjectID contract · `kanz/internal/prediction/feature_publish.go`

- [x] **PRED-02** Document degraded-mode contract — new `kanz-schemas/docs/inference-degraded-contract.md`, 7 numbered sections matching the existing kanz-schemas/docs style (envelope-policy, event-class-rules). **Core rule (§1)**: the prediction layer NEVER returns "no prediction" — always a PredictionEnvelope with honest `mode`. Black-holing is a correctness regression because the caller can't distinguish "model is silent" from "model said no" and has no signal to fall back on its own logic. **Three triggers (§2)** mapped to stable `degraded_reason` identifiers: 2.1 inference unavailable (`inference_unavailable`/`bus_lag_exceeded`/`circuit_open` — falls back to PRED-04 cache; no cache → `no_cached_prediction` with value=0 confidence=0); 2.2 inference slow (`inference_timeout` — sync gRPC budget exceeded falls back to cache); 2.3 low confidence (`low_confidence` — per-model threshold from artifact metadata, NOT global constant). Models without calibrated confidence are ALWAYS DEGRADED (the "unknown confidence as no confidence" rule from PRED-01's envelope semantics, repeated as a contract obligation here). **Producer obligations (§3)**: always return envelope, set mode honestly, machine-readable degraded_reason (free-form context goes in explanation sidecar), emit one DecisionLog per degraded response (audit trail independent of consumer logging), set envelope `QUALITY_FLAG_DEGRADED` on the bus envelope so payload-blind observability sees it. **Consumer obligations (§4)**: branch on mode BEFORE reading value (ignoring mode = correctness bug); document per-use-case fallback; treat UNSPECIFIED as DEGRADED (defensive — broken producer must not silently get full-trust); never elevate DEGRADED to NORMAL based on value alone. **§5 — mode is per-response, not per-subject**: rules out the "this subject is degraded" caching anti-pattern. **§6 — cross-path uniformity**: streaming and sync paths share one decision function (PRED-04 + PRED-07), so a consumer can't route around degraded mode by switching transport. **§7 explicitly scopes out** what the doc does NOT govern: timeout values (per-deployment tuning), feature-staleness signalling (DATA-02/03's territory), model-version pinning + shadow policy (PRED-09/10), and the risk engine's RISK-11 degraded mode (independent — risk can be NORMAL with DEGRADED prediction inputs and vice versa) · `kanz-schemas/docs/inference-degraded-contract.md`

- [x] **PRED-01** Define feature-vector + prediction-envelope proto schemas — new `kanz-schemas/proto/inference/v1/` package, two files. CODEOWNERS already maps `/proto/inference/` to `@kanz-eng/quant-ml`; buf.yaml auto-discovers anything under `proto/` — no infra changes needed. **`FeatureVector`**: subject_id (instrument/portfolio/counterparty — same identifier used as envelope partition_key when published as `inference.feature.computed`), feature_set_ref (`{name}:{version}` for routing to matching model version per PRED-09), values (name → FeatureValue map), as_of (effective domain time of the INPUTS, not computation time), source_event_ids (lineage to upstream events — same role as `RiskMeasure.source_event_ids`). **`FeatureValue` tagged union** (oneof): scalar (double — model inputs, BRAIN's banned-double rule doesn't apply since these aren't money), ordinal (sint64 for integer-encoded categoricals), categorical (string label), vector (DoubleArray wrapper — proto3 doesn't allow repeated fields directly in oneof). Categorical distinct from scalar so a model can't accidentally treat a class id as a magnitude. **`PredictionEnvelope`** (name has "Envelope" because it bundles prediction + model identity + confidence + mode + explanation + lineage): subject_id (mirrors FeatureVector), model (`{name}@{version}` — same shape as `DecisionLog.decider`), value (double prediction output), confidence (0..1 calibrated; set to 0 + DEGRADED mode when uncalibrated, forces consumer to handle degraded path explicitly rather than blindly trust), class_label (set for classification, empty for regression), explanation (per-feature SHAP-like contributions), mode (NORMAL/DEGRADED enum — consumers MUST branch before acting), degraded_reason (short operator-readable trigger), feature_vector_event_id (back-pointer for join-on-replay), as_of (effective domain time). **`PredictionMode` enum** with explicit UNSPECIFIED zero value (never set, per kanz-schemas enum convention). Both payloads transport on both the async streaming path (bus message body) and the sync gRPC path (PRED-06). Subject taxonomy: `inference.feature.computed` and `inference.prediction.scored` (already documented in subject-taxonomy.md §1 examples) · `kanz-schemas/proto/inference/v1/feature_vector.proto`, `kanz-schemas/proto/inference/v1/prediction.proto`

### Risk Engine

- [x] **RISK-13** Tests for degraded-mode behavior + uncertainty propagation — new `kanz/internal/risk/integration_test.go` (`package risk` internal-test so it can drive `Detector.now` for time mocking without expanding the public surface). **Cross-package integration scope distinct from individual-component tests**: compute/, state/, degraded_test.go each exercise one package; this file exercises the *composition* (state.Store → compute → Cache → Detector → response flags) that the future orchestrator will perform. **`computeExposureWithFlags` helper** inlines the canonical orchestrator composition pattern — try live, on success cache+tag, on failure fall back to cache; on no-cache return degraded "not found" signal. Doubles as documentation of the pattern; when the orchestrator EngineImpl lands, these tests get rewritten against it directly and the composition shifts from test code to production code. **9 integration scenarios**: fresh state (no flags), over-budget (STALE only), over-degraded (DEGRADED+STALE), cache-fallback-after-time-advance (uses two Detectors at different wall-clock times to demonstrate cache+Detector composing under failure), unknown-portfolio-returns-degraded-not-found, Engine.Health aggregating latest-AsOf across portfolios (single healthy portfolio carries the engine — intentional, ingestion-alive-somewhere semantic), uncertainty propagation through compute (positions post-applied with MarketValueUncertainty → measures carry √(σ_1²+σ_2²)), no-uncertainty-inputs leaves all UncertaintyAbs nil (not zero — the "no signal" vs "calibrated zero" distinction documented at api/v1 boundary), mixed uncertainty inputs skip nil (defends PropagateSumIndependent's skip-nil rule from a regression that would falsely tighten the band). **`applyState` test helper** wraps the state.Store apply path with realistic envelope construction so each test reads as "apply this state, do this query" without spelling out envelope plumbing. **Uncertainty post-apply pattern** (`p.SetPosition(withUnc)` after `store.Lookup`): the wire proto doesn't yet carry MarketValueUncertainty, so tests simulate the future RISK-11-staleness-synthesis path by populating the field directly on the live Portfolio — proves the algorithm is wired even though the production pipeline currently always leaves it nil. **Risk Engine epic complete (RISK-01..13)**: interface + boundary + domain + ingest + state + exposure + measures + uncertainty + scenario + publish + degraded + property tests + integration tests · `kanz/internal/risk/integration_test.go`

- [x] **RISK-12** Unit + property tests for measure correctness — new `kanz/internal/risk/compute/correctness_test.go` adds 11 property tests on top of RISK-06/07/08's unit tests. **Property iteration**: 50 seeded random portfolios per test (deterministic seeds 1..50 so failures are reproducible by re-running with the same seed). **Why not `testing/quick`**: stdlib quickcheck generates via reflection — works for plain types but cannot synthesise `*commonpb.Decimal` proto pointers without a custom Generator on a type we don't own. Seeded-loop approach gives the same property-test ergonomics without fighting the proto-type constraint. **Mathematical invariants** asserted: triangle inequality `|Net| ≤ Gross` (sum-of-abs vs signed-sum); `Gross ≥ 0` (always non-negative); `VaR99 / Gross = 0.01` exactly (RISK-07 placeholder formula — when a real VaR model replaces the placeholder, this test INTENTIONALLY breaks as failure-as-documentation that the placeholder is gone); `Delta == NetExposure` (same placeholder-identity contract); permutation invariance (positions order doesn't change measures — pins against any future change that conflates "Position exists" with "Position contributes"); zero-value-position no-op; empty portfolio yields zero for every measure; currency-bucket Nets sum to total Net (RISK-06 aggregation reconciliation). **Uncertainty invariants**: Cauchy-Schwarz bound `√(Σσ²) ≤ Σ|σ|` (independent never exceeds perfectly-correlated — defends the conservative-vs-textbook semantics from accidental reversal); scalar propagation exact `|c|×|σ|`; propagation sum is order-invariant. **Determinism**: same portfolio computed twice yields numerically identical measures (catches accidental map-iteration nondeterminism). **`dValue` helper** converts Decimal → float64 for property comparisons — uncertainty bands and order assertions don't need money-grade precision, sidesteps writing a Decimal comparator · `kanz/internal/risk/compute/correctness_test.go`

- [x] **RISK-11** Implement degraded mode — new `kanz/internal/risk/degraded.go` (first file at the top-level `kanz/internal/risk/` directory; all prior risk work lived in sub-packages — this establishes `package risk` as the home for cross-cutting concerns). **Two pieces**: `Detector` (staleness assessment, pure function) and `Cache` (last-known ExposureSet/MeasureSet per portfolio). **Detector.Assess(asOf) → (Mode, []QualityFlag)** uses two thresholds: `DefaultFreshnessBudget = 30s` (above → +QualityFlagStale, mode stays Normal) and `DefaultDegradedThreshold = 5m` (above → ModeDegraded + QualityFlagDegraded + QualityFlagStale). **Zero-AsOf is a special case**: a portfolio with no state ever applied returns ModeDegraded + QualityFlagDegraded regardless of the wall clock — caller must not read "no data" as "zero risk". **Why staleness drives mode, not health-checks**: the engine has no exotic dependencies to poll (no external risk service, no model server). The single operationally-meaningful observable is `now - portfolio.AsOf` — a healthy ingest keeps it small; any stall (broker outage, applier crash, network partition) shows up as growing staleness. Cheap signal, locally diagnosable cause. **Boundary semantics**: staleness EQUAL to threshold is FRESH (the boundary is "above the budget" not "at the budget") — off-by-one regression caught by test. **`Detector.Health(latestAsOf) → v1.Health`** composes Mode + AsOf + Staleness for the api/v1 Engine.Health implementation; zero-AsOf yields zero-staleness (meaningless to report wall-clock gap when no state ever existed). **Cache** is a per-portfolio map of last-known ExposureSet and MeasureSet, `sync.RWMutex` for cheap concurrent reads. No TTL: the cached value carries its own AsOf, Detector handles staleness — chronological cache is a different feature. Eviction = overwrite-on-next-store. Nil stores are no-ops (won't crash, won't write phantom entries). **Integration pattern for orchestrator (later task)**: on successful compute, `Cache.StoreX(id, result)`; on compute failure, fall back to `Cache.LookupX` + tag via `Detector.Assess(cached.AsOf())`. 14 tests cover Detector (fresh, over-budget, over-degraded, zero-AsOf, boundary-at-budget, custom thresholds, fallback-on-zero-thresholds, Health composition, zero-AsOf health) and Cache (store+lookup × 2 types, overwrite, miss, nil-store, AsOf round-trip through Detector) · `kanz/internal/risk/degraded.go`

- [x] **RISK-10** Emit risk-output events — new `kanz/internal/risk/publish/` package + two kanz-schemas additions: new `proto/domain/v1/measure.proto` with `RiskMeasureSet` + `RiskMeasure` (companion to ExposureSet, FACT-class, exact Decimal not double — `observation.v1.MetricObservation` uses double which violates BRAIN's banned-double rule for money/risk numbers), and additive enum extension `EXPOSURE_DIMENSION_INSTRUMENT = 6` (schema-evolution §4 "add enum value" non-breaking change). **Publisher.EmitExposure + EmitMeasures** wrap a shared `bus.Producer` and translate domain → proto at the boundary (symmetric to RISK-04's Ingestor handling proto → domain). Shared Producer is load-bearing: producer_sequence stays monotonic across event types from the same source. **Per-aggregate ordering preserved end-to-end**: `partition_key = portfolio_id` on both event types — same value RISK-05 keys on for state ordering, RISK-04 keys on for ingest — so the per-portfolio ordering guarantee holds ingest → state → compute → publish → consumer. **`RiskMeasure.source_event_ids[]` carries the lineage** to the inputs that produced each measure: envelope causation_id is single-valued and only meaningful when there's one triggering event; aggregated measures derived from many position-state events use the list. Caller (orchestrator) decides which inputs to attribute — Publisher just propagates. **`UncertaintyAbs` propagates as nil-or-decimal**: a measure with no propagated uncertainty (RISK-08 returned nil) marshals as absent, preserving the api/v1 "nil ⇒ no uncertainty propagated" signal across the wire; a measure with a propagated value marshals as the Decimal. Spurious-zero round-trip avoided. **Two event-type names**: `risk.portfolio.exposure_recomputed` (payload domain.v1.ExposureSet:1), `risk.portfolio.measures_computed` (payload domain.v1.RiskMeasureSet:1). Both follow taxonomy §1 ({domain}.{entity}.{event_type}, past-tense FACTs). **Dimension mapping**: domain.ExposureByInstrument → wire EXPOSURE_DIMENSION_INSTRUMENT (the new enum value), ExposureByCurrency → CURRENCY, ExposureBySector → SECTOR. Domain values without a wire counterpart map to UNSPECIFIED so a future domain-side addition that hasn't yet propagated to the schema still publishes (consumers see UNSPECIFIED per enum-unknown semantics, event-class-rules §3). **6 tests** cover NilProducer rejection, EmitExposure happy-path (envelope validate + payload round-trip + partition_key + FACT idempotency invariant), dimension mapping (3 dimensions), Nil rejection on both methods, EmitMeasures happy-path (source_event_ids propagation + per-measure UncertaintyAbs nil-vs-set), partition_key = portfolio_id contract · `kanz/internal/risk/publish/`, `kanz-schemas/proto/domain/v1/measure.proto`, `kanz-schemas/proto/domain/v1/exposure.proto`

- [x] **RISK-09** Implement scenario / what-if engine — new `kanz/internal/risk/scenario/` package + extension of api/v1 with concrete shock types. **Architectural tension resolved**: RISK-02's arch test forbids outsiders from importing `kanz/internal/risk/scenario/`, but external callers need to CONSTRUCT shocks to drive `Engine.EvaluateScenario`. Resolution — shock value types (`PriceShock`, `ParallelShift`) live in `api/v1/scenario.go` where outsiders can reach them; the scenario package owns the DISPATCH + APPLY (type-asserting on the api/v1 types it knows). Adding a shock = struct in api/v1 + case in scenario dispatch. **`Evaluate(p, shocks, registry) → *MeasureSet`** is a pure function: deep-clones the portfolio, applies shocks in order, returns measures computed against the shocked state. Original portfolio is never mutated — critical because the orchestrator holds RISK-05's per-portfolio lock during Evaluate, and mutation under-lock would falsify subsequent queries in the same lock window. **Two baseline shock types**: `PriceShock` (one instrument, percentage change) and `ParallelShift` (all positions, same percentage). PriceShock against an unheld instrument is a silent no-op so watchlist-wide batches against multiple portfolios succeed regardless of which instruments any given portfolio holds. **Unknown shock types are silent no-ops**: a custom shock the engine doesn't dispatch on must not abort the run — large batches of mixed shock types are best-effort. **`compute.ShockMoney(m, pct) = m × (1 + pct)`** exported as the boundary helper (vs package-private addDecimal/mulDecimal) so scenario reaches the high-level operation without depending on internal arithmetic primitives. **`InstrumentID` lifted to api/v1**: necessary for external scenario-shock construction; `domain.InstrumentID` becomes a type alias (`= v1.InstrumentID`) so the two packages share one canonical identifier with zero conversion. **Decimal representation note in tests**: shocks change exponents through multiplication (1000×10^0 × 90×10^-2 = 90000×10^-2 — same value as 900×10^0, different fields). Tests assert the actual representation, with comments referencing decimal.proto's "1.0 and 1.00 are equal in value but differ in fields" contract. 9 tests cover single-shock + immutability of original + no-op-on-unheld + parallel shift + shock composition (10% then -10% ≠ 0%) + unknown-shock skip + empty list + AsOf propagation + custom registry · `kanz/internal/risk/scenario/`, `kanz/internal/risk/api/v1/scenario.go`

- [x] **RISK-08** Implement uncertainty propagation through measure computations — new `kanz/internal/risk/compute/uncertainty.go`. **Three propagation primitives** keyed off correlation assumption: `PropagateSumIndependent(σ_i...) = √(Σσ_i²)` (uncorrelated inputs, standard textbook), `PropagateSumPerfectlyCorrelated(σ_i...) = Σ|σ_i|` (conservative when correlations unknown), `PropagateScalar(c, σ) = |c|×|σ|` (exact, no assumption — for VaR=0.01×Gross-style measures). **nil-semantics is load-bearing**: per-input nil ⇒ skip; all-nil ⇒ return nil (not zero) so the api/v1 `Measure.UncertaintyAbs` "nil ⇒ no uncertainty propagated" signal stays meaningful — a spurious zero would falsely claim a calibrated zero confidence interval. **Wired into the four baseline measures** (RISK-07): GrossExposure + NetExposure + Delta all propagate via `sumUncertaintyInBaseCurrency` (independent sum of per-position MarketValueUncertainty in BaseCurrency); VaR99 propagates via `PropagateScalar(0.01, σ_gross)`. The independence assumption is the default for cross-position aggregates; a future iteration can offer per-measure mode or a correlation matrix. **Domain extension**: added `MarketValueUncertainty *commonpb.Money` to `domain.Position` — populated from staleness signals (RISK-11) or a future per-instrument uncertainty source (the wire proto `domain.v1.PositionState` does not yet carry a matching field, so engine output has UncertaintyAbs nil today; RISK-08's propagation paths are exercised by tests until a producer ships uncertainty values). **`decimalSqrt` via float64 round-trip**: confidence intervals don't need money-grade precision; fixed output exponent (`uncertaintyExp = -6`) prevents propagation chains from drifting toward extreme exponents; negative input clamps to zero (sqrt of underflow recovers to "no uncertainty" rather than NaN). New helpers `decimalToFloat`/`floatToDecimal` use `math.Round` (not truncation) to minimize round-trip bias. **Currency rule preserved**: uncertainty inputs must be in BaseCurrency to count — same no-FX-in-compute discipline as RISK-06/07. 13 tests cover all three propagation primitives + nil semantics + classic Pythagorean triple (3-4-5) + integration with GrossExposure / VaR99 / Delta / NetExposure + the foreign-currency-skip rule · `kanz/internal/risk/compute/`

- [x] **RISK-07** Implement first concrete risk measure set — new `kanz/internal/risk/compute/measures.go` ships the **registry framework + 4 illustrative measures**. Honest scope: the engine has no historical return time series, no volatility model, no factor model, no per-instrument Greek data — real VaR / sensitivities need all of those. The four measures here (`GrossExposure`, `NetExposure`, `VaR99`, `Delta`) are deliberate placeholders; the architectural deliverable is the **Registry pattern** that lets a quant team plug in real measure implementations later WITHOUT changing the api/v1 contract, the MeasureSet shape, or any caller code. **`MeasureFunc = func(*Portfolio) v1.Measure`** — pure function, no side effects/I/O/errors; missing data signalled by zero-value Measure plus quality flag at response level (not per-measure error branches that complicate the api/v1 surface). **`Registry`** with Register/Names; **`DefaultRegistry()`** seeds the four baseline measures; **`ComputeMeasures(p, registry, filter) → *MeasureSet`** runs registered measures, narrowed by optional filter (unknown filter names silently dropped, matching `MeasureSet.Lookup` miss semantics). **Currency convention**: Measure values are dimensionless `*common.v1.Decimal` (api/v1 carries no currency field); money-valued measures are documented to be in BaseCurrency and only sum positions whose MarketValue currency matches — other-currency positions skipped (degraded-mode signalling is RISK-11's). **VaR99** = 1% × GrossExposure (clearly labelled placeholder); **Delta** = NetExposure (trivial "$1 position = $1 spot delta" placeholder, correct for cash equities, wrong for derivatives). **Quants replace via `r.Register(MeasureVaR99, realImpl)`** — one-line override; no other code changes. **Measure names follow api/v1 doc convention** (`VaR99`, `Delta`, `GrossExposure`, `NetExposure` — short, CamelCase, dimensional suffix); same one-way-door discipline as event_type names since rename breaks every dashboard/alert. **`sumInBaseCurrency(p, abs bool)`** is the single helper backing all four baseline measures — future "what counts as same-currency" change edits one function, not four. **`mulDecimal` added to compute/decimal.go** (coefficient product + exponent sum) — overflow flagged for math/big upgrade at extreme scales, fine for placeholder formulas. 10 tests cover each measure, registry registration + replacement, filter narrowing, unknown filter dropping, name ordering, AsOf propagation · `kanz/internal/risk/compute/`

- [x] **RISK-06** Implement exposure computation — new `kanz/internal/risk/compute/` package; `exposure.go` + `decimal.go` (first compute helpers — every risk computation needs `common.v1.Decimal` arithmetic and proto's representation-only type has no methods). **`ComputeExposure(Portfolio) → *ExposureSet`** emits two dimensions today: `ExposureByInstrument` (one Exposure per position with a MarketValue; Gross = `absMoney(MarketValue)`, Net = MarketValue signed) and `ExposureByCurrency` (positions bucketed by MarketValue.CurrencyCode, Gross/Net summed per bucket). **`ExposureBySector` deliberately deferred**: in the closed `domain.ExposureDimension` enum but needs an instrument→sector reference table the engine does not yet own; a RISK-06b can inject a sector-lookup function later without changing the existing signature. **No cross-currency conversion**: bucketing on currency first makes per-bucket sums well-defined without an FX layer; cross-currency aggregation needs an FX-conversion layer outside compute. **Skip semantics**: positions with nil MarketValue are skipped (no mark yet means no expressible exposure); zero-quantity positions retained for audit lineage flow through with zero MarketValue producing zero-row exposures (informative — position still exists — without distorting aggregates). **AsOf propagated** from Portfolio so api/v1 callers see the freshness of underlying state. **Decimal helpers** (`addDecimal`/`negateDecimal`/`absDecimal`/`zeroDecimal` + Money wrappers): exponent alignment to the more precise operand (1.50 + 0.025 = 1.525 = 1525×10^-3); nil operands treated as zero for accumulator initialization; `addMoney` requires matching currency codes (caller's responsibility); `pow10` is unguarded — bounded by typical exponent differences in financial data, switch to math/big if RISK-07/08 ever hit extreme scales. 8 exposure tests cover empty, single position, short (signed Net + absolute Gross), same-currency aggregation, mixed currencies, nil-MarketValue skip, exponent alignment, AsOf propagation; 7 decimal tests cover sum + align + nil + negate + abs + money-nil-handling + pow10 · `kanz/internal/risk/compute/`

- [x] **RISK-05** Implement per-aggregate ordered, idempotent state application — new `kanz/internal/risk/state/` package, two files (`store.go`, `dedup.go`) + tests. **Store** holds three lazy-created maps keyed by PortfolioID: `portfolios` (`*domain.Portfolio`), `locks` (per-portfolio `*sync.Mutex`), `dedup` (per-portfolio `dedupWindow`). The map-wrapping mutex (`Store.mu`) is held briefly only for lookup-or-create; per-portfolio locks then serialize the actual apply. **Per-aggregate ordering primitive**: each portfolio's mutex is the cheapest primitive that gives cross-event-type serialization. The bus only guarantees per-partition ordering (event-class-rules §1), but a portfolio receives PortfolioState on one partition and PositionState on others — per-aggregate ordering across all of them is the engine's correctness invariant; alternatives (per-aggregate goroutine + channel) add complexity without changing the contract. **Per-aggregate dedup window** (defense-in-depth alongside the bus.Consumer's global window EVT-17d): 2m TTL / 10k entries (matching the bus defaults so layers reinforce). Catches duplicates that arrive via different Consumers feeding the same Applier, and survives bus dedup-window loss on restart. Per-aggregate scoping (not global) matches event-class-rules §1 and avoids global-map contention. **Snapshot semantics** (event-class-rules §3, latest-wins-by-event-time): hard reset — clear positions, apply snapshot's positions, set aggregate fields, record `LogPosition` for RISK-04 bootstrap. Stale snapshot (older than current portfolio.AsOf) is silently skipped + dedup-recorded (it's a real envelope we've decided not to apply). **Lazy portfolio creation** with empty BaseCurrency: a PositionState arriving before its PortfolioState lazy-creates the portfolio; the first PortfolioState or PortfolioSnapshot fills BaseCurrency. The alternative ("reject unknown portfolios") would reject many valid sequences during rolling deployments. **`Lookup` returns the live pointer** (not a clone) — internal contract for compute layers (RISK-06/07) which read under appropriate locks; the api/v1 surface will clone before returning to external callers. **Domain extension**: extended `domain.Portfolio` with `displayName`/`cashBalance`/`totalMarketValue`/`positionCount` (mirroring PortfolioState aggregate fields) + `SetAggregate(AggregateUpdate)` bulk setter + `ClearPositions()` snapshot-reset hook; extended `domain.Position` with `MarketValue`/`RealizedPnL`/`UnrealizedPnL` and fixed `AveragePrice` type from `Money` → `Decimal` to match the proto. **`tsTime` nil-safety**: uses concrete `*timestamppb.Timestamp` (not an `AsTime() time.Time` interface) — calling AsTime on a typed-nil pointer returns Unix epoch, not Go zero time, which would falsely look like "state applied" to AsOf checks. **Compile-time `var _ ingest.Applier = (*Store)(nil)`** catches contract drift at build time. 8 tests cover happy-path (3 event types), missing aggregate id, idempotency (same key blocked / distinct keys both applied), snapshot hard reset, stale snapshot rejection, concurrent applies to different portfolios (parallel), concurrent applies to same portfolio (serialized, no lost updates) · `kanz/internal/risk/state/`, `kanz/internal/risk/domain/portfolio.go`
- [x] **RISK-04** Implement state ingestion consumer — new `kanz/internal/risk/ingest/` package. **Single-responsibility design**: Ingestor is the bus→engine bridge that takes `bus.EventHandler`-shaped deliveries (envelope + payload bytes — already validated by `bus.Consumer`), discriminates by `env.EventType`, unmarshals into the right `domain.v1` proto, and dispatches to an `Applier` interface (RISK-05 will implement). Three event types routed: `risk.portfolio.revalued` (→ `domain.v1.PortfolioState`), `risk.position.changed` (→ `PositionState`), `risk.portfolio.snapshot` (→ `PortfolioSnapshot` STATE_SNAPSHOT). **Scope-split with RISK-05 is load-bearing**: Ingestor does pure routing — no translation, idempotency, or ordering, because those need per-aggregate state RISK-05 owns; doing them here would split state across two packages and make the per-aggregate guarantee impossible. **Applier takes proto directly** (not pre-translated domain types): RISK-05 needs envelope metadata for dedup/ordering plus the typed payload, and translating to domain here would force Applier to receive both — simpler to let RISK-05 own the proto→domain mapping. **Applier interface lives in ingest, not state**: RISK-04 lands before RISK-05 (interface declared here, satisfied there) so tests + the orchestrator can wire a fake. **Live-only consumer**: REPLAYED-flagged events are already rejected by `bus.Validate` before reaching Handler (EVT-20c); any future replay-scoped state consumer constructs a separate Ingestor wired to `bus.WithValidator(bus.ValidateReplay)`. **Unknown event_type surfaces as `ErrUnknownEventType`** (errors.Is-able) → bus retry+DLQ picks it up; misconfiguration must be loud, not silently dropped. Six unit tests cover happy-path dispatch (3 event types), unknown event_type rejection, unmarshal error surfacing, and applier error surfacing — all via a 4-line fakeApplier · `kanz/internal/risk/ingest/`
- [x] **RISK-03** Define core domain model types — new `kanz/internal/risk/domain/` package, split across `portfolio.go` (Portfolio + Position + InstrumentID + CurrencyCode), `exposure.go` (Exposure + ExposureDimension + ExposureSet), `measure.go` (MeasureSet wrapping api/v1's Measure value type). **Two-layer separation from kanz-schemas proto types** (domain.v1.PortfolioState etc.): proto is the WIRE shape (serialization, evolution, cross-language); domain is the engine's WORKING in-memory shape (fast access, immutability of snapshots, natural Go ergonomics). RISK-04 will ingest proto → construct domain; RISK-10 will serialize domain → proto for publishing. Cost of duplication = ability to evolve either side without churning the other. **Implements the api/v1 opaque interfaces**: `*ExposureSet` satisfies `v1.ExposureSet` (AsOf), `*MeasureSet` satisfies `v1.MeasureSet` (AsOf + Lookup). Compile-time interface assertions (`var _ v1.ExposureSet = (*ExposureSet)(nil)`) catch any contract drift at build time. **Immutability discipline**: every accessor returns a fresh slice/map copy (Positions(), Items(), Names()) so the api/v1 caller cannot mutate the engine's internal storage; RISK-05's state-apply layer is the single writer. **Money/sizes/prices are common.v1 types** (no `float64`/`double` per BRAIN's banned-double rule): Quantity is `*common.v1.Decimal`, AveragePrice + Exposure.Gross/Net are `*common.v1.Money`. **Closed ExposureDimension enum for v1**: ExposureByInstrument (canonical), ExposureByCurrency, ExposureBySector — new dimensions take next constant, existing values never repurposed (mirrors envelope-policy §4 field-number discipline). **Position retention**: zero-quantity positions are kept for audit lineage; explicit removal only via Portfolio.Forget when a state event retires the instrument. **Snapshot bootstrap hook**: Portfolio.SetSnapshot records the `*common.v1.LogPosition` from the most recent snapshot so RISK-04's bootstrap path knows where to resume the log (event-class-rules §3) · `kanz/internal/risk/domain/`

- [x] **RISK-02** Add import-boundary architecture test — new `kanz/test/arch/risk_boundary_test.go` (first tenant of `kanz/test/arch/`, the convention future modules' arch tests will follow). Build-time enforcement of the RISK-01 contract: shells out to `go list -json ./...` from the module root (resolved via `go env GOMOD` → `filepath.Dir`), streams the per-package JSON via `json.Decoder`, walks `Imports + TestImports + XTestImports` (test code is held to the same rule — a test-only consumer of an impl package is still a hidden coupling). Two rules: `TestRiskBoundary_OutsidersUseAPIOnly` (any package outside `kanz/internal/risk/` may import only `kanz/internal/risk/api/v*`; impl sub-packages — compute, state, ingest, scenario, publish, degraded, domain — are private), `TestRiskBoundary_APIDoesNotImportImpl` (api/v* must not depend on impl sub-packages — a contract leak that would defeat the api-as-narrow-surface design). **Why `go list` over a third-party arch-lint library**: zero new deps, runs as a regular `go test`, output is structured JSON suitable for streaming. **Why direct + test imports both**: a hidden test-only import past the boundary creates a phantom consumer that breaks the moment the impl symbol is renamed — exactly the kind of slow rot the arch test exists to prevent. **Land-before-impl is OK**: with only `api/v1` shipped today (RISK-01) and no impl packages yet, both tests pass trivially — they become enforcing as RISK-04..11 land. The architecture-test pattern (`pkgInfo` struct, `loadPackages` helper, `allImports` helper) is reusable for future modules (PRED, DATA) which will add sibling `*_boundary_test.go` files under the same package · `kanz/test/arch/`
- [x] **RISK-01** Define risk-engine module interface — first tenant of `kanz/internal/` (greenfield; layout convention follows BRAIN's "promote to top-level when 2nd consumer appears" rule). New `kanz/internal/risk/api/v1/engine.go` is the **narrow external surface** module siblings (orchestrator, HTTP API, scenario tooling, degraded-mode probe) call through — never through `compute/`, `state/`, `ingest/` directly (RISK-02 wires the architecture test that enforces this). **Query + scenario + health only**, no Apply/Mutate/Command — per the anti-decision "all state changes must be event-driven", state arrives via the bus (RISK-04) and risk outputs leave via the bus (RISK-10); the api/v1 surface is read-side + scenario only. **Engine interface, 4 methods**: `Exposure(ExposureRequest) → ExposureResponse`, `Measures(MeasuresRequest) → MeasuresResponse`, `EvaluateScenario(ScenarioRequest) → ScenarioResponse`, `Health() → Health`. **Versioning under envelope-policy discipline**: additive-only within v1, breaking changes cut a sibling `v2/`; new method on Engine only when every plausible implementation can supply a non-degraded answer (else it goes on a separate interface). **Type-surface trade-offs**: rich domain types (`ExposureSet`, `MeasureSet`, `ScenarioShock`) declared as minimal-method interfaces (e.g. `ExposureSet.AsOf()`, `MeasureSet.Lookup(name)`) — RISK-03 provides concrete impls, api/v1 keeps surface narrow so domain types can evolve without churning callers; `Measure.Value` uses `*common.v1.Decimal` directly (already a transitive dep via event ingestion, banned-double rule from BRAIN, avoids string→decimal parsing at every read site); QualityFlag is api-local string enum (DEGRADED, STALE) rather than the envelope QualityFlag proto so api/v1 doesn't tangle with envelope-versioning churn for purely Go-API concerns. **Degraded mode surfaced two ways**: explicit Mode on `Health()` + per-response QualityFlags — callers cannot accidentally consume degraded data without seeing the signal. **Sentinel errors**: `ErrPortfolioNotFound`, `ErrInvalidRequest` (transport/runtime errors are distinct so callers branch correctly) · `kanz/internal/risk/api/v1/engine.go`

### Event System

- [x] **EVT-21d** Contract tests: replay determinism (same log → identical output) — new `kanz/test/contract/replay/determinism_test.go` proves the runtime claim from schema-evolution.md §8 that ties the EVT-20 replay pipeline to the EVT-21c compat contract. **Wire-determinism**: two Pipeline runs over the same fresh source-log slice (canonicalLog rebuilds per call so each run gets independent envelope pointers — Pipeline mutates `QualityFlags` in place via `StampReplayed`) with the same RunID produce byte-identical `bus.Message` captures (Subject, Key, Body, Headers); a regression introducing nondeterminism (per-run wall-clock metadata, map-field reordering, etc.) fails loudly. **RunID-orthogonality**: same log with different RunIDs produces identical Body + Key + Headers, differing only in Subject — proves the RunID is the only run-scoped wire input and isolates the determinism property from accidental coupling. **Replay-of-replay convergence**: feed Run1's captured output back as Run2's source (via Unframe → Event), with the same RunID — outputs converge byte-identically because `StampReplayed`'s idempotency (`IsReplayed` early return) means the chained run does not re-stamp. **Envelope identity preservation**: clone every source envelope pre-run via `proto.Clone`, run Pipeline, Unframe each output, assert every identity field (`EventId`, `IdempotencyKey`, `ProducerSequence`, `CorrelationId`, `CausationId`, `Source`, `ProducerVersion`) is byte-equal to the cloned original — only `QualityFlags` is permitted to differ, and only by adding exactly one `QUALITY_FLAG_REPLAYED`. **Compat-during-replay link**: simulate a future publisher's event landing in an old Kafka log by appending an unknown varint field at tag 1000 to envelope wire bytes, unmarshal so the unknown field lands in `ProtoReflect().GetUnknown()`, replay the event, Unframe the output, assert the unknown field is preserved — proves the §8 link that replay determinism depends on proto3 unknown-field preservation, the exact property EVT-21c tested in isolation. Tests sit in `package replay_test` (external) so they consume only the public `tools/replay` + `pkg/bus` surface, matching the other EVT-21 contract trees · `kanz/test/contract/replay/`
- [x] **EVT-21c** Contract tests: schema compatibility both directions — new `kanz/test/contract/compatibility/envelope_compat_test.go` proves the proto3 wire-runtime invariants the envelope-policy + schema-evolution docs depend on, complementing two static gates that cannot prove live decoder behavior: `buf breaking` (EVT-07, schema-text level) and CODEOWNERS review (semantic). **Forward compatibility** (older code reads newer bytes): an unknown field appended via `protowire.AppendTag/AppendVarint` at field number 1000 (well above the 1–19 currently allocated; outside the 19000–19999 reserved range) survives unmarshal — verified via `m.ProtoReflect().GetUnknown()` containing the tag — and re-marshal preserves it (scanned via `protowire.ConsumeField`); the same property is asserted at the EventFrame layer for `bus.Unframe` (with the deliberate scope note that replay re-marshals from `Envelope + payload` and so drops EventFrame-level unknowns on egress — only the "doesn't reject" half of the contract holds there); `bus.Validate` stays indifferent to unknown fields so a future publisher's new field cannot synthetically fail a current consumer's validator. **Backward compatibility** (current code reads older bytes): proto3 wire-omits zero-valued scalars, so an envelope marshaled with `CausationId/TraceContext/QualityFlags` unset is wire-indistinguishable from a v0 producer that never had them — asserted by round-trip + a wire-byte equality check that an explicitly-zero envelope and an unset envelope produce identical bytes under `Deterministic: true`. **Payload opacity**: `EventFrame.payload` round-trips identically for nil, empty, ASCII, arbitrary high-bit bytes, large blobs, parseable Timestamps, and Timestamps with appended unknown fields — proving the payload-blind tooling promise. **Payload-side forward/backward compat** using `timestamppb.Timestamp` as the stand-in domain payload: a v2 timestamp (Timestamp + unknown field 99) decoded by a v1 Timestamp binding preserves the unknown field through marshal + re-marshal; a v0 timestamp (only `seconds` set in raw wire bytes) decodes with `nanos=0` · `kanz/test/contract/compatibility/`
- [x] **EVT-21b** Contract tests: serialization round-trip parity (Go/Python/TS) — Go is the canonical generator: `kanz/test/contract/serialization/fixtures.go` defines the catalog (6 fixtures covering FACT root + lineage variants, COMMAND with caller-set idempotency_key, STATE_SNAPSHOT, OBSERVATION with no partition_key, OBSERVATION with a repeated QualityFlag), `cmd/genfixtures/main.go` marshals each via `proto.MarshalOptions{Deterministic: true}` and writes `<name>.bin` + `manifest.json` (language-agnostic, snake_case, enums as string names, timestamps split into seconds/nanos so any language can compare without an RFC-3339 parser). Three parity test suites load the same `.bin` + `manifest.json` and assert byte-identical payload + field-identical envelope: Go (`parity_test.go`) runs both in-memory parity and a drift-guard test that fails with the regen command if committed fixtures diverge from `BuildAll()`; Python (`kanz-py/tests/test_serialization_parity.py`) and TS (`kanz/ts/kanz-bus/test/serialization-parity.test.ts`) load via repo-relative paths, skip cleanly with regen hint if fixtures absent. Each language uses its native enum/timestamp APIs (Python `EventClass.Value`, TS `EventClass[name]`, Go `.String()`) so the contract is wire-form, not Go-specific. Every fixture also passes the EVT-21a `Validate` contract after the round-trip — fixtures cannot ship invalid envelopes · `kanz/test/contract/serialization/`, `kanz-py/tests/`, `kanz/ts/kanz-bus/test/`
- [x] **EVT-21a** Contract tests: envelope invariants — new `kanz/test/contract/envelope/` tree sits ABOVE the bus package and treats it as a black box: drives `Producer.Publish` through the real stamp → frame → wire path, then `Unframe` + `Validate` on the captured wire bytes. Tests are the wire-level spec (not internals): (1) end-to-end publish round-trip passes Validate + carries the `Nats-Msg-Id` broker-dedup header (EVT-17d); (2) every required field from envelope-policy §7 is rejected on receive — hand-framed bad envelopes assert what a misbehaving (non-Go, future-language, or buggy) publisher's wire bytes look like and that the consumer-side rejection holds; (3) `Unframe` rejects malformed bytes and frames missing the envelope (downstream nil-deref guarantee); (4) per-class invariants on the wire: FACT round-trip preserves idempotency_key == event_id; FACT mismatch rejected; COMMAND round-trip preserves caller-supplied idempotency_key and may differ from event_id; STATE_SNAPSHOT/OBSERVATION accepted with matching keys (only FACT carries the explicit mismatch rule per event-class-rules §1); (5) live vs replay sink contract: `Validate` rejects REPLAYED, `ValidateReplay` requires REPLAYED. Tests use the same `captureClient` + `validEnvelope` patterns as `kanz/pkg/bus` unit tests, but in the external `envelope_test` package so they exercise only the public surface · `kanz/test/contract/envelope/`
- [x] **EVT-01** Initialize kanz-schemas repo — `buf.yaml`, `proto/` + `docs/` skeleton, `CODEOWNERS` per subtree · `kanz-schemas/`
- [x] **EVT-02** Define Envelope proto — `envelope.proto` (19 fields + `QualityFlag`), `event_class.proto` enum; `buf build` + `buf lint` pass · `kanz-schemas/proto/envelope/v1/`
- [x] **EVT-03** Write envelope versioning & field-inclusion policy doc · `kanz-schemas/docs/envelope-policy.md`
- [x] **EVT-04** Write per-class handling rules doc · `kanz-schemas/docs/event-class-rules.md`
- [x] **EVT-05** Write subject/topic taxonomy spec · `kanz-schemas/docs/subject-taxonomy.md`
- [x] **EVT-06** Write schema evolution rules doc — field-number discipline, version-bump triggers, compatibility direction · `kanz-schemas/docs/schema-evolution.md`
- [x] **EVT-07** Set up CI breaking-change + lint gate (`buf lint`, `buf breaking`); block PR on failure · `kanz-schemas/.github/workflows/schema-ci.yml`
- [x] **EVT-08** Provision NATS JetStream — k8s manifests (3-node JetStream cluster), idempotent stream/consumer bootstrap Job, pub/sub smoke test · `kanz/infra/nats/`
- [x] **EVT-09** Provision Kafka — k8s manifests (3-node KRaft cluster), idempotent topic/retention/compaction bootstrap Job, produce/consume smoke test · `kanz/infra/kafka/`
- [x] **EVT-10** Define market data payload schemas — `MarketDataEvent` + `MarketDataBatch` (Trade/Quote/Bar oneof); new shared `common.v1.Decimal` · `kanz-schemas/proto/market/v1/`, `kanz-schemas/proto/common/v1/`
- [x] **EVT-11** Define domain/state payload schemas — `PortfolioState`, `PositionState`, `PortfolioSnapshot`, `ExposureState`, `ExposureSet`; new shared `common.v1` `Money` + `LogPosition` · `kanz-schemas/proto/domain/v1/`, `kanz-schemas/proto/common/v1/`
- [x] **EVT-12** Define command + command-outcome payload schemas — generic `CommandMetadata` (embedded by concrete commands) + universal `CommandOutcome` / `CommandOutcomeStatus` · `kanz-schemas/proto/command/v1/`
- [x] **EVT-13** Define lifecycle payload schemas — `ModelDeployed`, `ConfigChanged`, `ModeChanged` (+ `DeploymentRole`, `OperatingMode` enums) · `kanz-schemas/proto/lifecycle/v1/`
- [x] **EVT-14** Define observation + data-quality payload schemas — `MetricObservation`, `DecisionLog`; `DataQualityEvent` (gap/staleness/drift oneof) + `Severity` · `kanz-schemas/proto/observation/v1/`
- [x] **EVT-15a** Codegen: `buf.gen.yaml` + Go plugin (managed-mode `go_package`); tag-triggered `schema-release.yml` publishes the Go module to companion repo `kanz-schemas-go` · `kanz-schemas/buf.gen.yaml`
- [x] **EVT-15b** Codegen: Python plugins (`protocolbuffers/python` + `pyi` remote); `schema-release.yml` `python` job builds the `kanz-schemas` package from `packaging/python/pyproject.toml` and publishes to the package index · `kanz-schemas/buf.gen.yaml`
- [x] **EVT-15c** Codegen: TS plugin (`bufbuild/es` remote, `target=js+dts`); `schema-release.yml` `ts` job builds the `@kanz-eng/kanz-schemas` package from `packaging/ts/package.json` and publishes to the npm registry · `kanz-schemas/buf.gen.yaml`
- [x] **EVT-16a** Schema registry: service skeleton (stdlib HTTP, `/healthz` + `/readyz`, slog, pgxpool) + storage backend (`Storage` interface with idempotent `Put`/`Get`/`Ping`, Postgres impl + in-memory fake for tests, `schemas` table keyed on `(schema_id, version)`) · `kanz/services/schema-registry/`
- [x] **EVT-16b** Schema registry ingest: `Storage.Register` (server-assigned version, idempotent on sha256 fingerprint, serializable tx in Postgres for atomic latest+1) + `POST /schemas/{schema_id}` HTTP endpoint. CI client `kanz-schemas/tools/registry-ingest/` reads the `buf build` FileDescriptorSet, slices a per-message subset (file + transitive imports), POSTs each. New `registry-ingest` job in `schema-release.yml` runs on tag push · `kanz/services/schema-registry/`, `kanz-schemas/tools/registry-ingest/`
- [x] **EVT-16c** Schema registry resolve: `GET /schemas/{ref}` returning raw stored descriptor with metadata in `X-Schema-*` headers + `ETag` and `Cache-Control: public, max-age=31536000, immutable` (rows are immutable once published); `404` for unknown refs, `400` for malformed; httptest-based tests cover happy path / 404 / malformed / version-0 / non-numeric version. Register handler now rejects `schema_id` containing `:` so resolve URLs stay unambiguous · `kanz/services/schema-registry/`
- [x] **EVT-17a** Go bus client foundation: `kanz/pkg/bus` package with `Message {Subject, Key, Body, Headers}` and unified `Client` interface (`Publisher` + `Subscriber` + `Close`). `NATSClient` over `nats.go` + JetStream v2 (durable AckExplicit consumer per (subject, group)); `KafkaClient` over `segmentio/kafka-go` (Writer with Hash balancer + RequireAll + Snappy + `AllowAutoTopicCreation=false`; per-(topic, group) Reader with manual per-message commit). `Kanz-Partition-Key` header bridges `Message.Key` over NATS (stripped from user-visible Headers on receive). Unit tests for config validation; integration tests gated on `TEST_NATS_URL` / `TEST_KAFKA_BROKERS` provision their own ephemeral stream/topic · `kanz/pkg/bus/`
- [x] **EVT-17b** Go bus client stamping + validation: `Producer` (over a `Client`) stamps auto-fields (event_id UUIDv7, publish_time, ingestion_time default-to-now, correlation_id default-to-event_id for roots, idempotency_key derivation per event-class rules, per-(event_type, partition_key) monotonic producer_sequence, source/producer_version from `ProducerConfig`, envelope_version=1). New `envelope.v1.EventFrame {Envelope envelope = 1; bytes payload = 2}` proto defines the wire frame so Envelope stays metadata-only and payload-blind tooling can deserialize the Envelope without the payload schema. `Validate` enforces every required-field rule from envelope-policy §7 + event-class-rules (FACT idempotency_key == event_id; live publish rejects QUALITY_FLAG_REPLAYED; producer_sequence==0 when partition_key empty). `Unframe` is the consumer-side counterpart. Table-driven tests across stamping behaviour, sequence semantics, and every validation rule · `kanz/pkg/bus/`, `kanz-schemas/proto/envelope/v1/event_frame.proto`
- [x] **EVT-17c** Go bus client lineage propagation: `Consumer` wraps a `Subscriber`, unframes + validates each inbound message, and stashes `correlation_id`, `event_id` (as the next event's `causation_id`), and `trace_context` onto the handler's `context.Context` via `With*`/`*FromContext` helpers. `Producer.stamp` reads those fields when the corresponding `Event` field is empty (precedence: explicit > ctx > root default). End-to-end test asserts a Consumer→Producer chain inherits all three fields; unit tests cover unframe failure, validation failure, handler-error surfacing, empty-trace skip, and explicit-beats-ctx precedence · `kanz/pkg/bus/`
- [x] **EVT-17d** Go bus client dedup: two reinforcing layers. **Publish side** — Producer stamps `Nats-Msg-Id: <idempotency_key>` on every outbound `bus.Message`; NATS JetStream uses this for broker-side dedup within its 2m window (EVT-08), Kafka carries it as a benign user header. **Consumer side** — bounded sliding `DedupWindow` (`Seen`/`Record`, 2m / 10k default; functional option `WithDedupWindow` to override or disable). Pipeline: `Unframe → Validate → Seen ⇒ skip ack` else `dispatch → Record on success`; failure leaves the window untouched so retries work. Methods are nil-safe so the zero-config (disabled) path needs no nil-checks. Tests cover Seen/Record split, TTL expiry, capacity eviction (oldest first), nil-safety, dedup-on-redelivery, no-record-on-handler-failure, dedup-disabled, distinct-keys-distinct-dispatch, and publish-side header stamping · `kanz/pkg/bus/`
- [x] **EVT-17e** Go bus client retry + DLQ: `RetryConfig {MaxAttempts, InitialBackoff, MaxBackoff}` drives an in-handler retry loop with exponential backoff (capped, ctx-cancelable; no jitter at this layer). `WithRetry(cfg)` and `WithDLQ(Publisher)` are independent options — either composable on its own. Terminal failures (retry exhausted, or `Unframe` / `Validate` failure before dispatch) republish the original message to `dlq.<original-subject>` with `Kanz-DLQ-{Original-Subject,Attempts,Error}` headers attached and the original wire body/headers preserved, then ack. DLQ-publish failure surfaces so the broker holds the message. DLQ recording into the dedup window treats DLQ as terminal so subsequent duplicates skip. Tests cover retry-until-success, retry-then-surface (no DLQ), retry-then-DLQ, unframe→DLQ, validate→DLQ, DLQ-records-dedup, ctx-cancel-during-backoff, and DLQ-publish-failure surfaces · `kanz/pkg/bus/`
- [x] **EVT-18a** Python bus client foundation: new `kanz-py` package (`kanz-bus` on PyPI). `kanz_bus.bus` defines `Message`, `Handler`, and `Publisher`/`Subscriber`/`Client` Protocols mirroring the Go shape. `NATSClient` over `nats-py` (JetStream pull-subscribe loop with timeout-retry, durable per (subject, group), handler exception ⇒ `nak`); `KafkaClient` over `aiokafka` (Producer `acks="all"` + Snappy, per-call Consumer with `enable_auto_commit=False` and manual commit on success). Async-first API with `async with` context-manager lifecycle and `asyncio.CancelledError` as the cancel signal. `Kanz-Partition-Key` header bridges `Message.key` over NATS (stripped from user-visible headers on receive) — same wire format as Go. Unit tests for config + lifecycle; integration tests gated on `TEST_NATS_URL` / `TEST_KAFKA_BROKERS` provision their own ephemeral stream/topic · `kanz-py/`
- [x] **EVT-18b** Python bus client stamping + validation: `Producer` (over a `Client`) + `ProducerConfig{source, producer_version}` + `Event` dataclass mirror the Go EVT-17b shape — wire-identical `EventFrame` framing, same `Nats-Msg-Id` header, same per-(event_type, partition_key) monotonic `producer_sequence`. Inline UUIDv7 implementation (Python 3.13's stdlib `uuid.uuid7` is too new for the 3.10+ floor; ~10 lines of bit-packing avoids an extra dep). `validate(envelope)` enforces every required-field rule from envelope-policy §7 + the FACT idempotency-key invariant + live-publish rejection of `QUALITY_FLAG_REPLAYED`. `unframe(body)` is the consumer-side helper. New deps: `kanz-schemas>=0.1.0`, `protobuf>=5.29`. Parameterized validate tests cover every required field; producer tests cover stamping, sequence semantics, COMMAND idempotency, FACT mismatch rejection, None-event-time/payload rejection, and broker-dedup-header stamping · `kanz-py/`
- [x] **EVT-18c** Python bus client lineage propagation: lineage rides `contextvars.ContextVar` (PEP 567) — the Python analog of Go's `context.Context`. `kanz_bus.propagation` exposes `propagation_context(*, correlation_id="", causation_id="", trace_context="")` (sync context manager that sets non-empty fields and resets via the contextvars Token on exit, even in async code) and three `get_*` readers. `Consumer` wraps a `Subscriber`, unframes + validates each delivery, then stashes `env.correlation_id`, `env.event_id` (as the *next* event's causation_id), and non-empty `env.trace_context` for the handler's duration — so `Producer.publish` inside the handler auto-inherits via the contextvars. `EventHandler = Callable[[Envelope, bytes], Awaitable[None]]` has no explicit ctx arg (idiomatic Python; lineage is implicit). `Producer.stamp` precedence is explicit Event field > contextvar > root default. End-to-end Consumer→Producer chain test asserts inheritance; unit tests cover contextvar set/reset/nest semantics, empty-arg non-overwriting, asyncio.create_task copy semantics, and unframe/validate failure surfacing · `kanz-py/`
- [x] **EVT-18d** Python bus client dedup + retry + DLQ: combines Go EVT-17d+17e into one subtask per the Python plan. `DedupWindow{ttl_seconds, max_entries}` (default 2m/10k, `ttl_seconds<=0` ⇒ disabled-mode no-op instance — Python's nil-safe-method analog). `RetryConfig{max_attempts, initial_backoff_seconds, max_backoff_seconds}` exponential backoff capped at `max_backoff_seconds`; ctx-cancelable via `asyncio.sleep`. `ConsumerConfig{dedup_window, retry, dlq}` dataclass — composable; any field omitted defaults to the safe value. Pipeline: `unframe → validate → seen ⇒ skip ack` else `retry-loop(handler) → record on success / DLQ-then-record on exhausted`. Pre-dispatch failures (`unframe`/`validate`) route to DLQ with `Kanz-DLQ-Attempts: 0`. DLQ subject = `dlq.<original>`, `Kanz-DLQ-{Original-Subject,Attempts,Error}` headers + original body preserved. DLQ publish failure surfaces (broker holds the message). `asyncio.CancelledError` propagates out of the retry loop without being swallowed by `except Exception`. Tests: dedup seen/record + TTL + eviction + disabled-mode; retry backoff math; consumer retry-until-success / surface-after-retry / route-to-DLQ-after-retries / unframe→DLQ / validate→DLQ / dedup-on-redelivery / no-record-on-failure / dedup-disabled / DLQ-records-dedup / DLQ-publish-failure-surfaces · `kanz-py/`
- [x] **EVT-19a** TS bus client foundation: new `kanz/ts/kanz-bus/` package (`@kanz-eng/kanz-bus` on npm). NodeNext ESM, strict TS 5.6, `node:test` via `tsx` (zero runtime test deps). `src/bus.ts` defines `Message {subject, body, key?, headers?}`, `Handler = (Message) => Promise<void>`, and `Publisher`/`Subscriber`/`Client` interfaces mirroring the Go/Python shape. `NATSClient` over `nats@2` (JetStream consumer via `js.consumers.get(stream, group)` with idempotent durable-consumer upsert at AckExplicit, modern `consume()` async-iterator loop, handler throw ⇒ `nak`); `KafkaClient` over `kafkajs@2` + `kafkajs-snappy` (single shared `Producer` with `allowAutoTopicCreation: false` + `acks: -1` + Snappy compression matching the Go writer; per-Subscribe `Consumer` with `autoCommit: false` and manual per-message `commitOffsets` of `offset+1` on success). `AbortSignal` is the TS analog of Go ctx / Python `CancelledError` — `subscribe` resolves when signal aborts. `Kanz-Partition-Key` header bridges `Message.key` over NATS (stripped from user-visible headers on receive) — wire-identical to Go/Python. Unit tests for config validation, default application, lifecycle (`publish` before `connect` throws, `close` without `connect` is a no-op) · `kanz/ts/kanz-bus/`
- [x] **EVT-19b** TS bus client stamping + validation: `Producer` (over a `Client`) + `ProducerConfig{source, producerVersion}` + `Event` interface mirror Go EVT-17b / Python EVT-18b — wire-identical `EventFrame` framing, same `Nats-Msg-Id` header, same per-(event_type, partition_key) monotonic `producerSequence` (`bigint`, since uint64 in Protobuf-ES v2 surfaces as bigint). New deps: `@bufbuild/protobuf@^2.2.3` + `@kanz-eng/kanz-schemas@^0.1.0`. Inline UUIDv7 via `node:crypto`'s `randomBytes` (~10 lines) — `crypto.randomUUID` is v4-only as of Node 24. `validate(env)` throws `Error` (TS idiom) where Go returns error / Python raises ValueError — same rules, parallel messages. `unframe(body)` deserializes the consumer-side `EventFrame`. `PayloadWithSchema<Desc>` bundles a protobuf descriptor + its `MessageShape<Desc>` so Producer.publish can serialize the payload with the right schema while preserving type safety. Minimal `propagation.ts` ships the three getters (`getCorrelationId/CausationId/TraceContext`) backed by `AsyncLocalStorage`; the setter API + Consumer integration land in EVT-19c. Producer.publish currently auto-inherits from any pre-populated async-store, falling back to root defaults. Tests: full Python-mirror suite for validate (canonical + every required field + FACT-mismatch + REPLAYED + sequence-without-pk + COMMAND-with-caller-key); producer (root-stamping, explicit causation, sequence-per-pk, sequence-zero-without-pk, COMMAND-requires-idempotency, COMMAND-preserves-caller-key, FACT-rejects-mismatch, missing-eventTime, missing-payload, constructor validation, Nats-Msg-Id stamping, root correlation defaults to event_id); uuidv7 canonical-form/sortability/distinctness · `kanz/ts/kanz-bus/`
- [x] **EVT-20c** Replay: `QUALITY_FLAG_REPLAYED` stamping + live-sink hard-reject. **Producer side**: new `kanz/tools/replay/replayed.go` with idempotent `StampReplayed(env)` + `IsReplayed(env)` predicate; Pipeline (EVT-20b) now calls `StampReplayed(ev.Envelope)` once per event before re-marshaling the EventFrame, so the wire bytes carry the flag. Idempotent so replay-of-a-replay (degenerate but possible) does not double-stamp. **Consumer side**: refactored `bus.Validate` — extracted `validateEnvelopeFields` (unexported, all field/class checks) + split the REPLAYED guard in two: `rejectReplayed` (live mode) and `requireReplayed` (replay mode). `bus.Validate` = fields + rejectReplayed (existing live-path behavior preserved; an event flagged REPLAYED that leaks past namespace isolation is hard-rejected at the consumer boundary → DLQ if `WithDLQ`, surface otherwise). `bus.ValidateReplay` = fields + requireReplayed — replay-scoped consumers reject *un-flagged* events because an un-flagged event on a replay subject means a publisher skipped 20c stamping (defect, not data). New `bus.WithValidator(fn)` ConsumerOption swaps the per-message validator; replay-scoped consumers use `bus.WithValidator(bus.ValidateReplay)`. Tests: validate (ValidateReplay requires flag / accepts flagged / still enforces field rules), consumer (default validator rejects REPLAYED + never dispatches / WithValidator(ValidateReplay) accepts + dispatches / WithValidator(ValidateReplay) still rejects un-flagged), replayed (IsReplayed nil/empty/flagged, StampReplayed idempotency, preserves other flags, nil-safe), pipeline (every published message carries REPLAYED, stamping idempotent over pre-flagged source). EVT-20 epic complete: replay is now end-to-end runnable — Reader (20a) → Pipeline namespace + group isolation (20b) → flag stamping + bidirectional validator switch (20c) · `kanz/tools/replay/`, `kanz/pkg/bus/`
- [x] **EVT-20b** Replay: isolated consumer-group / `replay.{run_id}.*` namespace wiring. `RunID` is a UUIDv7 hex-encoded as 32 lowercase chars (single subject segment, no dash-escaping on either transport) — time-sortable so operator audit trails sort chronologically. `SubjectFor(runID, original) → "replay.{runID}.{original}"` enforces both reserved-prefix rules from subject-taxonomy.md §6 (rejects originals already under `replay.` or `dlq.`). `ConsumerGroup(runID) → "replay-{runID}"` is the durable-consumer / consumer-group ID replay-scoped subscribers MUST use so their offsets can never collide with live consumer groups — the consumer-group half of EVT-20b's isolation. `Source` interface (just `Next`) decouples Pipeline from the concrete EVT-20a Reader so it's fakeable in unit tests. `Pipeline {RunID, Source, Publisher, Logger}` drains Source → re-marshals each event into a fresh `EventFrame` → republishes to the SubjectFor-rewritten subject via the raw `bus.Publisher` (not Producer — we're replaying pre-stamped envelopes, not stamping fresh ones). Headers (notably `Nats-Msg-Id`) ride through so destination-stream dedup catches duplicate deliveries from the source range, while being per-stream means no cross-stream interference with the original live publication. Retrofit of EVT-20a: per-message unframe failures are now wrapped as `*MalformedFrameError` so Pipeline cleanly discriminates skippable-and-log from fatal-source-error via `errors.As`. Tests cover RunID format/distinctness/sortability, SubjectFor validation matrix, ConsumerGroup, Pipeline happy-path (with full envelope round-trip + header propagation), malformed-skip, fatal-source-abort, publish-error-abort, field validation, and wrapped-MalformedFrameError still routes to skip · `kanz/tools/replay/`
- [x] **EVT-20a** Replay: Kafka log reader. New `kanz/tools/replay/` package. `Reader` is a one-shot iterator (`Next` → io.EOF) over a finite Kafka log window; `Range` requires an explicit end bound (`EndOffset` inclusive, or `EndTime` exclusive) so the read terminates without a high-water-mark probe. Optional `StartOffset` / `StartTime` (mutually exclusive; default = `kafka.FirstOffset`). Per-partition readers under `kafka-go` with **no `GroupID`** — replays never mutate live consumer-group positions. Partition discovery via `kafka.Dialer.DialContext` + `conn.ReadPartitions`; optional `Config.Partitions` short-circuits the dial. Per-partition goroutine fans into a buffered channel multiplexed by `Next`. Each delivered `Event` carries the unframed `*envelopepb.Envelope`, `Payload` bytes, and Kafka coordinates (`Topic`/`Partition`/`Offset`/`KafkaTime`/`Key`/`Headers`); malformed frames surface as `(Event{kafka-coords-only}, err)` so investigators can log + skip without aborting the run. Per-message `Validate` is deliberately not run — replay must surface what's *in* the log, not filter it. Unit tests cover `Range` validation matrix + `NewReader` defaults + explicit-partition dedup/sort; integration test gated on `TEST_KAFKA_BROKERS` exercises offset-range/time-range/partition-restriction and the malformed-frame error path · `kanz/tools/replay/`
- [x] **EVT-19c** TS bus client lineage propagation: `withPropagation(ctx, fn)` wraps `AsyncLocalStorage.run` with the same "non-empty fields only" rule as Go's `WithTraceContext` skip-on-empty and Python's `propagation_context` partial-set. TS API shape is a function-taking wrapper (not a context manager / decorator) because `AsyncLocalStorage.run(store, callback)` already binds the store to a callback — the wrapper just computes `next = {explicit || outer || ""}` per-field and forwards. `Consumer` wraps a `Subscriber`, runs `unframe → validate → withPropagation({correlationId, causationId: env.eventId, traceContext}, () => handler(env, payload))`; pre-dispatch errors propagate out so the underlying NATSClient/KafkaClient triggers broker redelivery (nak / skip-commit) — bounded retry + DLQ are outside EVT-19 scope per the task board, so handlers must be idempotent (event-class-rules §1) and broker redelivery is the only recovery primitive. `EventHandler = (Envelope, Uint8Array) => Promise<void>` has no explicit context arg — lineage is implicit via the async-store, same idiom as Python's `EventHandler`. End-to-end Consumer→Producer chain test asserts inheritance of correlation/causation/trace; tests also cover explicit-beats-store precedence, empty-trace-doesn't-clobber-outer, unframe/validate failure surfacing with handler-not-called, async-store reset after handler, and propagation persisting across `await` + a `.then()`-chained child task (TS analog of asyncio.create_task copy semantics). EVT-19 epic complete: TS client is now end-to-end runnable and wire-compatible with Go EVT-17 + Python EVT-18 · `kanz/ts/kanz-bus/`
